spec: task
name: "第 16 章：从 Claude Code 到通用原则"
inherits: project
tags: [book-chapter, part-8]
---

## 意图

从前面 15 章的源码分析中提炼一套可复用的 Harness 工程决策框架。每条原则都有源码锚点支撑，帮助读者回答"什么应该由模型管理，什么必须由 Harness 控制"这个永恒问题。读完本章，读者将获得一份"设计你自己的 Harness"的检查清单，每一条都对应 Claude Code 源码中的真实决策。

## 约束

必须：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个 Mermaid 流程图或架构图
- ⛔ 写作风格：projector（零代码展示，源码仅作为隐含锚点）
- 章节结构遵循 projector 九段式：开篇→问题→黄金法则→适用场景→工作原理→权衡→踩坑指南→实证→章末主成分提炼
- 全文不出现代码块
- 跨章节引用使用"详见第 X 章"格式
- 每条原则必须关联至少一章的具体源码分析

禁止：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现空洞表述（"介绍了…"/"描述了…"）
- 不得展示代码
- 不得引入前 15 章未涉及的新源码模块

## 已定决策
- 写作语言：中文正文 + 英文技术术语保留原文
- ⛔ 写作风格：projector
- 章节结构：projector 九段式
- 源码引用格式：`相对路径:行号`
- 图表：Mermaid graph TD/LR
- 本章为全书终章，必须回扣全部前 15 章的分析

## 边界

### 允许修改
- book/src/part8/ch16.md

### 禁止做
- 不修改 DESIGN.md
- 不修改其他章节文件
- 不分析排除范围中的模块
- 不引入前 15 章未覆盖的新源码模块

## 完成条件

场景: 源码引用有效
  测试: manual-qa
  假设 读者跟随章节中的源码引用
  当 读者在项目目录中查找引用路径
  那么 每处引用都存在于实际文件中

场景: 零代码展示
  测试: manual-qa
  假设 读者检查本章全文
  当 读者搜索代码块标记
  那么 本章不包含任何代码块

场景: 投影师风格合规
  测试: manual-qa
  假设 DESIGN.md 声明 projector 风格
  当 读者检查章节结构
  那么 遵循九段式结构且不展示代码

场景: 不引入排除范围内容
  测试: manual-qa
  假设 DESIGN.md 列出了排除范围
  当 读者检查本章引用的源码路径
  那么 没有引用落在排除范围中

场景: 语言精确性
  测试: manual-qa
  假设 读者检查本章用词
  当 搜索空洞动词和模糊量词
  那么 已替换为具体数字和精确表述

场景: 章节间链接完整
  测试: manual-qa
  假设 本章为全书终章
  当 读者检查章节内引用
  那么 覆盖全部前 15 章的"详见第 X 章"链接

场景: 图表可渲染
  测试: manual-qa
  假设 读者在 Mermaid 环境中阅读
  当 读者遇到图表部分
  那么 Mermaid 代码块语法正确

场景: 推断有标注
  测试: manual-qa
  假设 章节中有无法直接从源码证明的结论
  当 读者检查这些结论
  那么 每处推断都有"（推断）"标注

## 排除范围
- assistant/、server/、ssh/、proactive/、bridge/ 中的 stub 文件
- 功能使用教程
- UI 组件渲染逻辑
- 与 Harness 无关的辅助功能
