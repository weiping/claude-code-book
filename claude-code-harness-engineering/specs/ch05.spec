spec: task
name: "第 5 章：提示词的分层构建"
inherits: project
tags: [book-chapter, part-3]
---

## 意图

揭示 Claude Code 如何通过分段式系统提示词实现 Prompt Cache 最大化命中——静态内容在前、动态内容在后，每节有独立缓存语义。记忆注入通过 MEMORY.md 参与上下文构建。读完本章，读者将理解为什么系统提示词被拆分为 20+ 个 section 而非一整块文本，以及这种分段如何直接映射到 Anthropic API 的缓存边界。

## 约束

必须：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个 Mermaid 流程图或架构图
- ⛔ 写作风格：projector（零代码展示，源码仅作为隐含锚点）
- 章节结构遵循 projector 九段式：开篇→问题→黄金法则→适用场景→工作原理→权衡→踩坑指南→实证→章末主成分提炼
- 全文不出现代码块
- 跨章节引用使用"详见第 X 章"格式

禁止：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现空洞表述（"介绍了…"/"描述了…"）
- 不得展示代码

## 已定决策
- 写作语言：中文正文 + 英文技术术语保留原文
- ⛔ 写作风格：projector
- 章节结构：projector 九段式
- 源码引用格式：`相对路径:行号`
- 图表：Mermaid graph TD/LR

## 边界

### 允许修改
- book/src/part3/ch05.md

### 禁止做
- 不修改 DESIGN.md
- 不修改其他章节文件
- 不分析排除范围中的模块

## 完成条件

场景: 源码引用有效
  测试: manual-qa
  假设 读者跟随章节中的源码引用
  当 读者在项目目录中查找引用路径
  那么 每处引用都存在于实际文件中

场景: 零代码展示
  测试: manual-qa
  假设 读者检查本章全文
  当 读者搜索代码块标记
  那么 本章不包含任何代码块

场景: 投影师风格合规
  测试: manual-qa
  假设 DESIGN.md 声明 projector 风格
  当 读者检查章节结构
  那么 遵循九段式结构且不展示代码

场景: 不引入排除范围内容
  测试: manual-qa
  假设 DESIGN.md 列出了排除范围
  当 读者检查本章引用的源码路径
  那么 没有引用落在排除范围中

场景: 语言精确性
  测试: manual-qa
  假设 读者检查本章用词
  当 搜索空洞动词和模糊量词
  那么 已替换为具体数字和精确表述

场景: 章节间链接完整
  测试: manual-qa
  假设 本章依赖或为其他章提供基础
  当 读者检查章节内引用
  那么 有正确的"详见第 X 章"链接

场景: 图表可渲染
  测试: manual-qa
  假设 读者在 Mermaid 环境中阅读
  当 读者遇到图表部分
  那么 Mermaid 代码块语法正确

场景: 推断有标注
  测试: manual-qa
  假设 章节中有无法直接从源码证明的结论
  当 读者检查这些结论
  那么 每处推断都有"（推断）"标注

## 排除范围
- assistant/、server/、ssh/、proactive/、bridge/ 中的 stub 文件
- 功能使用教程
- UI 组件渲染逻辑
- 与 Harness 无关的辅助功能
