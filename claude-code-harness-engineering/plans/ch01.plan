# 第 1 章写作计划

**批次**：批次 1（来自 plans/batch.md）
**章节**：ch01 — 当模型遇见 Harness
**风格**：projector

## 章节意图（从 spec `## 意图` 提取）

回答一个根本问题：Claude Code 的 51 万行代码到底在解决什么问题？本章从全局视角展示 Harness 控制层的完整边界，让读者理解"模型是引擎，Harness 是车身"这个类比在真实工程中的具体映射。读完本章，你将获得一张全书导航地图，知道后续每一章在全局架构中的位置。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**：
- 每个架构层次有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个全局架构分层 Mermaid 图（graph TD）
- 每节采用 projector 风格：现象描述→简洁原则→读后收获
- 跨章节引用使用"详见第 X 章"格式

**通用禁止**：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得深入子系统实现细节（留给后续各章）
- 不得出现代码展示

**排除范围**：
- assistant/、server/、ssh/、proactive/、bridge/ 中的 stub 文件
- 功能使用教程
- UI 组件渲染逻辑
- 与 Harness 无关的辅助功能

## ⛔ 风格专属写作要求

**开篇格式**：projector 模式预告框架（80-120字：复杂系统现象→揭示简洁原则→价值承诺）
- 51万行代码只为一个API调用服务——这个反直觉的事实揭示了Harness工程的核心：模型能力之外的一切都是可学习、可迁移的工程架构。读完本章你将看到Claude Code的全局架构地图。

**分节组织**：projector 九段式结构（开篇→问题→黄金法则→适用场景→工作原理→权衡→踩坑指南→实证→主成分提炼）

**章末结构**：projector 主成分提炼（本质、关键机制、适用边界、与其他模式的关系）

**风格专属必须**：
- 全文不出现代码块
- 源码仅作为隐含锚点出现，用文字描述架构关系
- 每个技术分析最终收束为一条可迁移的工程原则
- 原则格式为「**原则 N.N：[原则名]** — [一句话阐述]」
- 覆盖全部后续 15 章的"详见第 X 章"链接

**风格专属禁止**：
- 不得展示代码片段
- 不得逐行解读源码
- 不得使用 autopsy/detective/mentor/hunter 的开篇方式
- 不得使用其他风格的章末结构

**通用写作铁律**（写作期间必须遵守）：
- ⛔ 源码第一：每个断言必须附 `路径:行号` 证据，禁止基于 AI 记忆写作
- ⛔ 代码无裸露：projector 风格下为"不展示代码"
- ⛔ 数据驱动：不用模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文
- ⛔ 开篇铁律：开篇在初稿完成后最后补写，禁止直接引用源码

## 前置阅读清单
（按阅读顺序排列，不超过 6 个文件）

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | ARCHITECTURE_ANALYSIS.md | 三层架构 + 九子系统划分，全系统数据流图 | 行 1-200（全文件） |
| 2 | src/main.tsx | 启动序列（profileCheckpoint/startMdmRawRead/feature/Commander注册） | 行 1-35, 580-620 |
| 3 | src/QueryEngine.ts | QueryEngine 类结构和 submitMessage 方法 | 行 130-270 |
| 4 | src/Tool.ts | buildTool 工厂和 Tool 接口 | 行 700-792 |
| 5 | src/tools.ts | 工具注册和 feature flag 条件导入 | 行 1-100 |
| 6 | src/bootstrap/state.ts | 全局会话状态管理函数 | 行 1-100, 430-630 |

验证文件存在：
```bash
ls ARCHITECTURE_ANALYSIS.md src/main.tsx src/QueryEngine.ts src/Tool.ts src/tools.ts src/bootstrap/state.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用应存在于实际文件中 | 章节骨架各节的源码锚点 | ☐ |
| A2 | 推断有标注 | 每处推断有"（推断）"标注 | 章节骨架各节 | ☐ |
| A3 | 不引用排除范围 | 没有任何引用落在排除范围中 | 章节骨架各节 | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 M 章"代替重复 | 章节骨架各节 | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全文 | ☐ |
| B2 | 数据驱动 | "很多"/"通常"等模糊词已替换为具体数字 | 全文 | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 全文 | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | projector 九段式递进 | 全文结构 | ☐ |
| C2 | ⛔ 风格专属结构 | 九段式：开篇→问题→黄金法则→适用场景→工作原理→权衡→踩坑指南→实证→主成分提炼 | 全文结构 | ☐ |
| C3 | 核心主张可提炼 | 可提炼出 1-2 句答案 | 节 1.2 黄金法则 | ☐ |
| C4 | 设计权衡有说明 | 有"选 A 不选 B 的原因" | 节 1.5 权衡 | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末：用户能做什么 | ☐ |
| C6 | 章节间链接 | 覆盖全部后续 15 章的链接 | 章末 + 各节 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | Mermaid graph TD 语法正确，本章≥2 图 | 图 1-1、图 1-2、图 1-3 | ☐ |
| D2 | 图表标注格式 | 上方有标题，下方有说明 | 图表清单 | ☐ |
| D3 | 代码片段长度 | 全文不出现代码块（projector 零代码） | 全文 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 章节骨架各节 | ☐ |
| D5 | ⛔ 风格专属图表 | projector 架构图和子系统关系图，不展示代码运行流程 | 图 1-1、图 1-2 | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | ⛔ 写作风格符合性 | projector 九段式，零代码，原则驱动，模式预告框架开篇 | 全文结构 | ☐ |
| E2 | 读者对话感 | "我们"而非"用户"，有预告和设问 | 全文 | ☐ |
| E3 | 关键信息突出 | 加粗标注，表格展示对比 | 全文 | ☐ |

## 章节骨架

### 开篇（必填，projector 模式预告框架）
> 51万行代码只为一个API调用服务——这个反直觉的事实揭示了Harness工程的核心真相。

- 可选题词：「模型是引擎，Harness 是车身。引擎决定动力，车身决定你能开到哪里。」

### 节 1.1：问题——51 万行代码在解决什么问题
- 核心主张：Claude Code 的代码量不是功能堆砌，而是围绕"让模型稳定、安全、高效地完成任务"这个单一目标构建的精密控制层——也就是 Harness（安全带）
- 源码锚点：
  - `src/main.tsx:12` — `profileCheckpoint('main_tsx_entry')` — 进程入口第一行就打上了性能标记
  - `src/QueryEngine.ts:184` — `export class QueryEngine` — 核心循环类，一个会话一个实例
  - `src/Tool.ts:783` — `export function buildTool` — 工具工厂，定义了 Harness 与模型的能力边界
- 概念层次：定义（什么是 Harness）→ 设计意图（为什么需要 51 万行）→ 实际案例（各子系统代码量统计）
- 权衡分析：N/A（本节是问题定义，不涉及设计选择）
- 包含元素：表格（三层 × 九子系统的代码量分布）
- **⛔ 内容密度规划**：
  - 引入段要点：用"裸调 API 只需 10 行代码"与"Claude Code 用了 51 万行"的反差制造张力
  - 解读段要点：揭示三个架构层次——交互层处理用户输入，Harness 控制层是 51 万行代码的真正归宿（9 个子系统），模型层封装 API 通信。每层有清晰的职责边界和源码位置
  - 散文与代码比例目标：纯散文（projector 零代码），100% 散文
- 估计字数：800-1200 字
- 对应 spec 场景：A1（源码引用有效性）、B2（数据驱动）

### 节 1.2：黄金法则——Harness 的本质是回答一个问题
- 核心主张：Claude Code 的 51 万行代码本质上都在回答一个永恒问题——"什么应该由模型管理，什么必须由 Harness 控制"
- 源码锚点：
  - `src/Tool.ts:783` — `export function buildTool` — 工具工厂定义了 Harness 控制边界
  - `src/Tool.ts:761-766` — TOOL_DEFAULTS 默认值策略：`isConcurrencySafe → false`、`isReadOnly → false`（fail-closed 原则）
- 概念层次：设计意图 → 实际案例（buildTool 默认值如何编码 fail-closed 安全策略）
- 权衡分析：fail-closed（默认拒绝）vs fail-open（默认允许）——Claude Code 选择 fail-closed，牺牲便利性换取安全性
- 包含元素：对比表格（模型控制 vs Harness 控制）
- **⛔ 内容密度规划**：
  - 引入段要点：提出核心问题——"什么由模型管理，什么由 Harness 控制"，这是全书的主线
  - 解读段要点：用 buildTool 的默认值策略作为具体例证——`isConcurrencySafe` 默认 false 说明 Harness 不信任工具自行声明安全性。这条原则贯穿九个子系统
  - 散文与代码比例目标：100% 散文
- 估计字数：600-900 字
- 对应 spec 场景：C3（核心主张可提炼）、C4（设计权衡有说明）、E1（风格符合性）

### 节 1.3：适用场景——谁需要 Harness 工程
- 核心主张：任何构建 AI Agent 应用的人都在做 Harness 工程，只是大多数人没有意识到
- 源码锚点：
  - `src/tools.ts:195` — 工具注册列表（AgentTool、BashTool、FileEditTool 等 54 个工具）
  - `src/main.tsx:80` — `feature('KAIROS')` 条件导入——Feature Flag 控制能力边界
- 概念层次：定义（谁在做 Harness）→ 设计意图（为什么模式可以迁移）
- 权衡分析：N/A
- 包含元素：表格（三类读者及其场景）
- **⛔ 内容密度规划**：
  - 引入段要点：从"你在写 AI 应用时，有没有遇到过模型不听话"切入
  - 解读段要点：说明 54 个工具（19 始终启用 + 35 条件启用）如何映射到通用 Agent 应用的需求——文件操作、命令执行、网络搜索。这些需求是跨应用通用的
  - 散文与代码比例目标：100% 散文
- 估计字数：400-600 字
- 对应 spec 场景：E2（读者对话感）

### 节 1.4：工作原理——三层架构与九个子系统
- 核心主张：Claude Code 的架构可以清晰地分为三层，其中 Harness 控制层包含 9 个职责明确的子系统，每个子系统对应源码中独立的模块集群
- 源码锚点：
  - **交互层**：`src/main.tsx:968` — Commander 命令注册入口；`src/screens/REPL.tsx:1` — 5005 行的 Ink/React TUI 组件
  - **Harness 控制层 · 启动系统**：`src/main.tsx:12-20` — `profileCheckpoint → startMdmRawRead → startKeychainPrefetch` 三路并行启动；`src/setup.ts:477` — 会话初始化
  - **Harness 控制层 · 核心循环**：`src/QueryEngine.ts:184` — `QueryEngine` 类；`src/query.ts:219` — `query()` async generator
  - **Harness 控制层 · 提示词管理**：`src/constants/prompts.ts:914` — `getSystemPrompt()` 入口；`src/utils/claudemd.ts:60` — CLAUDE.md 分层加载
  - **Harness 控制层 · 工具编排**：`src/services/tools/toolOrchestration.ts:150` — `runTools()` 并发/串行分区；`src/services/tools/toolExecution.ts:337` — `runToolUse()` 单工具执行
  - **Harness 控制层 · 上下文管理**：`src/state/AppStateStore.ts:1` — AppState 定义；`src/services/compact/compact.ts:1` — 压缩系统
  - **Harness 控制层 · 权限护栏**：`src/utils/permissions/` — 9409 行的权限子系统（推断）；`src/utils/hooks.ts:1` — 5022 行的 Hook 系统
  - **Harness 控制层 · 任务编排**：`src/tasks/LocalAgentTask/LocalAgentTask.tsx:1` — 后台任务类型之一
  - **Harness 控制层 · 多智能体**：`src/tools/AgentTool/AgentTool.tsx:196` — `AgentTool = buildTool(...)`；`src/utils/swarm/` — Swarm/Teammate 系统
  - **模型层**：`src/services/api/claude.ts:1` — 3419 行的 API 客户端（流式/非流式）
- 概念层次：定义（三层是什么、九个子系统分别做什么）→ 设计意图（为什么要这样划分）→ 实际案例（各层/子系统在源码中的位置和代码量）
- 权衡分析：三层划分 vs 平铺——三层带来清晰的职责分离（交互层只管渲染、模型层只管通信、Harness 做所有"智能"决策），但增加跨层调用复杂度。Claude Code 选择清晰性优先，因为可维护性比运行时开销更重要
- 包含元素：
  - 图 1-1：三层 × 九子系统架构总图（Mermaid graph TD）
  - 图 1-2：Harness 控制层九子系统关系图（Mermaid graph TD，展示子系统间依赖）
  - 表格（三层职责 × 九子系统代码量 × 对应源码路径）
- **⛔ 内容密度规划**：
  - 引入段要点：将 51 万行代码映射为三层 + 九个子系统，每层/子系统有明确的输入输出和源码边界
  - 解读段要点：
    - 先从宏观分三层：交互层（CLI + TUI，~60K 行）→ Harness 控制层（~400K 行，9 个子系统）→ 模型层（API 通信，~35K 行）
    - 再聚焦 Harness 控制层，逐一介绍九个子系统的一句话定位和核心职责：
      1. **启动系统**——从进程入口到 REPL 就绪（详见第 2 章）
      2. **核心循环**——"输入→API→工具→继续"的无限循环状态机（详见第 3 章）
      3. **提示词管理**——动态构建 system prompt + 上下文注入（详见第 5 章）
      4. **工具编排与执行**——54 个工具的并发/串行编排 + 权限检查（详见第 7 章）
      5. **上下文管理**——会话状态 + 五级压缩策略保护 token 窗口（详见第 11 章）
      6. **权限护栏**——四层防御（规则→分类器→Hook→交互确认）（详见第 9 章）
      7. **任务编排**——后台任务生命周期 + Plan 模式（详见第 8 章）
      8. **多智能体**——子智能体派生 + Swarm/Coordinator 协作（详见第 13 章）
      9. **辅助系统**——API 客户端、费用追踪、设置管理、模型选择等
    - 每个子系统用"解决什么问题 → 源码位置 → 代码量"一句话带过，深入留给后续各章
  - 散文与代码比例目标：100% 散文
- 估计字数：1200-1600 字
- 对应 spec 场景：A1、C6（章节间链接）、D1（图表可渲染）、E1

### 节 1.5：权衡——三层架构的设计选择
- 核心主张：Claude Code 的每个架构决策都是权衡的结果，理解权衡比记住结构更重要
- 源码锚点：
  - `src/main.tsx:76` — `feature('COORDINATOR_MODE')` — Feature Flag 编码了"模型能力假设"
  - `src/main.tsx:80` — `feature('KAIROS')` — 条件导入实现死代码消除
  - `src/bootstrap/state.ts:1` — 全局状态集中管理，1762 行的单一状态模块
- 概念层次：设计意图 → 实际案例
- 权衡分析：
  - Feature Flag vs 配置文件：Feature Flag 编码在编译时（`bun:bundle`），实现零运行时开销；代价是灵活性降低——部署后无法动态开关
  - 集中状态 vs 分散状态：`bootstrap/state.ts` 集中管理 100+ 个状态函数，好处是单一来源（Single Source of Truth），代价是文件膨胀到 1762 行
  - 九子系统 vs 更粗粒度划分：九个子系统带来精准的职责分离（如提示词管理与上下文管理分开），但增加了跨子系统协调的复杂度——例如核心循环需要同时与提示词管理、工具编排、上下文管理、权限护栏四个子系统交互
- 包含元素：对比表格（三个权衡点）
- **⛔ 内容密度规划**：
  - 引入段要点：架构不是对错问题，是权衡问题
  - 解读段要点：逐一分析三个关键权衡——Feature Flag 的编译时编码、集中状态的单一来源、九子系统的精准 vs 复杂
  - 散文与代码比例目标：100% 散文
- 估计字数：600-900 字
- 对应 spec 场景：C4（设计权衡有说明）、E3（关键信息突出）

### 节 1.6：踩坑指南——从源码中看到的工程陷阱
- 核心主张：Claude Code 的源码揭示了一些常见的 Harness 工程陷阱
- 源码锚点：
  - `src/main.tsx:2-9` — 注释明确列出启动前必须完成的三个副作用操作
  - `src/Tool.ts:761-766` — TOOL_DEFAULTS 的 fail-closed 设计
- 概念层次：实际案例 → 可迁移原则
- 权衡分析：N/A
- 包含元素：列表（3-4 个陷阱）
- **⛔ 内容密度规划**：
  - 引入段要点：学习最佳实践前，先知道最常见的错误是什么
  - 解读段要点：陷阱一——"裸调 API 没有启动序列"（交互层和 Harness 层混淆），陷阱二——"工具默认不安全"（权限护栏子系统缺失），陷阱三——"上下文无上限增长"（上下文管理子系统缺失）
  - 散文与代码比例目标：100% 散文
- 估计字数：400-600 字
- 对应 spec 场景：B1（禁止空洞表述）

### 节 1.7：实证——三层架构在源码中的验证
- 核心主张：三层 + 九子系统架构不是理论模型，可以从源码的目录结构、import 关系和函数调用链中验证
- 源码锚点：
  - **交互层证据**：`src/main.tsx:968` — Commander 命令解析分支；`src/screens/REPL.tsx:1` — 5005 行 TUI 主组件；`src/commands/` — 103 个命令目录——全部职责是"用户输入处理和输出渲染"
  - **Harness 控制层证据**：`src/QueryEngine.ts:130` — `QueryEngineConfig` 类型定义了 30+ 个配置项，精确划定层间接口；`src/tools.ts:195` — 54 个工具注册列表体现工具编排子系统的边界；`src/utils/permissions/` — 9409 行代码、24 个文件的权限子系统目录
  - **模型层证据**：`src/services/api/claude.ts:1` — 3419 行 API 客户端，唯一的 API 通信入口；`src/utils/model/` — 模型选择和提供商检测（推断）
- 概念层次：实际案例 → 验证方法
- 权衡分析：N/A
- 包含元素：
  - 图 1-3：用户输入到 API 调用的完整数据流（Mermaid graph TD，标注三层边界）
  - 表格（三层 × 源码验证点）
- **⛔ 内容密度规划**：
  - 引入段要点：理论需要证据。让我们从源码中验证三层架构的真实性
  - 解读段要点：
    - 交互层验证：`main.tsx` 中的 Commander 分支和 REPL 组件，职责是"解析输入、渲染输出"，不含任何业务逻辑
    - Harness 控制层验证：`QueryEngineConfig` 的 30+ 个配置项精确定义了 Harness 需要的全部依赖——tools、commands、mcpClients、canUseTool——但不包含任何 API 通信细节。54 个工具注册在 `tools.ts` 中形成工具子系统的边界。9409 行权限代码形成独立的护栏子系统
    - 模型层验证：`services/api/claude.ts` 是唯一的 API 通信入口，3419 行代码封装了流式/非流式请求、Bedrock/Vertex 多提供商支持，但不知道工具是什么、权限怎么检查
    - 三层之间的依赖方向是单向的：交互层 → Harness 控制层 → 模型层。反向不存在直接依赖
  - 散文与代码比例目标：100% 散文
- 估计字数：800-1100 字
- 对应 spec 场景：A1、D1、C6

### 章末结构（projector 主成分提炼）

**主成分提炼**：Claude Code 的架构本质
- 本质：围绕"什么由模型管理，什么由 Harness 控制"这个核心问题组织的三层控制架构——交互层管输入输出，Harness 控制层管所有"智能"决策（9 个子系统），模型层管通信
- 关键机制：Feature Flag 编码能力假设（启动系统）、buildTool 统一工具接口（工具编排）、QueryEngine 管理会话生命周期（核心循环）、四层权限防御（权限护栏）
- 适用边界：适用于需要稳定、安全、可扩展的 Agent 系统；不适用于一次性脚本或简单 API 封装
- 与其他模式的关系：Harness 三层架构是 Agent 框架的底层模式，上层可以是 ReAct、Plan-and-Execute 等推理模式

### 章末：用户能做什么（必填）
预规划 5-8 条行动建议：
1. 用三层 + 九子系统架构模型审视你自己的 AI 应用，画出三层边界
2. 检查你的工具定义是否有 fail-closed 默认值策略（Harness 控制层·工具编排）
3. 识别你的应用中"交互层"和"Harness 控制层"是否混淆在一起
4. 对照 Claude Code 的启动序列，审视你的初始化是否有并行优化空间（详见第 2 章）
5. 画出你应用的子系统地图，对照九子系统看缺少哪些能力
6. 用 Feature Flag 的思路评估你应用中硬编码的"模型能力假设"（详见第 15 章）
7. 检查你的上下文管理是否有压缩策略（详见第 11 章）
8. 阅读后续章节前，带着"这个子系统解决了什么权衡"的问题意识

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图1-1 | Claude Code 三层架构总图 | Mermaid graph TD | 交互层（CLI/TUI）→ Harness 控制层（九子系统）→ 模型层（API）的分层关系，标注各子系统和源码路径 | 1.4 |
| 图1-2 | Harness 控制层九子系统关系图 | Mermaid graph TD | 核心循环为中心，向提示词管理、工具编排、上下文管理、权限护栏、任务编排、多智能体辐射的子系统依赖关系 | 1.4 |
| 图1-3 | 三层架构数据流验证 | Mermaid graph TD | 从用户输入经交互层→Harness 控制层（九子系统协作）→模型层的完整数据流，标注三层边界和层间接口 | 1.7 |

## 写作顺序

1. ⛔ **确认完整 spec 与风格规则**：重新读取完整 spec 文件和 DESIGN.md 中的 STYLE_ID，确认全部 16 项 spec 要素已在 plan 中反映
2. 先写 1.4 节（三层架构与九个子系统）——这是本章的核心内容，确立全局视图
3. 然后写 1.1 节（问题）——铺垫问题，引向 1.4 的答案
4. 写 1.2 节（黄金法则）——提炼核心原则
5. 写 1.3 节（适用场景）——建立读者共鸣
6. 写 1.5 节（权衡）——深化理解
7. 写 1.6 节（踩坑指南）——实用价值
8. 写 1.7 节（实证）——用源码验证三层架构
9. 写章末结构（主成分提炼 + 用户能做什么）
10. 最后补写开篇（此时知道全章讲了什么，才能写出符合 projector 模式预告框架的开篇）

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
# 交互层
sed -n '968p' src/main.tsx                                  # Commander 注册
head -1 src/screens/REPL.tsx                                 # TUI 主组件
ls src/commands/                                             # 103 个命令目录

# Harness 控制层 — 启动系统
sed -n '12p' src/main.tsx                                    # profileCheckpoint
sed -n '477p' src/setup.ts                                   # setup() 尾部

# Harness 控制层 — 核心循环
sed -n '184p' src/QueryEngine.ts                             # QueryEngine 类
sed -n '219p' src/query.ts                                   # query() 函数

# Harness 控制层 — 提示词管理
wc -l src/constants/prompts.ts                               # 提示词构建
head -60 src/utils/claudemd.ts                               # CLAUDE.md 加载

# Harness 控制层 — 工具编排
sed -n '150p' src/services/tools/toolOrchestration.ts        # runTools()
sed -n '337p' src/services/tools/toolExecution.ts            # runToolUse()

# Harness 控制层 — 上下文管理
head -1 src/state/AppStateStore.ts                            # AppState
head -1 src/services/compact/compact.ts                       # 压缩系统

# Harness 控制层 — 权限护栏
wc -l src/utils/hooks.ts                                     # Hook 系统 5022 行
find src/utils/permissions/ -type f -name '*.ts' | wc -l     # 权限文件数

# Harness 控制层 — 任务编排
head -1 src/tasks/LocalAgentTask/LocalAgentTask.tsx           # 后台任务

# Harness 控制层 — 多智能体
sed -n '196p' src/tools/AgentTool/AgentTool.tsx               # AgentTool
ls src/utils/swarm/                                          # Swarm 系统

# 模型层
wc -l src/services/api/claude.ts                             # API 客户端 3419 行

# 通用锚点
sed -n '76p' src/main.tsx                                    # feature COORDINATOR_MODE
sed -n '80p' src/main.tsx                                    # feature KAIROS
sed -n '783p' src/Tool.ts                                    # buildTool
sed -n '130p' src/QueryEngine.ts                             # QueryEngineConfig
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`specs/ch01.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束 → 通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束 → 通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求 → 风格专属必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求 → 风格专属禁止 |
| 6 | 章节结构 | ✓ | 章节骨架（九段式） |
| 7 | 源码引用格式 | ✓ | 章节骨架 → 每节锚点格式 |
| 8 | 图表格式 | ✓ | 图表清单（Mermaid graph TD，3 张图） |
| 9 | 边界路径 | ✓ | 边界一致性检查（排除范围已列出） |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单（E>A>B=C>D） |
| 11 | 验收场景·A 源码锚点组 | ✓ | Spec 验收场景清单 A（4 场景） |
| 12 | 验收场景·B 语言精确性组 | ✓ | Spec 验收场景清单 B（3 场景） |
| 13 | 验收场景·C 结构完整性组 | ✓ | Spec 验收场景清单 C（6 场景） |
| 14 | 验收场景·D 图表与代码组 | ✓ | Spec 验收场景清单 D（5 场景） |
| 15 | 验收场景·E 风格符合性组 | ✓ | Spec 验收场景清单 E（3 场景） |
| 16 | 排除范围 | ✓ | 通用约束 → 排除范围 |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 3/3 = **21/21**
