# 第 16 章写作计划

**批次**：批次 8（来自 plans/batch.md）
**章节**：ch16 — 从 Claude Code 到通用原则
**风格**：projector

## 章节意图（从 spec `## 意图` 提取）

从前面 15 章的源码分析中提炼一套可复用的 Harness 工程决策框架。每条原则都有源码锚点支撑，帮助读者回答"什么应该由模型管理，什么必须由 Harness 控制"这个永恒问题。读完本章，读者将获得一份"设计你自己的 Harness"的检查清单，每一条都对应 Claude Code 源码中的真实决策。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个 Mermaid 流程图或架构图
- 跨章节引用使用"详见第 X 章"格式
- 每条原则必须关联至少一章的具体源码分析

**通用禁止**：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现空洞表述
- 不得展示代码
- 不得引入前 15 章未涉及的新源码模块

**排除范围**：
- assistant/、server/、ssh/、proactive/、bridge/ 中的 stub 文件
- 功能使用教程
- UI 组件渲染逻辑
- 与 Harness 无关的辅助功能
- 不引入前 15 章未覆盖的新源码模块

## ⛔ 风格专属写作要求

**开篇格式**：projector 模式预告框架（80-120字）
- 15 章源码分析，数百处锚点，最终归于一个问题：什么该交给模型，什么必须由 Harness 控制？本章不教你怎么复制 Claude Code 的实现——它给你一套决策框架，让你在任何 LLM 应用中回答同一个问题。

**分节组织**：projector 九段式结构

**章末结构**：projector 主成分提炼

**风格专属必须**：
- 全文不出现代码块
- 源码仅作为隐含锚点
- 原则格式「**原则 N.N：[原则名]** — [一句话阐述]」
- 覆盖全部前 15 章的"详见第 X 章"链接

**风格专属禁止**：
- 不得展示代码
- 不得使用其他风格的开篇或章末
- 不得引入新的源码模块

**通用写作铁律**：
- ⛔ 源码第一：每个断言附 `路径:行号` 证据
- ⛔ 数据驱动：具体数字（引用前 15 章的具体数据点）
- ⛔ 术语标准化：首次出现标注原文
- ⛔ 开篇铁律：最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 对应章节 |
|---|---------|---------|---------|
| 1 | 前述所有 15 章已分析的核心源码文件 | 本章不引入新模块，只综合前 15 章的分析 | ch01-ch15 |
| 2 | plans/ch01.plan - plans/ch15.plan | 已有的源码锚点和原则 | 全部 |

## Spec 验收场景清单

**验收优先级**：E > A > B = C > D

### A. 源码锚点组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效 | 每处引用存在 | 各节锚点 | ☐ |
| A2 | 推断有标注 | 推断有"（推断）" | 各节 | ☐ |
| A3 | 不引用排除范围 | 无排除路径 | 全文 | ☐ |
| A4 | 不重复其他章内容 | "详见第 X 章" | 各节 | ☐ |

### B. 语言精确性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 无空洞动词 | 全文 | ☐ |
| B2 | 数据驱动 | 具体数字 | 全文 | ☐ |
| B3 | 术语标准化 | 首次标注 | 全文 | ☐ |

### C. 结构完整性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | 九段式 | 全文 | ☐ |
| C2 | 风格专属结构 | 九段式完整 | 全文 | ☐ |
| C3 | 核心主张可提炼 | 1-2 句 | 节 16.2 | ☐ |
| C4 | 设计权衡有说明 | 选 A 不选 B | 节 16.5 | ☐ |
| C5 | 章末行动建议 | 5-8 条 | 章末 | ☐ |
| C6 | 章节间链接 | 覆盖全部 15 章 | 全文 | ☐ |

### D. 图表与代码组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | ≥2 图 | 图 16-1、16-2 | ☐ |
| D2 | 图表标注格式 | 标题+说明 | 图表清单 | ☐ |
| D3 | 代码片段长度 | 零代码块 | 全文 | ☐ |
| D4 | 源码注释引用 | 注释+路径 | 各节 | ☐ |
| D5 | 风格专属图表 | projector 架构图 | 图 16-1、16-2 | ☐ |

### E. 风格符合性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | 写作风格符合性 | projector 九段式 | 全文 | ☐ |
| E2 | 读者对话感 | "我们"+预告 | 全文 | ☐ |
| E3 | 关键信息突出 | 加粗+表格 | 全文 | ☐ |

## 章节骨架

### 开篇（projector 模式预告框架）
- 可选题词：「15 章之后，问题没有变——什么交给模型，什么交给代码？但答案变了：这不是一个二选一，而是一个有 7 条原则的决策框架。」

### 节 16.1：问题——"模型 vs Harness"是一个伪问题
- 核心主张：前 15 章的分析反复证明，"模型"和"Harness"不是竞争关系——Harness 编码的是模型在当前版本中做不到的事，而 feature flag 机制确保这些代码在模型足够好时会消失
- 源码锚点（仅引用前 15 章已覆盖的）：
  - `src/utils/bunFeatures.ts:25` — feature() 默认 false（详见第 15 章）
  - `src/services/compact/autoCompact.ts:70` — MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3（详见第 11 章）——Harness 补偿模型摘要生成的不稳定性
  - `src/tools/AgentTool/built-in/verificationAgent.ts:53` — RECOGNIZE YOUR OWN RATIONALIZATIONS（详见第 13 章）——Harness 补偿模型自我评估的偏差
- 概念层次：定义（为什么"vs"是错误的框架）→ 设计意图（正确的框架是什么）
- 权衡分析：N/A
- 包含元素：表格（15 章中 Harness 补偿的具体模型能力不足）
- **⛔ 内容密度规划**：
  - 引入段要点：15 章的分析指向一个结论——Harness 不是在"与模型竞争"，是在"等待模型足够好"
  - 解读段要点：压缩策略补偿上下文窗口限制、verification agent 补偿自我评估偏差、coordinator 补偿单 Agent 并行能力不足、feature flag 确保这些补偿是临时的
  - 散文与代码比例目标：100% 散文
- 估计字数：600-900 字
- 对应 spec 场景：A1、C6

### 节 16.2：黄金法则——7 条 Harness 工程原则
- 核心主张：从 15 章的分析中可以提炼 7 条可复用的原则，每条都有 Claude Code 源码中的真实决策支撑
- 7 条原则及其源码锚点：

**原则 16.1：能力边界硬编码，行为指导软编码** — 用工具禁用（disallowedTools）而非提示词约束来隔离能力
- 源码：`src/tools/AgentTool/built-in/planAgent.ts:77` — 5 种工具被硬性禁用（详见第 13 章）
- 关联章：ch13

**原则 16.2：补偿措施分层，轻量先行，重量托底** — 5 种压缩策略按深度排序执行
- 源码：`src/query.ts:396-470` — snip→micro→collapse→autocompact 执行顺序（详见第 11 章）
- 关联章：ch11

**原则 16.3：缓存是约束，不是优化** — CacheSafeParams 的 5 个参数必须完全相同才能命中缓存
- 源码：`src/utils/forkedAgent.ts:47` — "must be identical between the fork and parent API requests"（详见第 12 章）
- 关联章：ch12

**原则 16.4：对抗优于自评** — 用独立的 Verification Agent 对抗模型的自我评估偏差
- 源码：`src/tools/AgentTool/built-in/verificationAgent.ts:53` — "RECOGNIZE YOUR OWN RATIONALIZATIONS"（详见第 13 章）
- 关联章：ch13

**原则 16.5：并行按隔离分层** — in-process→tmux→remote 三层，每层的隔离和代价递增
- 源码：`src/utils/swarm/backends/types.ts:12` — BackendType 三种类型（详见第 14 章）
- 关联章：ch14

**原则 16.6：每个补偿措施都有拆除计划** — feature flag 确保 Harness 代码可以随模型进步而消失
- 源码：`src/services/compact/autoCompact.ts:177` — "Inside feature() so the string DCEs from external builds"（详见第 15 章）
- 关联章：ch15

**原则 16.7：记忆只存不可推导的信息** — MEMORY.md 只存用户偏好、历史决策和反馈，不存代码架构
- 源码：`src/memdir/memoryTypes.ts:1` — "context NOT derivable from the current project state"（详见第 12 章）
- 关联章：ch12

- 概念层次：设计意图 → 可迁移原则
- 权衡分析：原则之间可能有冲突——例如原则 16.1（能力硬编码）与原则 16.6（补偿有拆除计划）之间，硬编码的 disallowedTools 不容易通过 feature flag 移除
- 包含元素：表格（7 条原则 × 源码锚点 × 关联章节 × 适用边界）
- **⛔ 内容密度规划**：
  - 引入段要点：7 条原则不是凭空设计，是从源码中提炼的工程决策模式
  - 解读段要点：逐一展开每条原则——源码证据→设计意图→可迁移性
  - 散文与代码比例目标：100% 散文
- 估计字数：1200-1600 字
- 对应 spec 场景：C3、C4、C6、E1

### 节 16.3：适用场景——什么时候你需要一个 Harness
- 核心主张：不是所有 LLM 应用都需要完整的 Harness——需要 Harness 的信号是"模型输出有不可接受的失败模式"
- 源码锚点：引用前述各章的失败模式
  - 上下文爆炸（详见第 11 章）→ 需要压缩策略
  - 自我评估偏差（详见第 13 章）→ 需要对抗性验证
  - 并行需求（详见第 14 章）→ 需要 Coordinator 模式
- 概念层次：定义 → 设计意图
- 权衡分析：N/A
- 包含元素：决策树表格（问题→对应原则→参考章节）
- **⛔ 内容密度规划**：
  - 引入段要点：什么时候该建 Harness，什么时候不需要
  - 解读段要点：简单单轮任务→不需要 Harness；长会话 Agent→需要压缩策略；代码生成→需要验证；多任务→需要协调
  - 散文与代码比例目标：100% 散文
- 估计字数：400-600 字
- 对应 spec 场景：E2

### 节 16.4：工作原理——7 条原则的协同关系
- 核心主张：7 条原则不是独立的检查清单，它们之间存在依赖和协同——能力隔离（原则 1）是 fork 机制的前提，fork 是 Coordinator 的基础，feature flag 是所有原则的退出机制
- 源码锚点：综合前述各章的关键路径
  - disallowedTools（原则 1）→ CacheSafeParams（原则 3）→ runForkedAgent → runAgent → Coordinator workers（原则 5）
  - feature flag（原则 6）贯穿所有层级
- 概念层次：定义 → 设计意图 → 实际案例
- 权衡分析：原则的协同有时是串行依赖（必须先有能力隔离，才能安全 fork），有时是独立选择（记忆系统与压缩策略无关）
- 包含元素：Mermaid 架构图（7 条原则的依赖关系和协同路径）
- **⛔ 内容密度规划**：
  - 引入段要点：7 条原则组成一个有依赖关系的决策网络
  - 解读段要点：能力隔离→fork 安全→多 Agent 并行→对抗性验证，每一层都依赖上一层。feature flag 是横切关注点，贯穿所有层。记忆系统是独立的纵向切面
  - 散文与代码比例目标：100% 散文
- 估计字数：800-1100 字
- 对应 spec 场景：A1、D1、E1

### 节 16.5：权衡——7 条原则的适用边界
- 核心主张：7 条原则各有适用边界——超出边界时遵循原则反而会增加复杂度
- 源码锚点：
  - 原则 2（分层补偿）的边界：5 种压缩策略在短会话中完全不触发（详见第 11 章）
  - 原则 4（对抗验证）的边界：Verification Agent 默认关闭，需要双重 flag 启用（详见第 13 章 builtInAgents.ts:65）
  - 原则 5（并行分层）的边界：in-process 后端的共享状态需要同步机制（详见第 14 章）
  - 原则 7（只存不可推导信息）的边界：自动提取有时会提取"可推导"的信息，需要人工审查
- 概念层次：实际案例 → 可迁移原则
- 权衡分析：过度应用原则的代价——原则 1（能力硬编码）如果应用在所有工具上会让 Agent 无法完成任何任务；原则 2（分层补偿）如果引入太多层级会增加调试难度
- 包含元素：对比表格（7 条原则 × 适用边界 × 过度应用的代价）
- **⛔ 内容密度规划**：
  - 引入段要点：原则不是铁律，是决策工具——错误场景下遵循原则也是一种错误
  - 解读段要点：每条原则的"够了"边界——什么时候停止添加压缩策略、什么时候不需要 Verification Agent、什么时候不需要 Coordinator
  - 散文与代码比例目标：100% 散文
- 估计字数：700-1000 字
- 对应 spec 场景：C4、E3

### 节 16.6：踩坑指南——设计 Harness 时的 3 个常见误区
- 核心主张：最常见的误区是"为模型还不需要补偿的地方建补偿"、"把 Harness 功能做成永久架构"和"忽视 Harness 自身的 token 成本"
- 源码锚点：
  - `src/tools/AgentTool/loadAgentsDir.ts:132` — omitClaudeMd 注释："Saves ~5-15 Gtok/week across 34M+ Explore spawns"（详见第 13 章）——Harness 自身消耗 token
  - `src/services/compact/autoCompact.ts:70` — MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3（详见第 11 章）——补偿措施本身可能失败
  - `src/utils/bunFeatures.ts:16` — DEFAULT_FEATURES 只有 3 个——大部分补偿是实验性的
- 概念层次：实际案例 → 可迁移原则
- 权衡分析：N/A
- 包含元素：列表
- **⛔ 内容密度规划**：
  - 引入段要点：三种最可能导致 Harness 过度设计的原因
  - 解读段要点：误区一——过早补偿（模型可能很快就不需要这个补偿了）；误区二——永久架构（没有 feature flag 的补偿会变成技术债务）；误区三——忽视 Harness 的 token 成本（Explore 每周 3400 万次调用，omitClaudeMd 节省 5-15 Gtok/周）
  - 散文与代码比例目标：100% 散文
- 估计字数：400-600 字
- 对应 spec 场景：B1

### 节 16.7：实证——用 7 条原则审视一个新项目
- 核心主张：用 7 条原则检查一个假设的 LLM 应用（例如"代码审查 Agent"），展示原则如何指导具体决策
- 源码锚点：不引入新源码，只引用前 15 章的分析模式
- 概念层次：实际案例 → 验证方法
- 包含元素：Mermaid 决策树（假设项目→7 条原则检查→具体决策）
- **⛔ 内容密度规划**：
  - 引入段要点：把 7 条原则应用到一个新场景
  - 解读段要点：假设设计一个"代码审查 Agent"——原则 1（审查 Agent 不能修改代码，只能报告→disallowedTools）、原则 2（审查报告可能很长→需要截断策略）、原则 3（多文件审查可能需要 fork→CacheSafeParams）、原则 4（审查质量需要独立验证→Verification Agent）、原则 6（所有补偿措施都要有 feature flag→将来可移除）、原则 7（记忆用户偏好→不存代码模式）
  - 散文与代码比例目标：100% 散文
- 估计字数：500-700 字
- 对应 spec 场景：D1

### 章末结构（projector 主成分提炼）

**主成分提炼**：从 Claude Code 到通用原则的本质
- 本质：Harness 不是产品的永久部分，是产品在"模型还不够好"阶段的临时支架。7 条原则是设计支架的工程方法，不是产品设计方法
- 关键机制：能力隔离（硬约束）、分层补偿（轻→重）、缓存一致性、对抗性验证、并行分层、拆除计划、记忆约束
- 适用边界：当模型能力超越 Harness 补偿的阈值时，对应的 Harness 代码应该消失
- 全书结论：Harness 工程的最高目标不是建更好的 Harness，而是让 Harness 变得不需要

### 章末：用户能做什么（必填）
1. 列出你的 LLM 应用中"模型还不够好"的具体场景——每个场景都是一条 Harness 候选
2. 为每条 Harness 设计 feature flag——确保将来可以安全移除
3. 用 disallowedTools（或等价机制）硬性隔离能力，不要只靠提示词
4. 为补偿措施设计分层——先轻量后重量，先尝试微补偿再考虑全量重做
5. 如果需要并行执行，按隔离需求选择进程内/进程外/远程
6. 为每个子 Agent 提供自包含的提示词——它看不到你的对话
7. 定期审查 feature flag——模型在进步，你的 Harness 应该在缩小

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图16-1 | 7 条原则的依赖关系 | Mermaid graph TD | 能力隔离→fork安全→多Agent并行→对抗性验证，feature flag 横切 | 16.4 |
| 图16-2 | Harness 适用性决策树 | Mermaid flowchart TD | "模型输出有不可接受的失败模式？"→是→7 条原则逐一检查 | 16.7 |

## 写作顺序

1. ⛔ 确认 spec 与风格
2. 先写 16.2 节（7 条原则）——本章核心
3. 写 16.4 节（原则协同关系）
4. 写 16.1 节（问题）
5. 写 16.3 节（适用场景）
6. 写 16.5 节（适用边界）
7. 写 16.6 节（踩坑指南）
8. 写 16.7 节（实证）
9. 写章末
10. 最后补写开篇

## 写作前 checklist

```bash
# 本章不引入新源码，验证引用的前 15 章锚点仍然有效
sed -n '77p' src/tools/AgentTool/built-in/planAgent.ts            # 原则 1
sed -n '396p' src/query.ts                                        # 原则 2
sed -n '47p' src/utils/forkedAgent.ts                             # 原则 3
sed -n '53p' src/tools/AgentTool/built-in/verificationAgent.ts    # 原则 4
sed -n '12p' src/utils/swarm/backends/types.ts                    # 原则 5
sed -n '177p' src/services/compact/autoCompact.ts                 # 原则 6
sed -n '1p' src/memdir/memoryTypes.ts                             # 原则 7
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`specs/ch16.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属禁止 |
| 6 | 章节结构 | ✓ | 九段式 |
| 7 | 源码引用格式 | ✓ | 各节锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 排除范围 |
| 10 | 验收优先级 | ✓ | E>A>B=C>D |
| 11 | 验收场景·A | ✓ | A 组 |
| 12 | 验收场景·B | ✓ | B 组 |
| 13 | 验收场景·C | ✓ | C 组 |
| 14 | 验收场景·D | ✓ | D 组 |
| 15 | 验收场景·E | ✓ | E 组 |
| 16 | 排除范围 | ✓ | 排除范围 |
| 17 | 每条原则关联章节 | ✓ | 16.2 各原则 |
| 18 | 不引入新模块 | ✓ | 全文 |

**验收场景覆盖统计**：A 4/4 + B 3/3 + C 6/6 + D 5/5 + E 3/3 = **21/21**
