# 第 25 章写作计划

**批次**：批次 2b（来自 plans/batch.md）
**章节**：ch25 — 第25章：7 种任务类型的状态机——Task 系统设计
**风格**：hunter（模式猎人）

---

## 章节意图（从 spec `## 意图` 提取）

揭示 `src/tasks/types.ts`（46行）如何用统一的 TaskState 联合类型管理 7 种任务（LocalShellTask/LocalAgentTask/RemoteAgentTask/InProcessTeammateTask/LocalWorkflowTask/MonitorMcpTask/DreamTask），以及"类型收窄 + 状态守卫"如何实现并发安全。读者读完后能理解 Agent 后台任务系统的状态机设计，并能在自己的系统中实现类型安全的任务管理。

---

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**（从 spec 约束·必须 前 4 条提取）：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**（从 spec 约束·禁止 前 4 条提取）：
- 不得引用排除范围中的 stub 或未实现模块（src/assistant/、src/ssh/、src/server/、src/proactive/）
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- LocalAgentTask 内部实现（第26章）
- RemoteAgentTask 内部实现（第27章）
- DreamTask 内部实现（第28章）
- src/assistant/、src/ssh/、src/server/、src/proactive/（排除范围 stub）

---

## ⛔ 风格专属写作要求

> STYLE_ID = hunter（模式猎人）

**开篇格式**：模式预告（150-200字）
- 以"在这个代码库中，7 种不同的任务类型共享同一套状态管理接口"切入
- 预告模式名称：「辨别联合类型状态机」（Discriminated Union State Machine）
- 价值承诺：识别这个模式，你就能用 TypeScript 联合类型实现类型安全的多态状态管理，让编译器替你检查状态访问合法性
- ⛔ 开篇禁止直接引用任何源码路径或行号
- ⛔ 全章不混用其他风格的写作手法

**分节组织**：hunter 风格结构
```
## [模式预告开篇]
## 问题
## 源码实例 1（tasks/types.ts + Task.ts）
## 源码实例 2（stopTask.ts 类型守卫实战）
## 模式剖析
## 适用范围
## 权衡与局限
## 与已知模式的对话
## 你能做什么
```

**章末结构**：模式提炼
- 3 个命名模式，每个有五层结构（模式名→解决的问题→核心做法→前置条件→源码证据）
- 接"你能做什么"（5-8条行动建议）

**风格专属必须**：
- 展示至少 2 处不同位置的源码实例，证明模式的普遍性（tasks/types.ts + stopTask.ts）
- 每个实例说明与第一个实例的关键区别（类型定义 vs 类型守卫使用）
- 包含适用范围表（✓ 适用场景 + ✗ 不适用场景，每行附理由和替代方案）
- 包含权衡与局限分析（适用边界、潜在风险、性能影响、替代方案）
- 与业界已知模式对话：与 GoF「状态模式」比较、与函数式「代数数据类型（ADT）」概念联系
- 关键结论前有设问句，复杂逻辑前有预告性文字

**风格专属禁止**：
- 不跳过模式命名框，不用笼统描述代替五层结构
- 不跳过多实例证明
- 不假设读者了解 TypeScript 类型系统的 discriminated union 特性

**通用写作铁律**：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文（中文（English Full Name, ABBR））
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写，禁止在开篇直接引用源码

---

## 前置阅读清单
（按阅读顺序排列，≤6 个文件）

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/tasks/types.ts | TaskState 联合类型(12-19行)、BackgroundTaskState(22-29行)、isBackgroundTask(37-44行) | 全文（46行）|
| 2 | src/Task.ts | TaskType(6-14行)、TaskStatus(15-20行)、isTerminalTaskStatus(27-29行)、TaskStateBase(45-57行) | 1-120 |
| 3 | src/tasks/LocalShellTask/guards.ts | LocalShellTaskState 类型(12行)、isLocalShellTask 类型守卫(34行) | 全文（约50行）|
| 4 | src/tasks/LocalAgentTask/LocalAgentTask.tsx | LocalAgentTaskState(116行)、isLocalAgentTask(149行) | 116-155 |
| 5 | src/tasks/stopTask.ts | status 守卫(50行)、getTaskByType(57行)、isLocalShellTask 使用(70行)、command 三元表达式(97行) | 全文（约100行）|

验证文件存在：
```bash
ls src/tasks/types.ts src/Task.ts src/tasks/LocalShellTask/guards.ts \
   src/tasks/LocalAgentTask/LocalAgentTask.tsx src/tasks/stopTask.ts
```

---

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用都实际存在于对应文件 | 所有节的源码锚点（已验证） | ☑ |
| A2 | 推断有标注 | 每处推断有"（推断）"或"源码中未直接说明"标注 | 模式剖析、权衡与局限节 | ☑ |
| A3 | 不引用排除范围 | 没有任何引用落在排除范围的章节内容中（各任务类型内部实现） | 排除范围已列出，写作时避免进入第26-28章内容 | ☑ |
| A4 | 不重复其他章内容 | 用"详见第 X 章"代替重复 | LocalAgentTask→第26章，RemoteAgentTask→第27章，DreamTask→第28章 | ☑ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全章审查 | ☑ |
| B2 | 数据驱动 | 准确使用数字："7 种任务类型"、"5 种状态"（不写"多种"）| 节25.1、25.2 | ☑ |
| B3 | 术语标准化 | 首次出现时有原文标注（辨别联合类型（Discriminated Union Type）、类型守卫（Type Guard）等）| 所有首次出现的英文术语 | ☑ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 概念递进层次 | 按 hunter 风格：问题识别→模式命名→实例证明→规律提炼 | 节25.1→25.2→25.3→章末模式提炼 | ☑ |
| C2 | ⛔ hunter 专属结构 | 模式预告开篇→问题→源码实例1→源码实例2（变体）→模式剖析→适用范围→权衡局限→与已知模式对话→你能做什么 | 全章骨架 | ☑ |
| C3 | 核心主张可提炼 | 可提炼出"用辨别联合类型管理多态状态机"这一核心主张 | 模式剖析节 | ☑ |
| C4 | 设计权衡有说明 | 有"选联合类型不选 class 继承的原因" | 权衡与局限节 | ☑ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的可操作建议 | 你能做什么节 | ☑ |
| C6 | 章节间链接 | 章末向后链接（第26-28章各任务类型）+ 章节内向前链接（第8章触发任务创建） | 开篇引用第8章，章末链接第26/27/28章 | ☑ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表可渲染且达标 | 包含 Mermaid 或 ASCII 图，展示 7 种任务类型的状态机；字数约 3500-5000，需 ≥2 个图表 | 节25.1（状态机图）、节25.2（7种类型对比表） | ☑ |
| D2 | 图表标注格式 | 上方有 `**图 N-X：标题**`，下方有说明文字 | 所有图表 | ☑ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 所有代码块 | ☑ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 节25.2（TaskStateBase 中的注释）| ☑ |
| D5 | ⛔ hunter 专属图表 | 适用范围表（✓/✗）+ 模式命名框（五层结构）+ 7种任务类型对比表 | 节25.5适用范围表 + 节25.2对比表 + 章末模式命名框 | ☑ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | ⛔ hunter 风格符合性 | 开篇为模式预告（150-200字，含N处实例+模式名+价值承诺），分节按 hunter 结构，章末有模式命名框；⛔ 开篇不引源码路径；全章不混用其他风格 | 开篇 + 全章骨架 + 章末模式提炼 | ☑ |
| E2 | 读者对话感 | "我们"而非"用户"，有预告和设问 | 全章 | ☑ |
| E3 | 关键信息突出 | 加粗标注，表格展示对比 | 7种类型对比表、关键结论 | ☑ |

---

## 章节骨架
（每节必须有源码锚点，写前先确认锚点存在。⛔ 骨架结构与 spec `## 已定决策→章节结构` 一致）

### 开篇（hunter 风格：模式预告，最后补写）
> ⛔ 开篇禁止直接引用任何源码路径或行号，在全章初稿完成后最后补写

**模式预告公式**：
- 触发句：在这个代码库里，7 种完全不同的任务（bash 命令、本地 Agent、远程 Agent、进程内队友、工作流、MCP 监控、梦境任务）共享同一套状态查询接口——你能用同一个 `task.status` 字段检查它们的运行状态，但只有在正确的任务类型下才能访问特有字段，否则 TypeScript 直接拒绝编译
- 模式名预告：「辨别联合类型状态机」（Discriminated Union State Machine）
- 价值承诺：识别这个模式，你就能用一个 46 行的 types.ts 管理任意数量的有状态实体，同时保证类型安全

- 可选题词：待阅读源码后从注释选取

### 节 25.1：问题
- 核心主张：**当系统有多种"外形不同但生命周期相似"的实体时，面向对象继承会产生"基类膨胀"，而联合类型能在保持多态的同时保持类型精确**
- 源码锚点：`src/Task.ts:6`（TaskType 联合类型定义，7 种类型字面量）
- 概念层次：
  - 定义：7 种任务类型的本质差异（执行环境不同：本地 bash、本地 Agent、远程 Agent、进程内、工作流、MCP、Dream）
  - 设计意图：为什么需要统一管理？—— AppState.tasks 是一个字典，UI 组件需要遍历所有任务显示状态
  - 实际案例：background tasks indicator 需要用 `isBackgroundTask(task)` 过滤显示，不关心任务类型，只关心是否在前台/后台
- 权衡分析：为什么不用 class 继承（TaskBase → LocalShellTask extends TaskBase）？— 联合类型在 TypeScript 中有更好的穷举性检查（exhaustive check），class 继承在序列化/持久化时有额外开销（推断）
- 包含元素：**图25-1**：7种任务类型关系图（Mermaid）
- **⛔ 内容密度规划**：
  - 引入段要点：用"AppState.tasks 字典里混放着 7 种类型的任务"场景，说明为什么需要统一类型接口
  - 解读段要点：解释"外形不同但生命周期相似"——每种任务都有 pending/running/completed/failed/killed，但 LocalShellTask 有 command 字段而 DreamTask 没有
  - 散文与代码比例目标：至少 3 句散文/行代码
- 估计字数：600-800 字（散文 ≥ 60%）
- 对应 spec 场景：C1、C4

### 节 25.2：源码实例 1——types.ts + Task.ts 的联合类型设计
- 核心主张：**TaskState 联合类型的"魔法"在于每个成员类型都有唯一的 `type` 字面量字段——这是 TypeScript 辨别联合的关键，让编译器自动推断具体类型**
- 源码锚点：
  - `src/tasks/types.ts:12`（`export type TaskState = ...` 联合类型）
  - `src/Task.ts:6`（`export type TaskType = ...` 7 个字面量）
  - `src/Task.ts:15`（`export type TaskStatus = ...` 5 种状态）
  - `src/Task.ts:45`（`export type TaskStateBase = { id, type, status, ... }`）
  - `src/Task.ts:27`（`isTerminalTaskStatus()` 状态守卫函数）
  - `src/tasks/types.ts:37`（`isBackgroundTask()` 组合守卫）
- 概念层次：
  - 定义：`TaskStateBase`（共有字段基底）+ 各 XxxTaskState 扩展字段（如 `LocalShellTaskState` 加 `command` 字段）
  - 设计意图：`type` 字段使用字面量类型（'local_bash', 'local_agent' 等），让 TypeScript 能通过 if/switch 缩窄联合类型
  - 实际案例：一张对比表——7 种 TaskState 的 type 字段值、特有字段、生命周期特点
- 权衡分析：为什么 `TaskStateBase` 不直接继承而是用 `& TypescriptIntersection`？— 交叉类型（& ）比继承更扁平，便于 spread 操作（`...prevTask` 更新状态）（推断）
- 包含元素：
  - **图25-2**：7种任务类型对比表（特型字段、用途、生命周期差异）
  - TaskState 联合类型代码片段（tasks/types.ts 全文，≤20行）带引入+解读
  - TaskStateBase 代码片段（Task.ts:45-57，≤15行）带引入+解读
- **⛔ 内容密度规划**：
  - 引入段要点：先说"46行的 types.ts 是整个 Task 系统的类型宪法"，再展示它的全文
  - 解读段要点：重点说明 TaskStateBase 是"共有字段规范"，而不是 OOP 的父类——因为它通过 `&` 而非 `extends` 合并
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：900-1200 字（散文 ≥ 60%）
- 对应 spec 场景：A1、E1、七种任务类型对比表（spec 场景 ch25_task_type_table）

### 节 25.3：源码实例 2——stopTask.ts 类型守卫实战
- 核心主张：**同一个 `task: TaskStateBase` 变量，在 `isLocalShellTask(task)` 守卫之后，TypeScript 会自动"收窄"到 `LocalShellTaskState`，让 `task.command` 字段可访问——这就是类型收窄（Type Narrowing）**
- 源码锚点：
  - `src/tasks/stopTask.ts:50`（`if (task.status !== 'running')` — 状态守卫）
  - `src/tasks/stopTask.ts:70`（`if (isLocalShellTask(task))` — 类型守卫）
  - `src/tasks/stopTask.ts:97`（`task.command` — 类型收窄后才能访问）
  - `src/tasks/LocalShellTask/guards.ts:34`（`isLocalShellTask` 函数实现）
  - `src/tasks/LocalAgentTask/LocalAgentTask.tsx:149`（第二个类型守卫 `isLocalAgentTask`）
- 概念层次：
  - 定义：类型守卫（Type Guard）= 运行时检查 + 编译时收窄的双重效果
  - 设计意图：`guards.ts` 文件独立存在的原因——让非 React 消费者（stopTask.ts）不需要引入 React/ink 的模块依赖（源码注释说明）
  - 实际案例：`stopTask()` 函数的三步类型安全流程：① `task.status !== 'running'`（状态守卫）→ ② `isLocalShellTask(task)`（类型守卫）→ ③ `task.command`（字段安全访问）
- 权衡分析：为什么 `isLocalShellTask` 接受 `unknown` 而不是 `TaskState`？— 更宽松的参数类型让该函数在任何上下文都可复用，不局限于 TaskState 的使用方（推断）
- 包含元素：
  - stopTask.ts 的三步类型安全代码（≤20行，关键行）带引入+解读
  - isLocalShellTask 实现代码（guards.ts:34-39，≤8行）带引入+解读
- **⛔ 内容密度规划**：
  - 引入段要点：先说"看了类型定义，我们来看它们在运行时如何被安全使用"
  - 解读段要点：重点说明守卫函数分离的原因——guards.ts 不引入 React，stopTask.ts 是纯逻辑，这是模块边界意识
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：700-900 字（散文 ≥ 60%）
- 对应 spec 场景：A1、E1（多实例证明）、类型收窄（spec 场景 ch25_type_narrowing）

### 节 25.4：模式剖析——辨别联合类型状态机
- 核心主张：**这个模式的核心是"类型即文档、守卫即协议"——类型字面量表达了实体的身份，守卫函数表达了安全访问的前置条件**
- 源码锚点：`src/tasks/types.ts:12-19`（联合类型定义）
- 概念层次：
  - 定义：辨别联合 = 公共辨别字段（`type`）+ 各成员专属字段的联合
  - 设计意图：让 switch 或 if/else 分支中的类型检查变成编译时穷举（exhaustive check）
  - 实际案例：用 switch(task.type) 遍历所有 7 种类型时，如果漏写一种，TypeScript 会报错
- 权衡分析：N/A（模式剖析节重在描述模式结构，权衡在节25.6）
- 包含元素：五层模式结构图（文字列表）
- **⛔ 内容密度规划**：
  - 引入段要点：先说"现在我们看到了模式的两处实例，可以开始命名和提炼了"
  - 解读段要点：解读"类型即文档"——`type: 'local_bash'` 不只是运行时标识，它把"这是一个 Bash 任务"的意图写进了类型系统
  - 散文与代码比例目标：80% 散文，20% 代码示例
- 估计字数：500-700 字（散文 ≥ 70%）
- 对应 spec 场景：C2（hunter 专属：模式剖析节）

### 节 25.5：适用范围
- 核心主张：**辨别联合类型状态机最适合"多态但共享生命周期"的场景，不适合需要热替换或动态扩展的插件架构**
- 源码锚点：`src/Task.ts:6`（TaskType 字面量联合，添加新类型需修改此处）

**适用范围表**：
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 多种实体共享状态机（pending/running/completed） | ✓ | 联合类型自然表达多态，同时保持类型安全 | class 继承（但穷举检查弱）|
| 需要遍历所有实体类型（如 UI 渲染）| ✓ | 编译时穷举保证不漏类型 | switch + instanceof（但 instanceof 在序列化后失效）|
| 状态数量固定、变化少 | ✓ | 每次新增类型只需改 TaskType 枚举 + types.ts | 动态注册表（Map<string, Constructor>）|
| 需要热插拔任务类型（插件系统）| ✗ | 联合类型是编译时静态的，无法运行时动态扩展 | 策略模式 + 运行时注册 |
| 任务类型超过 20 种 | ✗（谨慎）| 联合类型越多，类型推断越慢（推断）| 细粒度拆分或 class 继承 |

- **⛔ 内容密度规划**：
  - 引入段要点：先说"任何模式都有最适合的土壤，我们来找辨别联合的最佳场景"
  - 解读段要点：重点说明"热插拔"是本模式的边界——Claude Code 目前 7 种类型是固定的，如果需要插件动态注册任务，则需要策略模式
  - 散文与代码比例目标：80% 散文
- 估计字数：500-700 字（散文 ≥ 70%）
- 对应 spec 场景：D5（适用范围表）

### 节 25.6：权衡与局限
- 核心主张：**联合类型的最大隐患是"联合爆炸"——当 7 种任务类型各有 5-10 个特有字段时，ide 的类型推断性能开始下降，调试变复杂**
- 源码锚点：`src/tasks/LocalAgentTask/LocalAgentTask.tsx:116`（LocalAgentTaskState 的字段数量）
- 概念层次：
  - 潜在失败：新增任务类型时忘记更新 `TaskType`（src/Task.ts:6），会导致编译错误；但忘记更新 `types.ts` 中的 TaskState 联合，TypeScript 不会自动发现
  - 性能影响：联合类型数量增加会导致 TypeScript 语言服务（LSP）在类型推断时耗时增加（推断，无源码依据）
  - 替代方案：对于字段极多的类型，可以考虑用 interface 合并（declaration merging）代替联合
- 权衡分析：为什么不用 enum 代替字面量联合？— 字面量联合无需 import，序列化/反序列化更直接（推断）
- **⛔ 内容密度规划**：
  - 引入段要点：先说"理解一个模式的成熟度，在于知道它在什么时候开始让你头疼"
  - 解读段要点：重点说明 `types.ts` 和 `Task.ts` 的 TaskType 需要同步更新——这是两处而非一处，是潜在的维护漏洞
  - 散文与代码比例目标：至少 3 句散文/行代码
- 估计字数：500-700 字（散文 ≥ 65%）
- 对应 spec 场景：C4（权衡分析）

### 节 25.7：与已知模式的对话
- 核心主张：**这个模式与 GoF「状态模式」有形似神异之处——GoF 状态模式用对象封装状态行为，本模式用类型编码状态身份；前者管理状态转换，后者管理状态访问安全**
- 包含元素：对比表（本模式 vs GoF 状态模式 vs 函数式 ADT）
- 概念层次：
  - 与 GoF 状态模式（State Pattern）：相同——多态状态管理；不同——GoF 在运行时通过对象替换管理状态行为，本模式在编译时通过类型系统管理字段访问权限
  - 与函数式代数数据类型（Algebraic Data Type, ADT）：相同——和类型（Sum Type）思想，Haskell/Rust 中 `enum/sealed class` 的 TypeScript 等价形式；本模式是 TS 对 ADT 的不完整实现（推断）
  - 与策略模式（Strategy Pattern）：相同——多个算法/行为通过同一接口调用；不同——策略模式关注行为多态，本模式关注数据多态
- **⛔ 内容密度规划**：
  - 引入段要点：先说"识别一个模式的终极测试，是找到它和它最相似的'假朋友'"
  - 解读段要点：重点解读"数据多态 vs 行为多态"的区别——TaskState 联合管理的是数据形状（哪些字段可用），不是行为（如何执行）
  - 散文与代码比例目标：85% 散文
- 估计字数：500-700 字（散文 ≥ 80%）
- 对应 spec 场景：与已知模式的对话

### 章末：模式提炼（hunter 风格）

**模式命名框 1**：
```
模式名称：辨别联合类型状态机（Discriminated Union State Machine）
问题：系统有多种"外形不同但生命周期相似"的实体，需要统一管理又保证类型安全
解决方案：用公共辨别字段（type 字面量）+ 联合类型，让类型系统自动推断具体类型
前置条件：语言支持字面量类型和类型守卫（TypeScript、Rust enum 等）
源码锚点：src/tasks/types.ts:12 (TaskState 联合), src/Task.ts:6 (TaskType 字面量)
```

**模式命名框 2**：
```
模式名称：守卫函数模块分离（Guard Function Module Isolation）
问题：类型守卫函数被多个消费者使用，但某些消费者不能引入特定依赖（如 React）
解决方案：将类型守卫函数提取到独立的 guards.ts 文件，不引入非必要依赖
前置条件：消费者对依赖图有明确要求（如 stopTask.ts 不能引入 React）
源码锚点：src/tasks/LocalShellTask/guards.ts:1 (独立守卫文件，注释说明理由)
```

**模式命名框 3**：
```
模式名称：状态守卫前置检查（Status Pre-condition Guard）
问题：多步骤操作中，后续步骤依赖特定状态，需要在状态错误时提前失败并说明原因
解决方案：函数入口处先检查 task.status，不满足则抛出带有错误码的自定义异常
前置条件：有明确的"操作前置状态"语义（如 stopTask 要求 status === 'running'）
源码锚点：src/tasks/stopTask.ts:50 (status 守卫), src/tasks/stopTask.ts:10 (StopTaskError)
```

### 章末：你能做什么
预规划 5-8 条行动建议（写作前列出方向）：
1. 用字面量联合类型（而非枚举）定义系统中所有实体类型，享受编译时穷举检查
2. 为每种实体类型创建独立的 `XxxState = BaseState & { type: 'xxx', ...扩展字段 }` 交叉类型，而非 class 继承
3. 将类型守卫函数独立到 `guards.ts` 文件，避免守卫函数引入不必要的依赖
4. 在操作函数入口处用 `isTerminalTaskStatus()` 类似的守卫检查状态前置条件，并抛出带错误码的自定义异常
5. 用 switch(task.type) + default: never 的穷举模式，确保新增任务类型时编译器强制要求处理
6. 将 TaskState 联合类型的定义（types.ts）与 TaskType 字面量联合（Task.ts）分层管理，TaskType 是基础定义，TaskState 是完整的类型宇宙

---

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图25-1 | 7种任务类型与状态机关系 | Mermaid flowchart | 7种任务类型 → 共享 TaskStateBase → 5种状态(pending/running/completed/failed/killed) | 25.1 |
| 图25-2 | 7种任务类型对比表 | Markdown 表格 | type字段值/特有字段/使用场景/生命周期特点（7行×4列）| 25.2 |

---

## 写作顺序

1. ⛔ **确认完整 spec 与风格规则**：重新读取 `specs/ch25.spec` 和 DESIGN.md 的 STYLE_ID=hunter
2. 先读取所有前置文件（按前置阅读清单）
3. 写节 25.1（问题节——建立"为什么需要统一类型管理"认知，含图25-1）
4. 写节 25.2（实例 1——types.ts + Task.ts 类型定义，含图25-2对比表）
5. 写节 25.3（实例 2——stopTask.ts 类型守卫实战）
6. 写节 25.4（模式剖析）
7. 写节 25.5（适用范围表）
8. 写节 25.6（权衡与局限）
9. 写节 25.7（与已知模式的对话）
10. 写章末模式命名框（3个）
11. 写"你能做什么"（6条）
12. 最后补写开篇（模式预告，150-200字）

---

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
# 验证所有关键锚点
sed -n '12p' src/tasks/types.ts        # TaskState =
sed -n '6p' src/Task.ts                # TaskType =
sed -n '15p' src/Task.ts               # TaskStatus =
sed -n '27p' src/Task.ts               # isTerminalTaskStatus
sed -n '45p' src/Task.ts               # TaskStateBase =
grep -n "isLocalShellTask" src/tasks/LocalShellTask/guards.ts   # line 34
sed -n '149p' src/tasks/LocalAgentTask/LocalAgentTask.tsx       # isLocalAgentTask
sed -n '50p' src/tasks/stopTask.ts     # status !== 'running'
sed -n '70p' src/tasks/stopTask.ts     # isLocalShellTask
sed -n '97p' src/tasks/stopTask.ts     # task.command
sed -n '37p' src/tasks/types.ts        # isBackgroundTask
```

如果任何锚点不存在，重新搜索正确行号：
```bash
grep -n "TaskState\|TaskType\|TaskStatus\|isTerminalTaskStatus" src/Task.ts src/tasks/types.ts
```

---

## ⛔ Spec 一致性声明

**完整 spec 文件**：`specs/ch25.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束→通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束→通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求→风格专属必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求→风格专属禁止 |
| 6 | 章节结构 | ✓ | 章节骨架（与 spec 已定决策一致）|
| 7 | 源码引用格式 | ✓ | 所有节的锚点使用 `src/相对路径:行号` |
| 8 | 图表格式 | ✓ | 图表清单（Mermaid + Markdown 表格）|
| 9 | 边界路径 | ✓ | 排除范围已列出 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单（E>A>B=C>D）|
| 11 | 验收场景·A 源码锚点组 | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B 语言精确性组 | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C 结构完整性组 | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D 图表与代码组 | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E 风格符合性组 | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束→排除范围 |

**验收场景覆盖统计**：A 组 [4/4] + B 组 [3/3] + C 组 [6/6] + D 组 [5/5] + E 组 [3/3] = **[21/21]**
（已覆盖 spec 中全部 15 个命名场景，含 hunter 专属场景）

**⛔ 所有项目均已勾选 ✓，plan 审查通过。**
