# 第 44 章写作计划

**批次**：批次 11（来自 plans/batch.md）
**章节**：ch44 — Harness 工程模式图谱——12 个可复用设计模式
**风格**：hunter（模式猎人）

## 章节意图
从 Claude Code 源码提炼 12 个可复用的 Agent Harness 设计模式，每个模式有名称、问题陈述、解决方案、源码锚点和可携带性说明。读者读完后拥有一张完整的 Agent Harness 模式图谱，可直接用于自己的 Agent 系统设计。

## 通用约束
**前置依赖**：第1-39章全部内容
**写作风格**：hunter 风格，但本章是"图谱总结"章，重点是系统化提炼，而非追踪单一模式

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。触发句：12个模式，来自代码库的12个不同位置，共同回答"如何把 AI 的不确定性包裹在工程系统的确定性边界内"
**章末**：完整的模式索引表 + 你能做什么（5-8条）
**写作重点**：每个模式用标准五层结构（名称/问题/解决方案/前置条件/源码锚点），篇幅比其他章节更紧凑（每模式300-500字）
**铁律**：⛔源码第一、⛔不重复其他章已详细讲解的内容（用"详见第X章"）、⛔开篇最后写

## 前置阅读清单
（本章需要回顾 ch01-ch39 的章末模式提炼节，而非读取源码）

**阅读策略**：
1. 从 ch01-ch39 的 plans/*.plan 文件提取章末模式命名框
2. 选出最具代表性和可复用性的 12 个
3. 按"Agent Harness 工程层次"组织（工具层/上下文层/执行层/任务层/Swarm层/安全层/扩展层）

**12个模式候选**（从各章提炼）：
1. **可中断多轮工具调用循环** — ch08（src/QueryEngine.ts）
2. **辨别联合类型状态机** — ch25（src/tasks/types.ts）
3. **分层提示词组装** — ch15（src/utils/queryContext.ts）
4. **协议化工具扩展** — ch13（src/services/mcp/client.ts）
5. **写入先于执行** — ch17（src/QueryEngine.ts:436注释）
6. **可观测 Agent 事件总线** — ch20（src/entrypoints/sdk/coreTypes.ts）
7. **多态执行器** — ch21（src/utils/hooks.ts）
8. **异步钩子注册表** — ch22（src/utils/hooks/AsyncHookRegistry.ts）
9. **子进程 Agent 隔离** — ch26（src/tasks/LocalAgentTask/）
10. **领导者-队友事件驱动协调** — ch29（src/utils/swarm/）
11. **分层安全防线** — ch33（src/utils/permissions/permissions.ts）
12. **声明式能力包** — ch14（src/tools/SkillTool/）

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：39章的追踪结束于同一个问题的12种答案：如何在 AI 的不确定性边界上修一道工程护栏？每个模式都是一道护栏——它们命名、定位、可携带。

### 节 44.1：模式图谱概览
- 12个模式按层次组织，展示 Agent Harness 的完整架构视角
- 包含元素：**图44-1** — 12个模式的层次关系图（哪些模式在哪层工作）
- 估计字数：400-600字

### 节 44.2-40.13：12个模式详述（每模式1节）
每个模式包含：
- 名称（中英文）
- 问题（一句话）
- 解决方案（一句话）
- 源码锚点（文件:行号）
- 可携带性说明（哪些场景可以复用这个模式）
- 与本书其他模式的关系（如模式A依赖模式B）
- 估计字数：300-500字/模式

### 节 44.14：模式选型指南
- 根据 Agent 系统的特征（工具密集/上下文密集/多智能体/插件化），推荐优先应用哪些模式
- 包含元素：**图44-2** — 模式选型决策树

### 章末：12个模式索引表
| # | 模式名称 | 解决的问题 | 源码来源 |
|---|---------|-----------|---------|
| 1 | 可中断多轮工具调用循环 | AI工具调用需要多轮对话 | QueryEngine.ts |
| ... | ... | ... | ... |

### 章末：你能做什么
1. 选取"可中断多轮工具调用循环"作为 AI 集成的起点——这是构建 Agent 的最小核心
2. 用"辨别联合类型状态机"管理所有 Task/任务类型，让类型系统成为你的安全网
3. 用"分层提示词组装"管理系统提示词的不同来源，保持可维护性
4. 用"可观测 Agent 事件总线"在关键执行节点预埋钩子，保证 Agent 行为可审计
5. 用"分层安全防线"设计权限系统——每层只负责自己的决策，null 表示"请上层裁决"

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图44-1 | 12个模式层次关系 | Mermaid flowchart | 工具层→上下文层→执行层→任务层→Swarm层→安全层→扩展层 | 40.1 |
| 图44-2 | 模式选型决策树 | Mermaid flowchart | 根据Agent特征推荐模式组合 | 40.14 |

## 写作顺序
1. 从 plans/ch01.plan～ch39.plan 提取所有章末模式命名框（grep "模式名称：" plans/*.plan）
2. 选出12个最具代表性的模式
3. 按层次组织，写概览节（含图44-1）
4. 依次写12个模式详述节 44. 写模式选型指南（含图44-2）
6. 写索引表
7. 写你能做什么
8. 最后补写开篇

## 写作前 checklist
```bash
# 提取所有章末模式命名框
grep -h "模式名称：" /Users/liuweiping/workspace/dev/claude-code-sourcemap/plans/ch*.plan | sort | uniq
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
