# 第 31 章写作计划

**批次**：批次 7b（来自 plans/batch.md）
**章节**：ch31 — RemoteAgentTask——远程任务的通信架构
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx`（855行）如何把本地任务模型扩展到远程执行，以及在 SSE/WebSocket/轮询三种通信方式中如何选型。读者读完后能理解远程 Agent 任务的通信架构设计，并能在自己的分布式 Agent 系统中做有依据的通信方式选型。

## 通用约束
**排除范围**：LocalAgentTask（第 30 章）、DreamTask（第 32 章）、Swarm（第29-32章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「远程任务代理」（Remote Task Proxy）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（RemoteAgentTaskState 字段 + pollRemoteSessionEvents 轮询），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/tasks/RemoteAgentTask/RemoteAgentTask.tsx | RemoteAgentTaskState(22行)、RemoteTaskType(60行)、pollStartedAt字段(41行)、RemoteTaskCompletionChecker(73行) | 22-90 |
| 2 | src/utils/teleport.ts | pollRemoteSessionEvents 函数 | 相关行 |

```bash
ls src/tasks/RemoteAgentTask/RemoteAgentTask.tsx
grep -n "pollRemoteSessionEvents\|export.*function.*poll" src/utils/teleport.ts | head -10
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：远程 Agent 任务（在云端服务器上运行）与本地任务共享同一套 Task 状态机接口——但通信方式完全不同：不是本地函数调用，而是轮询远程事件流。

### 节 31.1：问题
- 核心主张：**远程 Agent 任务的核心挑战是"状态同步"——本地 UI 需要实时反映远程进程的状态，但网络延迟和断连使同步变得复杂**
- 源码锚点：`src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:41`（pollStartedAt 字段注释："Review timeout clocks from here so a restore doesn't immediately time out a task spawned >30min ago"）
- 包含元素：**图31-1** — Mermaid，本地 RemoteAgentTask 与远程 Agent 进程的通信架构
- 估计字数：600-800字

### 节 31.2：源码实例1 — RemoteAgentTaskState 的远程特有字段
- 核心主张：**RemoteAgentTaskState 比 LocalAgentTaskState 多了 remoteTaskType、sessionId、todoList、ultraplanPhase 等远程特有字段——这些字段反映了远程执行的不透明性**
- 源码锚点：
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:22`（RemoteAgentTaskState 完整定义）
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:60`（REMOTE_TASK_TYPES：5种远程任务类型）
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:73`（RemoteTaskCompletionChecker 类型——可插拔的完成检查器）
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:54`（ultraplanPhase 字段，状态从远程解析）
- 概念层次：remoteTaskType 的5种类型（remote-agent/ultraplan/ultrareview/autofix-pr/background-pr）、isLongRunning 语义（不在第一个 result 后就完成）
- 估计字数：900-1100字

### 节 31.3：源码实例2 — pollRemoteSessionEvents（轮询而非推送）
- 核心主张：**RemoteAgentTask 用轮询（polling）而非 WebSocket——轮询简单容错（网络断连后可以重试），但有延迟；pollStartedAt 防止恢复时误判超时**
- 源码锚点：
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:19`（`import { pollRemoteSessionEvents } from '../../utils/teleport.js'`）
  - `src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:81`（RemoteTaskCompletionChecker 注释："Checkers that hit external APIs should self-throttle"）
- 概念层次：轮询 vs SSE vs WebSocket 的选型理由（容错性优先）、pollStartedAt 的超时计算意义
- 估计字数：700-900字

### 节 31.4：模式剖析
- 核心主张：**远程任务代理的本质是"本地状态机代理远程进程"——本地维护一份镜像状态，通过轮询保持同步，对 UI 呈现与本地任务相同的接口**
- 估计字数：500-700字

### 节 31.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 长时间运行的云端 AI 任务（>5分钟）| ✓ | 轮询容忍网络中断，任务可从断点恢复 | WebSocket（但断线后需重连）|
| 多种任务类型共享同一 UI 框架 | ✓ | RemoteTaskType 枚举支持5种类型 | 每种类型独立 UI（难维护）|
| 需要实时流式输出（每秒更新）| ✗（谨慎）| 轮询有延迟，不如 SSE | SSE/WebSocket |
| 延迟敏感的交互（<200ms响应）| ✗ | 轮询间隔通常>1s | 直连本地进程 |

### 节 31.6：权衡与局限
- 轮询延迟（用户看到的进度有延迟）、超时处理复杂性（pollStartedAt vs 任务创建时间）、isLongRunning 的边界情况
- 估计字数：500-700字

### 节 31.7：与已知模式的对话
- 与 GoF 代理模式（Proxy）：相同——本地代理透明访问远程资源；不同——代理模式通常是同步的，本模式需要处理异步轮询和超时
- 与 EIP 事件驱动消费者：相同——消费远程事件流；不同——事件驱动是推送，本模式是主动轮询

### 章末：模式提炼
```
模式名称：远程任务代理（Remote Task Proxy）
问题：远程进程的状态需要在本地 UI 中呈现，但远程状态变化不透明、网络不可靠
解决方案：本地维护一份 RemoteAgentTaskState 镜像状态，通过轮询 pollRemoteSessionEvents 保持同步
前置条件：远程任务有唯一 sessionId，可通过 API 查询当前状态
源码锚点：src/tasks/RemoteAgentTask/RemoteAgentTask.tsx:22 (RemoteAgentTaskState), :41 (pollStartedAt)
```

### 章末：你能做什么
1. 为远程任务在本地维护镜像状态，而非实时直连远程——网络中断后本地状态仍可显示
2. 记录 pollStartedAt 而非任务创建时间，让恢复后的超时计算基于"本地开始监控"的时刻
3. 设计可插拔的完成检查器（RemoteTaskCompletionChecker），让不同任务类型有不同的完成判断逻辑
4. 为长时间任务设计 isLongRunning 语义，区分"第一次返回结果"和"真正完成"
5. 为轮询实现 self-throttle 机制，避免对远程 API 的过度请求

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图31-1 | RemoteAgentTask 通信架构 | Mermaid flowchart | 本地状态机 ↔ 轮询 ↔ 远程 Agent 进程 | 27.1 |

## 写作前 checklist
```bash
sed -n '22p' src/tasks/RemoteAgentTask/RemoteAgentTask.tsx  # RemoteAgentTaskState
sed -n '41p' src/tasks/RemoteAgentTask/RemoteAgentTask.tsx  # pollStartedAt 注释
sed -n '60p' src/tasks/RemoteAgentTask/RemoteAgentTask.tsx  # REMOTE_TASK_TYPES
sed -n '73p' src/tasks/RemoteAgentTask/RemoteAgentTask.tsx  # RemoteTaskCompletionChecker
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
