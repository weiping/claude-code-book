# 第 26 章写作计划

**批次**：批次 6a（来自 plans/batch.md）
**章节**：ch26 — AsyncHookRegistry——异步钩子的注册、超时与并发控制
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/utils/hooks/AsyncHookRegistry.ts`（309行）如何处理异步钩子的完整生命周期：挂起、超时、取消、结果回调，以及为何需要一个专门的注册表而非直接 await。读者读完后能将这一注册表模式用于自己的异步钩子系统，理解"挂起→完成/超时/取消"三态的工程价值。

## 通用约束
**排除范围**：hooksConfigSnapshot（第 27 章）、Frontmatter Hooks（第 28 章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「异步钩子注册表」（Async Hook Registry）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（注册+超时处理 vs 批量检查），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/utils/hooks/AsyncHookRegistry.ts | PendingAsyncHook(12行)、pendingHooks Map(28行)、registerPendingAsyncHook(30行)、超时15s(51行)、finalizeHook(91行)、checkForAsyncHookResponses(113行)、finalizePendingAsyncHooks(281行) | 全文(309行) |

```bash
ls src/utils/hooks/AsyncHookRegistry.ts
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：当一个 hook 以异步方式（fire-and-forget）启动后，谁来追踪它是否完成？默认超时多久？如果用户提前退出怎么处理？这些问题催生了一个专门的"异步钩子注册表"。

### 节 26.1：问题
- 核心主张：**fire-and-forget 的异步钩子需要一个全局注册表来追踪——否则进程退出时无法等待钩子完成，也无法报告超时**
- 源码锚点：`src/utils/hooks/AsyncHookRegistry.ts:28`（`const pendingHooks = new Map<string, PendingAsyncHook>()`——模块级全局 Map）
- 包含元素：**图26-1** — Mermaid 状态图，钩子从"挂起"到"完成/超时/取消"的三态转换
- 估计字数：600-800字

### 节 26.2：源码实例1 — registerPendingAsyncHook（注册与超时设置）
- 核心主张：**registerPendingAsyncHook 在注册时立刻设置超时计时器——超时后自动 finalize，不需要外部轮询**
- 源码锚点：
  - `src/utils/hooks/AsyncHookRegistry.ts:12`（`export type PendingAsyncHook = { processId, hookName, startTime, timeout, ... }`）
  - `src/utils/hooks/AsyncHookRegistry.ts:30`（`export function registerPendingAsyncHook({`）
  - `src/utils/hooks/AsyncHookRegistry.ts:51`（`const timeout = asyncResponse.asyncTimeout || 15000`——默认 15 秒）
  - `src/utils/hooks/AsyncHookRegistry.ts:91`（`async function finalizeHook(`——成功/错误/取消三种结果）
- 概念层次：PendingAsyncHook 的字段语义、15s 默认超时的来源、finalizeHook 的三种 outcome
- 估计字数：800-1000字

### 节 26.3：源码实例2 — checkForAsyncHookResponses（轮询检查）与 finalizePendingAsyncHooks（清理）
- 核心主张：**checkForAsyncHookResponses 是批量轮询——一次调用检查所有挂起钩子的状态；finalizePendingAsyncHooks 是会话结束时的强制清理**
- 源码锚点：
  - `src/utils/hooks/AsyncHookRegistry.ts:113`（`export async function checkForAsyncHookResponses(`）
  - `src/utils/hooks/AsyncHookRegistry.ts:138`（`const pendingCount = pendingHooks.size`——批量处理）
  - `src/utils/hooks/AsyncHookRegistry.ts:281`（`export async function finalizePendingAsyncHooks(`）
  - `src/utils/hooks/AsyncHookRegistry.ts:270`（removeDeliveredAsyncHooks — 清理已完成 hooks）
- 概念层次：轮询 vs 推送的权衡、会话结束时的强制 finalize、removeDeliveredAsyncHooks 的清理时机
- 估计字数：700-900字

### 节 26.4：模式剖析
- 核心主张：**异步钩子注册表的本质是"生命周期委托"——调用方不管理钩子生命周期，注册表统一管理超时/取消/清理**
- 估计字数：500-700字

### 节 26.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| fire-and-forget 任务需要超时保证 | ✓ | 注册表统一管理超时，不遗漏 | 每处单独 setTimeout（容易漏）|
| 会话结束时需要等待后台任务完成 | ✓ | finalizePendingAsyncHooks 确保清理 | 进程退出信号（但不等待）|
| 需要取消特定钩子 | ✓ | 注册表持有引用，可按 processId 取消 | 无法取消 |
| 高频短任务（每秒>100个）| ✗（谨慎）| Map 查找开销积累（推断）| 轻量队列 |

### 节 26.6：权衡与局限
- Map 持有引用阻止 GC、超时精度依赖 setTimeout、批量轮询 vs 实时推送的延迟差异
- 估计字数：500-700字

### 节 26.7：与已知模式的对话
- 与 Promise Registry 模式：几乎完全一致——中央 Map 追踪所有 pending Promise；本模式多了超时自动 finalize
- 与 GoF 备忘录（Memento）：相同——持有状态供后续恢复；不同——备忘录是为了撤销，本模式是为了超时后清理

### 章末：模式提炼
```
模式名称：异步钩子注册表（Async Hook Registry）
问题：fire-and-forget 的异步任务缺乏生命周期管理——超时、取消、会话结束清理无人负责
解决方案：全局 Map 注册所有挂起钩子，注册时设置超时计时器，会话结束时强制 finalize 所有挂起钩子
前置条件：任务有唯一 processId，可通过回调或轮询获取结果
源码锚点：src/utils/hooks/AsyncHookRegistry.ts:28 (pendingHooks Map), :30 (registerPendingAsyncHook), :51 (15s超时)
```

### 章末：你能做什么
1. 用全局 Map 追踪所有 fire-and-forget 异步任务，而非 void Promise 后不管
2. 在注册时而非调用时设置超时计时器，保证每个任务都有确定的最大执行时间
3. 定义三态结果（success/error/cancelled），让调用方能区分正常结束、失败和主动取消
4. 实现 finalizePendingXxx 函数，在进程退出或会话结束时强制清理所有挂起任务
5. 为批量轮询提供 checkForXxxResponses 接口，而非让每个调用方自行轮询

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图26-1 | 异步钩子三态生命周期 | Mermaid stateDiagram | 挂起→完成/超时/取消 | 22.1 |

## 写作前 checklist
```bash
sed -n '12p' src/utils/hooks/AsyncHookRegistry.ts  # PendingAsyncHook
sed -n '28p' src/utils/hooks/AsyncHookRegistry.ts  # pendingHooks Map
sed -n '30p' src/utils/hooks/AsyncHookRegistry.ts  # registerPendingAsyncHook
sed -n '51p' src/utils/hooks/AsyncHookRegistry.ts  # 15s timeout
sed -n '91p' src/utils/hooks/AsyncHookRegistry.ts  # finalizeHook
sed -n '113p' src/utils/hooks/AsyncHookRegistry.ts # checkForAsyncHookResponses
sed -n '281p' src/utils/hooks/AsyncHookRegistry.ts # finalizePendingAsyncHooks
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
