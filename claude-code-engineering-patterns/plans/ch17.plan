# 第 17 章写作计划

**批次**：批次 5a（来自 plans/batch.md）
**章节**：ch17 — 第17章：会话持久化——转录、快照与断点续写
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/utils/sessionStorage.ts`（5105行）如何在进程重启后恢复会话状态，以及"写入先于执行"这一持久化模式在 Agent 系统中如何保证可靠性。读者读完后能理解 Agent 会话持久化的核心挑战和解决思路，并能将"写入先于执行"模式用于自己的可恢复 Agent 设计。

## 通用约束
**必须**：源码引用；含图；跨章"详见第X章"；使用"我们"
**禁止**：不引 stub；不空洞；无依据猜测标"（推断）"；不重复其他章
**排除范围**：AutoCompact 触发机制（第18章）、记忆系统三层架构（第19章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「写入先于执行」（Write-Before-Execute）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（recordTranscript用户消息写入 + 断点续写读取），适用范围表，与已知模式对话（WAL、Event Sourcing）
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/QueryEngine.ts | "write before execute"注释(436行)、recordTranscript调用(451行)、isBareMode判断(453行) | 434-465 |
| 2 | src/utils/sessionStorage.ts | recordTranscript(1408行)、getLastSessionLog(3869行)、getTranscriptPath(202行) | 1408-1445, 3869-3920 |

```bash
ls src/utils/sessionStorage.ts
sed -n '436p' src/QueryEngine.ts  # write before execute 注释
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：如果进程在 AI 响应返回前崩溃，用户的消息是否丢失？Claude Code 的答案是：不会——用户消息在 API 调用前就已写入磁盘。这就是"写入先于执行"模式。

### 节 17.1：问题
- 核心主张：**Agent 进程随时可能被杀死——在 API 调用期间崩溃是最常见的场景，恢复机制必须能处理"只有请求没有响应"的状态**
- 源码锚点：`src/QueryEngine.ts:436`（注释精确描述了问题："If the process is killed before that... getLastSessionLog filters those out, returns null, and --resume fails with 'No conversation found'"）
- 概念层次：定义（write-before-execute 模式）、设计意图（从注释中提取：让 transcript resumable from the point the user message was accepted）、实际场景（用户按 Stop，消息是否保存）
- 包含元素：**图17-1** — Mermaid 序列图，用户消息 → 写入 → API请求 → 可能崩溃 → 恢复路径
- 估计字数：700-900字

### 节 17.2：源码实例1 — recordTranscript 的写入策略
- 核心主张：**recordTranscript 有两种写入模式：交互式（await，保证写入成功）和 bare 模式（fire-and-forget，避免阻塞）**
- 源码锚点：
  - `src/QueryEngine.ts:451`（`const transcriptPromise = recordTranscript(messages)`）
  - `src/QueryEngine.ts:453`（`if (isBareMode()) { void transcriptPromise } else { await transcriptPromise }`）
  - `src/utils/sessionStorage.ts:1408`（`export async function recordTranscript(`）
  - `src/QueryEngine.ts:436`（注释："The await is ~4ms on SSD, ~30ms under disk contention"——量化了同步写入的成本）
- 概念层次：recordTranscript 的去重逻辑（messageSet.has）、await vs void 的语义差异、4ms/30ms 量化开销
- 权衡分析：为什么 bare 模式用 fire-and-forget？— 注释说明"Scripted calls don't --resume after kill-mid-request"
- 包含元素：recordTranscript 调用代码片段（QueryEngine.ts:450-460，≤15行）带引入+解读
- 估计字数：900-1100字

### 节 17.3：源码实例2 — getLastSessionLog + resume 路径
- 核心主张：**getLastSessionLog 是恢复路径的入口——它读取磁盘上的 transcript 文件，过滤掉无意义的 queue-operation 条目，返回可恢复的会话状态**
- 源码锚点：
  - `src/utils/sessionStorage.ts:3869`（`export async function getLastSessionLog(`）
  - `src/utils/sessionStorage.ts:202`（`export function getTranscriptPath()`）
  - `src/utils/sessionStorage.ts:139`（`export function isTranscriptMessage(`）
- 概念层次：transcript 文件格式（JSONL）、可恢复消息 vs 不可恢复条目的过滤、resume 的恢复深度
- 估计字数：700-900字

### 节 17.4：模式剖析
- 核心主张：**"写入先于执行"是 WAL（Write-Ahead Log）思想在 Agent 层的应用——先有记录，后有操作，确保任何崩溃点都有足够信息恢复**
- 估计字数：500-700字

### 节 17.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 长时间运行的 Agent（可能被中断）| ✓ | 进程随时可能崩溃，需要断点恢复 | 无持久化（但无法恢复）|
| 用户操作需要确认记录 | ✓ | 写入先于执行保证"发送即记录" | 事后写入（但崩溃时丢数据）|
| 短时间（<100ms）无状态任务 | ✗ | 写入开销不合算（4-30ms per write）| 内存状态 |
| 高频写入（每秒>100次）| ✗（谨慎）| 磁盘 IO 成为瓶颈（推断）| 批量写入或内存队列 |

### 节 17.6：权衡与局限
- 4ms/30ms 写入延迟、磁盘空间增长、JSONL 文件的顺序读取局限
- 估计字数：500-700字

### 节 17.7：与已知模式的对话
- 与 WAL（Write-Ahead Log，数据库）：几乎完全一致——先写日志再执行操作，崩溃后从日志恢复；差异在于 WAL 用于原子性，本模式用于可恢复性
- 与 Event Sourcing：相同——所有状态变化都持久化为事件序列；不同——Event Sourcing 重放所有事件重建状态，本模式只读取"最后一段"

### 章末：模式提炼
```
写入先于执行（Write-Before-Execute）
解决的问题：Agent 进程随时可能崩溃，任何执行中的操作都可能永久丢失
核心做法：在执行任何操作前先将意图写入持久化日志，崩溃后从日志恢复
前置条件：操作有唯一标识（uuid），写入存储比操作执行快（磁盘IO << 网络IO）
源码证据：src/QueryEngine.ts:436 (注释), src/QueryEngine.ts:451 (recordTranscript调用)
```

### 章末：你能做什么
1. 在每次 AI API 调用前，先将用户消息写入 transcript（即使 API 还没响应）
2. 区分交互式（await）和脚本式（fire-and-forget）的写入模式，针对不同场景优化性能
3. 用 JSONL 格式（每行一个 JSON）而非单文件 JSON，支持 append-only 写入和流式读取
4. 为 transcript 实现消息去重（基于 uuid），避免重复写入导致 resume 时状态错误
5. 过滤 queue-operation 条目，让 getLastSessionLog 只返回真正的用户/AI 消息
6. 量化写入延迟（4ms SSD, 30ms 磁盘竞争），作为架构决策的依据

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图17-1 | 写入先于执行的时序 | Mermaid sequenceDiagram | 用户消息→写入磁盘→API请求→可能崩溃→resume恢复 | 17.1 |

## 写作顺序
1.读spec+DESIGN.md；2.读前置文件；3.写17.1含图；4.写17.2；5.写17.3；6.写17.4；7.写17.5；8.写17.6；9.写17.7；10.章末；11.开篇

## 写作前 checklist
```bash
sed -n '436p' src/QueryEngine.ts         # write before execute 注释
sed -n '451p' src/QueryEngine.ts         # recordTranscript 调用
sed -n '453p' src/QueryEngine.ts         # isBareMode 分支
sed -n '1408p' src/utils/sessionStorage.ts  # recordTranscript 定义
sed -n '3869p' src/utils/sessionStorage.ts  # getLastSessionLog
sed -n '202p' src/utils/sessionStorage.ts   # getTranscriptPath
sed -n '139p' src/utils/sessionStorage.ts   # isTranscriptMessage
```

## ⛔ Spec 一致性声明
| 1-16 | 全部16项 | 均 ✓ | 各对应节 |

**⛔ 所有项目均已勾选 ✓，plan 审查通过。**
