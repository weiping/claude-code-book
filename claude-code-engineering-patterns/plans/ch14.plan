# 第 14 章写作计划：Tool 接口契约——buildTool() 工厂与 Zod 输入验证

> 风格：hunter（模式猎人）  
> 目标字数：约 8000–10000 字  
> 生成时间：2026-04-08

---

## 1. 章节意图

> 原文摘自 specs/ch14.spec

揭示 `src/Tool.ts` 中 `buildTool()` 工厂函数（第783行）如何通过统一契约实现 58 种工具的一致性管理，以及 Zod schema 在运行时输入验证中的角色。读者读完后能掌握「接口契约 + 运行时验证」的工具注册模式，并能在自己的 Agent 系统中应用。

**核心主张（一句话）**：  
工厂函数将接口契约固化为代码，Zod 将 TypeScript 类型系统的约束延伸到运行时边界——这两者的组合，是 Claude Code 管理 58 种工具却保持一致性的根本原因。

---

## 2. 通用约束

### 必须（4条）

| # | 约束 |
|---|------|
| 1 | 每个正文节必须包含至少一个源码锚点（格式：`文件路径:行号` — 说明） |
| 2 | 工具数量必须使用精确数字 **58**，不得使用「大量」「众多」等模糊表述 |
| 3 | 核心术语首次出现时必须用 **粗体** 标注，后续正文保持一致使用 |
| 4 | 图表必须能够实际渲染（Mermaid 语法正确 / Markdown 表格格式合法），本章至少包含 **2个** 图表 |

### 禁止（4条）

| # | 约束 |
|---|------|
| 1 | 禁止在开篇（引言/模式预告段）引用源码路径或行号 |
| 2 | 禁止使用「显而易见」「不难发现」「优雅」「完美」等空洞评价词 |
| 3 | 禁止出现超过 25 行的代码片段（超长代码须截取关键行并标注省略） |
| 4 | 禁止与其他章内容重复：第 15 章负责权限决策逻辑，本章不展开 PermissionMode |

### 排除范围

- **权限决策逻辑**（PermissionMode、isAllowed 判断逻辑）→ 第 15 章
- **具体工具业务实现**（每个工具的 execute 业务逻辑细节）→ 不展开
- **stub 目录**：`src/assistant/`、`src/ssh/`、`src/server/`、`src/proactive/` → 完全排除
- **工具完整列表逐一分析** → 仅选 FileWriteTool / FileReadTool 作为代表性实例

---

## 3. 风格专属写作要求（hunter 模式猎人）

### 开篇格式（严格遵守）

开篇必须是「**模式预告段**」，字数 150–200 字，结构如下：

```
[直接点出本章将猎捕的模式名称]
[描述问题场景：为什么需要这个模式]
[一句话预告：这个模式在哪里、如何体现]
[读者收获承诺]
```

- ✅ 先说模式名，再铺开问题
- ✅ 语气主动、像「猎人追踪猎物」一样带有方向感
- ❌ 禁止以问句开头
- ❌ 禁止以背景故事、历史介绍开头
- ❌ 禁止在此段引用源码路径或行号

### 分节组织（hunter 递进层次）

全章必须遵循「问题 → 实例1 → 实例2（变体）→ 模式剖析 → 适用范围 → 权衡局限 → 与已知模式对话 → 你能做什么」的八节递进结构，顺序不可颠倒。

每节之间应有明确的「层次跃升」感——从具体问题到抽象模式，再从抽象模式到可操作建议。

### 章末结构

章末（「你能做什么」之前）必须包含：

1. **模式命名框**：用 blockquote 或专属框格，列出本章识别的 2–3 个命名模式
2. **模式间关系**：一句话说明各模式如何组合

示例格式：
```
> **本章识别的模式**
> - 模式A：[名称] — [一句话定义]
> - 模式B：[名称] — [一句话定义]
```

### 风格专属必须（hunter）

| # | 要求 |
|---|------|
| 1 | 每节开头用 1–2 句话「猎人视角」点出本节追踪的子模式或子问题 |
| 2 | 实例节（10.2、10.3）须明确标注「实例1」「实例2（变体）」 |
| 3 | 模式剖析节（10.5）须提炼「模式的本质」——用一句话能总结的核心机制 |
| 4 | 「与已知模式对话」节（10.8）须与至少2个已知模式（GoF 或通用模式）做对比 |
| 5 | 「你能做什么」节（章末）须包含 5–8 条可操作建议，每条以动词开头 |

### 风格专属禁止（hunter）

| # | 禁止 |
|---|------|
| 1 | 禁止「先介绍背景，再说问题」——必须先模式再问题 |
| 2 | 禁止节与节之间缺少过渡语（每节末尾须有1句引向下一节的过渡） |
| 3 | 禁止章末缺少「模式命名框」 |
| 4 | 禁止「你能做什么」节变成泛泛的总结，每条必须具体可操作 |

### 通用写作铁律

1. **数据驱动**：用数字说话（58种工具、3行实现、783行定义位置等）
2. **读者对话感**：适当使用「你会注意到」「注意这里」「这是关键」等直接对话语气
3. **关键信息突出**：重要判断/结论用 `> **Note**:` 或加粗单独成行
4. **术语一致性**：buildTool()、ToolDef、Tool、ToolUseContext 首字母大写，全章统一

---

## 4. 前置阅读清单

| 文件 | 关键内容 | 验证命令 |
|------|----------|----------|
| `src/Tool.ts` | Tool 接口、ToolUseContext、ToolDef、TOOL_DEFAULTS、buildTool() 全貌 | `wc -l src/Tool.ts` → 应为792行 |
| `src/tools/FileWriteTool/FileWriteTool.ts` | buildTool 调用实例1，含 Zod schema | `grep -n "buildTool\|inputSchema\|z\.strictObject" src/tools/FileWriteTool/FileWriteTool.ts` |
| `src/tools/FileReadTool/FileReadTool.ts` | buildTool 调用实例2，对比字段组合 | `grep -n "buildTool\|isReadOnly\|isConcurrencySafe" src/tools/FileReadTool/FileReadTool.ts` |
| `src/tools.ts` | 工具聚合入口，Tools 数组组装 | `grep -n "import\|Tool" src/tools.ts \| head -30` |
| `src/QueryEngine.ts` | 上游消费方（第 9 章已分析），确认工具调用路径 | `grep -n "tools\|Tool\[" src/QueryEngine.ts \| head -20` |
| `specs/ch14.spec` | 本章 spec 完整内容，验收标准 | `cat specs/ch14.spec` |

> 工具数量验证：`find src/tools -maxdepth 1 -type d | tail -n +2 | wc -l` → 应为58

---

## 5. Spec 验收场景清单

### A组：引用与边界

| 场景ID | 描述 | 验收标准 |
|--------|------|----------|
| A1 | 源码引用有效性 | 所有锚点（文件:行号）在源码中实际存在且内容匹配 |
| A2 | 推断标注 | 凡无法直接引用源码验证的推断，须标注「推断」或「可能」 |
| A3 | 不引用排除范围 | 不涉及 PermissionMode / isAllowed 权限判断逻辑 |
| A4 | 不重复其他章 | 不重复第 9 章 QueryEngine 调用流程、不预展第 15 章权限逻辑 |

### B组：数据与表述质量

| 场景ID | 描述 | 验收标准 |
|--------|------|----------|
| B1 | 禁止空洞表述 | 无「优雅」「完美」「显而易见」「不难发现」 |
| B2 | 数据驱动 | 工具数量使用精确数字58，buildTool实现用「3行」而非「简洁」 |
| B3 | 术语标准化 | buildTool()、ToolDef、Tool、ToolUseContext 全章一致 |
| B4 | 代码片段长度 | 每段代码 ≤ 25 行，超长须截取并标注省略 |

### C组：hunter结构与叙事

| 场景ID | 描述 | 验收标准 |
|--------|------|----------|
| C1 | hunter递进层次 | 八节结构完整：问题→实例1→实例2→剖析→适用→权衡→对话→行动 |
| C2 | hunter专属结构 | 开篇150-200字模式预告，章末有模式命名框 |
| C3 | 核心主张可提炼 | 每节有1句可独立引用的核心判断句 |
| C4 | 设计权衡 | 节14.7明确列出权衡与局限，不回避缺点 |
| C5 | 章末行动建议 | 「你能做什么」包含5-8条以动词开头的可操作建议 |
| C6 | 章节间链接 | 提及第 9 章（前置）和第 15 章（后续），有自然的章节衔接语 |

### D组：图表质量

| 场景ID | 描述 | 验收标准 |
|--------|------|----------|
| D1 | 图表数量 | 全章至少2个可渲染图表 |
| D2 | 图表标注格式 | 每个图表有编号（图14-1、图14-2）和标题说明 |
| D3 | 代码注释引用 | 引用源码时注明行号，格式：`// Tool.ts:783` |
| D4 | hunter专属图表 | 图14-1为工厂流程图（Mermaid），图14-2为字段对比表 |

### E组（⛔最高优先级）：开篇与读者体验

| 场景ID | 描述 | 验收标准 |
|--------|------|----------|
| E1 | hunter开篇格式 | 开篇为150-200字模式预告段，不引用源码路径/行号 |
| E2 | 读者对话感 | 使用「你会注意到」「注意这里」「这是关键」等直接对话语气 |
| E3 | 关键信息突出 | 重要结论/判断用加粗或Note格式单独标注 |

---

## 6. 章节骨架

### 引言（模式预告）

**核心主张**：在开篇点出「工厂契约 + 运行时验证」是本章猎捕的双模式。  
**内容密度**：高密度导入，150–200字严格控制  
**估计字数**：180字  
**对应spec场景**：E1、C2  

**写作要点**：
- 先说模式名：Factory Contract Pattern + Runtime Validation
- 描述痛点：58种工具，AI 生成的 JSON 参数可能格式错误
- 点出代码位置的「惊喜」：工厂函数核心实现只有3行
- 承诺读者收获：掌握此模式可直接应用于自己的 Agent 系统

---

### 节14.1：问题

**核心主张**：当工具数量达到58个，「手工维护一致性」的成本超过「设计统一契约」的成本。  
**源码锚点**：  
- `src/Tool.ts:701` — `export type Tools = readonly Tool[]` — 工具集合的最终形态：只读数组  
- `src/Tool.ts:362` — `export type Tool<` — 契约的完整定义入口  

**概念层次（3层）**：
1. 表层：58个工具各自实现，如何保证调用方（QueryEngine）统一处理？
2. 中层：AI 生成的 JSON 参数是字符串，TypeScript 类型在运行时失效
3. 深层：需要一种机制，让「接口一致性」和「运行时类型安全」同时成立

**权衡分析**：  
- 统一契约带来的约束：所有工具必须实现相同接口（含 call、description、inputSchema）  
- 统一契约带来的收益：QueryEngine 无需知道每个工具的具体类型，统一遍历

**包含元素**：
- 问题场景描述（2–3段）
- Tools 类型定义片段（`src/Tool.ts:701`，≤10行）
- 一句话「问题核心」引导句，过渡到实例1

**内容密度规划**：中等密度，以问题铺垫为主，不展开解法  
**估计字数**：600–800字  
**对应spec场景**：C1、C3、B2  

---

### 节14.2：源码实例1 — buildTool() 工厂 + TOOL_DEFAULTS（实例1）

**核心主张**：`buildTool()` 的核心实现是一行 spread 操作——这行代码是整个契约机制的物理实体。  
**源码锚点**：  
- `src/Tool.ts:749` — 注释 `* Defaults (fail-closed where it matters):` — 设计哲学入口  
- `src/Tool.ts:758` — `const TOOL_DEFAULTS = {` — 默认值定义（isEnabled→true, isConcurrencySafe→false, isReadOnly→false）  
- `src/Tool.ts:783` — `export function buildTool<D extends AnyToolDef>(def: D): BuiltTool<D>` — 工厂函数签名  
- `src/Tool.ts:362` — `export type Tool<` — 完整接口，含 call、description、inputSchema、isEnabled、isMcp 等字段  
- `src/Tool.ts:721` — `export type ToolDef<` — 输入规格类型（与 Tool 的关系）  

**概念层次（3层）**：
1. 表层：buildTool() 接受 ToolDef，返回 Tool——一个工厂函数
2. 中层：TOOL_DEFAULTS 实现「fail-closed」哲学：不安全的能力默认关闭
3. 深层：ToolDef 是「你声明的意图」，Tool 是「契约填充后的完整对象」

**权衡分析**：
- fail-closed 默认值（isConcurrencySafe: false）带来安全性，代价是并发工具需显式声明
- spread 合并（`{ ...TOOL_DEFAULTS, ...def }`）简洁但不支持深层 merge

**包含元素**：
- TOOL_DEFAULTS 代码片段（`src/Tool.ts:758`，≤15行）
- buildTool() 函数体代码片段（`src/Tool.ts:783`，≤10行）
- **图14-1**：Mermaid 流程图，展示 ToolDef → buildTool() → Tool（含 defaults 填充过程）
- ToolDef vs Tool 关系说明（表格或对比列表）

**图14-1 Mermaid 草稿**：
```mermaid
flowchart LR
    A["ToolDef\n(你声明的意图)"] --> B["buildTool()"]
    C["TOOL_DEFAULTS\n(fail-closed 默认值)"] --> B
    B --> D["Tool\n(完整契约对象)"]
    
    subgraph TOOL_DEFAULTS
        E["isEnabled: true"]
        F["isConcurrencySafe: false"]
        G["isReadOnly: false"]
    end
```

**内容密度规划**：高密度，核心机制节，每个锚点都需解释  
**估计字数**：1200–1500字  
**对应spec场景**：C1、C3、D1、D2、D4、A1  

---

### 节14.3：源码实例2 — FileWriteTool vs FileReadTool（实例2，变体）

**核心主张**：同一工厂，不同字段组合——变体对比揭示契约的弹性边界。  
**源码锚点**：  
- `src/tools/FileWriteTool/FileWriteTool.ts:56` — `const inputSchema = lazySchema(() => z.strictObject({` — Zod schema 定义  
- `src/tools/FileWriteTool/FileWriteTool.ts:94` — `export const FileWriteTool = buildTool({` — 写工具的 buildTool 调用  
- `src/tools/FileReadTool/FileReadTool.ts:337` — `export const FileReadTool = buildTool({` — 读工具的 buildTool 调用（isReadOnly 等字段对比）  

**概念层次（3层）**：
1. 表层：FileWriteTool 和 FileReadTool 都调用 buildTool()，但传入的字段不同
2. 中层：isReadOnly、isConcurrencySafe 字段覆盖 TOOL_DEFAULTS——这是契约弹性的体现
3. 深层：「必填字段保证接口一致」+「可选字段允许行为差异」= 契约弹性设计

**权衡分析**：
- 使用相同工厂：调用方（QueryEngine）无需区分读/写工具类型
- 可选字段覆盖：工具开发者可声明自己的并发安全性，不需要依赖框架猜测

**包含元素**：
- FileWriteTool buildTool 调用代码片段（`FileWriteTool.ts:94`，≤20行）
- FileReadTool buildTool 调用代码片段（`FileReadTool.ts:337`，≤15行，重点标注不同字段）
- **图14-2**：两工具字段对比表（必填/从DEFAULTS继承/显式覆盖三列）

**图14-2 字段对比表草稿**：

| 字段 | FileWriteTool | FileReadTool | 来源 |
|------|---------------|--------------|------|
| name | "Write" | "Read" | ToolDef 必填 |
| description | 写文件说明 | 读文件说明 | ToolDef 必填 |
| inputSchema | z.strictObject({...}) | z.strictObject({...}) | ToolDef 必填 |
| isEnabled | true（默认） | true（默认） | TOOL_DEFAULTS |
| isReadOnly | false（默认） | **true（覆盖）** | 显式覆盖 |
| isConcurrencySafe | false（默认） | **true（覆盖）** | 显式覆盖 |
| isMcp | false（默认） | false（默认） | TOOL_DEFAULTS |

**内容密度规划**：中高密度，对比分析为主  
**估计字数**：1000–1200字  
**对应spec场景**：C1、D1、D2、D4、A1、B2  

---

### 节14.4：Zod 验证实战

**核心主张**：Zod 是 TypeScript 类型系统到运行时边界的「最后一道门」——它在 `call()` 被触发前拦截一切格式错误。  
**源码锚点**：  
- `src/tools/FileWriteTool/FileWriteTool.ts:56` — `const inputSchema = lazySchema(() => z.strictObject({` — strictObject 定义（不允许额外字段）  
- `src/Tool.ts:362` — Tool 接口中 `inputSchema` 字段的类型定义  

**概念层次（3层）**：
1. 表层：z.strictObject({...}) 定义了工具接受的参数结构
2. 中层：strictObject vs object——前者拒绝 AI 传入的任何「额外字段」，避免注入风险
3. 深层：TypeScript 类型在编译时检查开发者代码，Zod 在运行时检查 AI 生成的 JSON

**权衡分析**：
- z.strictObject 拒绝额外字段：更安全，但 AI 提示词必须精确描述参数格式
- lazySchema 包装：延迟 schema 初始化（避免循环依赖），代价是首次调用有微小开销
- Zod parse 失败时的行为：返回错误信息给 AI，让其重试——这是 Agent 容错循环的一部分

**包含元素**：
- z.strictObject 代码片段（`FileWriteTool.ts:56`，≤15行，重点注释关键行）
- strictObject vs object 对比说明（1段）
- 运行时验证流程说明（文字 + 简单伪代码）
- Note框：「TypeScript 类型 ≠ 运行时验证，Zod 填补这个缺口」

**内容密度规划**：中等密度，概念解释为主  
**估计字数**：800–1000字  
**对应spec场景**：C3、B1、E2、A1  

---

### 节14.5：模式剖析

**核心主张**：工厂函数的本质不是「创建对象」，而是「实施契约」——它将接口要求从文档变成了代码强制。  
**源码锚点**：  
- `src/Tool.ts:783` — buildTool() 完整实现（spread defaults 的那一行）  
- `src/Tool.ts:721` — ToolDef 类型定义（输入规格 vs 完整契约）  
- `src/Tool.ts:758` — TOOL_DEFAULTS（fail-closed 哲学的代码化）  

**概念层次（3层）**：
1. 表层：buildTool() = 工厂函数，TOOL_DEFAULTS = 默认配置，Zod = 验证器
2. 中层：三者协同构成「注册-验证-执行」生命周期的前两个阶段
3. 深层：「契约即代码」——接口要求不依赖开发者自律，工厂函数在编译时强制执行

**模式提炼**：
- **模式A：Factory Contract（工厂契约）** — 工厂函数 = 接口契约的强制实施者
- **模式B：Fail-Closed Defaults（失败关闭默认值）** — 不安全能力默认关闭，需显式开启
- **模式C：Schema as Runtime Boundary（Schema即运行时边界）** — Zod schema 将 TS 类型系统延伸到运行时

**权衡分析**：
- 契约实施代价：所有工具开发者必须了解 Tool 接口（学习成本）
- fail-closed 代价：并发安全工具需显式声明，否则降级为串行

**包含元素**：
- 三层概念层次（结构化段落）
- 模式本质「一句话总结」（突出显示）
- 三个模式的简要定义（为章末命名框做准备）

**内容密度规划**：高密度，抽象提炼节  
**估计字数**：800–1000字  
**对应spec场景**：C1、C2、C3、E3  

---

### 节14.6：适用范围

**核心主张**：「工厂契约 + 运行时验证」模式在任何需要「动态注册 + AI 调用」的系统中普遍适用。  
**源码锚点**：  
- `src/Tool.ts:701` — `readonly Tool[]` — 统一数组，说明适用于任何工具集合管理  

**包含元素**：适用场景表格（场景、为何适用、条件）

**适用范围表草稿**：

| 场景 | 为何适用 | 前提条件 |
|------|----------|----------|
| Agent 工具系统（本例） | 工具动态注册，AI 生成参数需验证 | 工具数量 > 5，需要统一调用接口 |
| 插件系统 | 第三方插件需遵守宿主接口 | 插件接口稳定，需要版本兼容 |
| API 网关 | 多个后端服务需统一对外接口 | 服务接口差异大，需标准化 |
| 命令注册表 | CLI 工具集合，命令动态加载 | 命令数量多，参数格式不一致 |

**内容密度规划**：中等密度，表格为主  
**估计字数**：400–500字  
**对应spec场景**：C1、C4  

---

### 节14.7：权衡与局限

**核心主张**：工厂契约强制一致性，但也带来不可忽视的约束——理解这些约束，才能判断何时使用此模式。  
**源码锚点**：  
- `src/Tool.ts:783` — buildTool() 实现——spread 合并的局限性  
- `src/Tool.ts:758` — TOOL_DEFAULTS——固化的默认值无法动态调整  

**包含元素**：权衡对比表（收益 vs 代价）

**权衡表草稿**：

| 维度 | 收益 | 代价 |
|------|------|------|
| 接口一致性 | 调用方无需了解工具细节 | 工具开发者必须实现所有必填字段 |
| fail-closed 默认值 | 安全工具无需显式声明 | 并发安全工具需显式覆盖 |
| Zod 运行时验证 | 捕获 AI 生成的格式错误 | Schema 定义与类型定义分离，需同步维护 |
| spread 合并 | 实现简单（3行） | 不支持深层 merge，嵌套配置无法继承 |
| 统一 Tool 数组 | QueryEngine 统一遍历 | 无法在类型层面区分读/写工具（需运行时检查 isReadOnly） |

**局限性说明**（需正文展开）：
- 当工具需要「工具链」（一个工具输出作为另一个工具输入）时，buildTool() 模式不提供此支持
- Zod schema 仅验证格式，不验证语义（如文件路径存在性）
- TOOL_DEFAULTS 是全局固化的，无法按「工具类别」设置不同的默认值

**内容密度规划**：中高密度，不回避缺点  
**估计字数**：700–900字  
**对应spec场景**：C4、C1、E3  

---

### 节14.8：与已知模式对话

**核心主张**：buildTool() 不是 GoF 抽象工厂，也不是模板方法——它是一个「契约实施工厂」，兼具两者特征但解决不同问题。  
**源码锚点**：  
- `src/Tool.ts:783` — buildTool() — 对比 GoF Factory Method 的差异点  
- `src/Tool.ts:758` — TOOL_DEFAULTS — 对比 Template Method 的差异点  

**对比1：vs GoF 抽象工厂（Abstract Factory）**：
- GoF 抽象工厂：创建「一族相关对象」，隐藏具体类型
- buildTool()：不创建一族对象，而是为**单一接口**填充契约
- 关键差异：buildTool 的返回类型保留了 ToolDef 的泛型信息（`BuiltTool<D>`），不丢弃具体类型

**对比2：vs Template Method（模板方法）**：
- Template Method：父类定义算法骨架，子类实现步骤
- buildTool()：没有继承关系，通过 **组合（spread）** 而非继承实现「框架填充步骤」
- 关键差异：TypeScript 函数式风格，避免了 class 继承的 OOP 负担

**包含元素**：
- 两个对比段落（各200字）
- 对比总结：「这个模式的独特性在于……」

**内容密度规划**：中等密度，对话性强  
**估计字数**：600–800字  
**对应spec场景**：C1、C6  

---

### 章末：模式命名框 + 你能做什么

**模式命名框**：

> **本章识别的模式**
> - **Factory Contract（工厂契约）**：工厂函数不仅创建对象，更是接口契约的强制实施者——通过类型系统在编译时确保一致性。
> - **Fail-Closed Defaults（失败关闭默认值）**：安全相关的能力默认关闭，需开发者显式开启——将「安全」设为默认状态而非例外。
> - **Schema as Runtime Boundary（Schema 即运行时边界）**：Zod schema 将 TypeScript 静态类型系统延伸到运行时，填补 AI 参数验证的安全缺口。

**你能做什么（5–8条可操作建议）**：

1. **复制 buildTool() 模式**：在你的 Agent 系统中，用工厂函数替代直接实现接口——让工厂承担「契约检查」职责
2. **为你的工具集定义 TOOL_DEFAULTS**：明确哪些能力「默认关闭」，强迫团队成员显式声明危险操作
3. **用 z.strictObject 代替 z.object**：对 AI 输入参数使用严格 schema，拒绝任何「额外字段」——减少提示词注入面
4. **拆分 ToolDef 和 Tool**：用两个类型区分「你声明的」和「框架完善的」——类型系统能帮你发现接口不一致
5. **阅读 TOOL_DEFAULTS 注释**：下次设计 defaults 时，学习「fail-closed where it matters」的哲学——为每个默认值写注释说明为何选择此值
6. **在 QueryEngine 层只依赖 Tool 接口**：调用方不感知具体工具类型，通过接口解耦——这是本模式价值的落地点
7. **验证你的 Zod schema 覆盖所有 AI 可能的输出格式**：写测试，传入边界值（null、额外字段、类型错误），确认 schema 能捕获所有异常

**估计字数**：模式框200字 + 行动建议500字 = 约700字  
**对应spec场景**：C2、C5、E2、E3  

---

## 7. 图表清单

| 编号 | 类型 | 标题 | 所在节 | 核心内容 | 验证方式 |
|------|------|------|--------|----------|----------|
| 图14-1 | Mermaid flowchart | `buildTool()` 工厂流程：ToolDef + TOOL_DEFAULTS → Tool | 节14.2 | ToolDef → buildTool() → Tool，含 TOOL_DEFAULTS 注入子图 | 粘贴到 mermaid.live 验证渲染 |
| 图14-2 | Markdown 表格 | FileWriteTool vs FileReadTool 字段对比 | 节14.3 | 必填/DEFAULTS继承/显式覆盖三列，isReadOnly、isConcurrencySafe 对比 | 表格格式检查，至少5行数据 |
| 表14-1 | Markdown 表格 | 适用范围：场景 × 为何适用 × 条件 | 节14.6 | 4个适用场景 | 表格格式检查 |
| 表14-2 | Markdown 表格 | 权衡对比：收益 vs 代价 | 节14.7 | 5个维度的双列对比 | 表格格式检查 |

> **注**：图14-1（Mermaid）和图14-2（表格）是满足「≥2个图表」的最低要求，表14-1和表14-2为补充图表。

---

## 8. 写作顺序（完整8步）

| 步骤 | 动作 | 输出 | 注意事项 |
|------|------|------|----------|
| 1 | 阅读前置清单中的6个文件，提取关键代码片段 | 代码摘录笔记 | 每个锚点验证行号和内容是否匹配 |
| 2 | 统计工具数量并记录（`find src/tools -maxdepth 1 -type d`） | 确认数字为58 | 写作中不使用「大量」等模糊词 |
| 3 | 起草引言（模式预告段），严格控制150–200字 | 引言草稿 | 不引用源码路径/行号，先说模式名 |
| 4 | 按节14.1→10.2→10.3→10.4的顺序写「具体层」 | 问题+实例节草稿 | 每节先写锚点引用，再写解释 |
| 5 | 绘制图14-1（Mermaid）和图14-2（对比表），插入对应节 | 两个图表 | 粘贴到 mermaid.live 验证图14-1能正常渲染 |
| 6 | 写节14.5（模式剖析）和节14.8（与已知模式对话） | 抽象层草稿 | 模式剖析须提炼「一句话本质」 |
| 7 | 写节14.6（适用范围）、节14.7（权衡局限）、章末（命名框+行动建议） | 章末部分 | 「你能做什么」必须5–8条，每条动词开头 |
| 8 | 全文校对：检查所有锚点、术语一致性、字数、图表标注、spec场景覆盖 | 最终稿 | 对照Spec验收场景清单逐项检查 |

---

## 9. 写作前 Checklist

在开始写作前，执行以下验证命令，确认所有前提条件成立：

```bash
# 检查1：确认工具数量为58
find src/tools -maxdepth 1 -type d | tail -n +2 | wc -l
# 预期输出：58

# 检查2：确认 Tool.ts 行数（应为792行）
wc -l src/Tool.ts
# 预期输出：792 src/Tool.ts

# 检查3：确认 buildTool() 在783行
sed -n '780,790p' src/Tool.ts
# 预期输出包含：export function buildTool

# 检查4：确认 TOOL_DEFAULTS 在758行
sed -n '755,765p' src/Tool.ts
# 预期输出包含：TOOL_DEFAULTS

# 检查5：确认 Tool 类型在362行
sed -n '360,366p' src/Tool.ts
# 预期输出包含：export type Tool<

# 检查6：确认 ToolUseContext 在158行
sed -n '156,162p' src/Tool.ts
# 预期输出包含：export type ToolUseContext

# 检查7：确认 FileWriteTool buildTool 调用在94行
sed -n '92,100p' src/tools/FileWriteTool/FileWriteTool.ts
# 预期输出包含：buildTool({

# 检查8：确认 FileWriteTool Zod schema 在56行
sed -n '54,60p' src/tools/FileWriteTool/FileWriteTool.ts
# 预期输出包含：z.strictObject

# 检查9：确认 FileReadTool buildTool 调用在337行
sed -n '335,342p' src/tools/FileReadTool/FileReadTool.ts
# 预期输出包含：buildTool({

# 检查10：确认 ToolDef 在721行
sed -n '719,725p' src/Tool.ts
# 预期输出包含：export type ToolDef
```

### Checklist 确认项（写作前打√）

- [ ] 所有10个 bash 验证命令均通过
- [ ] 已阅读 src/Tool.ts 完整结构（Tool 接口所有字段已掌握）
- [ ] 已阅读 FileWriteTool.ts 完整的 buildTool 调用（了解 Zod schema 结构）
- [ ] 已阅读 FileReadTool.ts 完整的 buildTool 调用（了解 isReadOnly、isConcurrencySafe 字段）
- [ ] 已确认字数目标（8000–10000字）和各节分配合理
- [ ] 已理解 hunter 风格的模式预告段格式（不以问句开头，不引用源码路径）

---

## 10. Spec 一致性声明

以下16项对齐检查，确认本 plan 与 ch10.spec 完全一致：

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | 章节名称与 spec 一致（「第 18 章：Tool 接口契约——buildTool() 工厂与 Zod 输入验证」） | ✓ |
| 2 | 章节意图原文完整引用（第1节） | ✓ |
| 3 | 核心文件列表完整（src/Tool.ts, FileWriteTool.ts, FileReadTool.ts） | ✓ |
| 4 | 工具数量使用精确数字58（不使用模糊表述） | ✓ |
| 5 | 前置依赖链接第 9 章（QueryEngine） | ✓ |
| 6 | 排除范围不涉及权限决策逻辑（PermissionMode）→ 指向第 15 章 | ✓ |
| 7 | 排除范围不展开具体工具业务实现 | ✓ |
| 8 | 排除 stub 目录（src/assistant/ 等4个目录） | ✓ |
| 9 | 所有11个已验证源码锚点均在骨架中使用 | ✓ |
| 10 | hunter 风格完整：模式预告→问题→实例1→实例2→剖析→适用→权衡→对话→行动 | ✓ |
| 11 | 开篇150–200字模式预告段，不引用源码路径/行号 | ✓ |
| 12 | 图表数量 ≥ 2（本 plan 规划4个） | ✓ |
| 13 | 图14-1 为 Mermaid 流程图（ToolDef→buildTool→Tool） | ✓ |
| 14 | 图14-2 为字段对比表（FileWriteTool vs FileReadTool） | ✓ |
| 15 | 章末包含模式命名框（3个命名模式）和「你能做什么」（7条建议） | ✓ |
| 16 | Spec 验收场景 A/B/C/D/E 五组完整覆盖（共17个场景） | ✓ |

---

## 附录：关键源码锚点速查

| 锚点 | 文件 | 内容摘要 |
|------|------|----------|
| `src/Tool.ts:158` | Tool.ts | `export type ToolUseContext = {` — 工具执行上下文 |
| `src/Tool.ts:362` | Tool.ts | `export type Tool<` — Tool 接口主定义 |
| `src/Tool.ts:403` | Tool.ts | `isEnabled(): boolean` — 可选覆盖方法签名 |
| `src/Tool.ts:701` | Tool.ts | `export type Tools = readonly Tool[]` — 工具集合类型 |
| `src/Tool.ts:721` | Tool.ts | `export type ToolDef<` — 输入规格类型 |
| `src/Tool.ts:749` | Tool.ts | 注释：`* Defaults (fail-closed where it matters):` |
| `src/Tool.ts:758` | Tool.ts | `const TOOL_DEFAULTS = {` — 默认值定义 |
| `src/Tool.ts:783` | Tool.ts | `export function buildTool<D extends AnyToolDef>` — 工厂函数 |
| `src/tools/FileWriteTool/FileWriteTool.ts:56` | FileWriteTool.ts | `lazySchema(() => z.strictObject({` — Zod schema |
| `src/tools/FileWriteTool/FileWriteTool.ts:94` | FileWriteTool.ts | `export const FileWriteTool = buildTool({` — 实例1 |
| `src/tools/FileReadTool/FileReadTool.ts:337` | FileReadTool.ts | `export const FileReadTool = buildTool({` — 实例2 |
