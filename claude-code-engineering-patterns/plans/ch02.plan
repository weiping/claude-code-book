# 第 2 章写作计划

**批次**：批次 1b（来自 plans/batch.md）
**章节**：ch02 — Bootstrap 与全局状态——50+ 函数背后的单例设计
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

揭示 `src/bootstrap/state.ts`（1762 行）为何选择模块级变量而非依赖注入作为全局状态管理方案，以及这一选择在 Agent 系统中的实际工程效果。读者读完后能说清"轻量全局状态"模式的问题场景、解决方案与取舍，并能在自己的项目中判断何时该用此模式、何时该用依赖注入。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**（从 spec 约束·必须 前 4 条提取）：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**（从 spec 约束·禁止 前 4 条提取）：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- bootstrap/state.ts 的所有调用方业务逻辑（只分析状态模块本身）
- 依赖注入框架的详细实现（只做概念对比，不展开框架源码）
- src/assistant/、src/ssh/、src/server/、src/proactive/（排除范围 stub）

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例 1` → `## 源码实例 2（变体）` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号（开篇是问题场景+模式预告+价值承诺，150-200字）
- 全章不得混用其他风格的写作手法

**通用写作铁律**（所有风格通用，写作期间必须遵守）：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据，禁止基于 AI 记忆写作
- ⛔ 代码无裸露：禁止"标题→代码→下一个标题"，每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文（中文（English Full Name, ABBR)）
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写，禁止在开篇直接引用源码

## 前置阅读清单
（按阅读顺序排列，不超过 6 个文件）

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/bootstrap/state.ts | State 类型定义（行 45-100）、STATE 常量（行 429）、get/set 函数对 | 行 45-100, 429-540, 846-860 |
| 2 | src/utils/signal.ts | createSignal 定义（纯事件信号原语，约 30 行全文件） | 全文件 |
| 3 | src/bootstrap/state.ts | setIsRemoteMode（行 1635）、sessionSwitched signal（行 481） | 行 481-490, 1635-1640 |
| 4 | src/bootstrap/state.ts | regenerateSessionId（行 435-450，展示 STATE 直接变异） | 行 435-450 |

验证文件存在：
```bash
ls src/bootstrap/state.ts src/utils/signal.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用应存在于实际文件中 | 节 2.1-2.7（骨架每节均有锚点） | ☐ |
| A2 | 推断有标注 | 无源码直接依据的结论标注「（推断）」 | 骨架全部节（DCE 设计意图等推断处） | ☐ |
| A3 | 不引用排除范围 | 不引用 src/assistant/、src/ssh/、src/server/、src/proactive/ | 骨架全部节（已避开排除范围） | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 M 章"代替重复 | 节 2.1（第1章"存在性"用"详见第1章"） | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全章 | ☐ |
| B2 | 函数数量具体化 | 使用精确数字（grep 统计 export function 数量 = 212）而非"50+"泛指 | 节 2.1（212 个导出函数/常量） | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 节 2.1-2.7（每个术语首次出现时标注） | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 概念递进层次 | 按定义→设计意图→实际案例递进 | 节 2.1-2.4（每节覆盖至少两层） | ☐ |
| C2 | ⛔ 风格专属结构 | hunter：模式预告开篇→问题→源码实例→模式剖析→适用范围→权衡与局限→与已知模式对话→你能做什么 | 骨架整体结构与 spec 一致 | ☐ |
| C3 | 核心主张可提炼 | "为什么不用依赖注入"有明确回答 | 节 2.1（单例 vs DI 权衡） | ☐ |
| C4 | 设计权衡有说明 | 模块级变量 vs DI 的"选 A 不选 B"理由 | 节 2.1、节 2.5（权衡分析） | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末"你能做什么" | ☐ |
| C6 | 章节间链接 | 章末向后链接 + 章节内向前链接 | 节 2.1（向前引用第1章）+ 章末（向后引用第 6 章） | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表可渲染且达标 | ≥1 个 Mermaid 图（<2000字→≥2） | 节 2.2（架构图）、节 2.3（信号流图） | ☐ |
| D2 | 图表标注格式 | 上方 `**图 2-X：标题**`，下方说明文字 | 节 2.2-2.3（每个图表） | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 节 2.1（State 类型片段）、节 2.2（get/set 配对） | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 节 2.1（STATE 注释）、节 2.2（createSignal 注释） | ☐ |
| D5 | ⛔ 风格专属图表 | hunter：适用范围表 + 模式命名框 | 节 2.5（适用范围表）、章末（模式命名框） | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 开篇不引用源码，含三要素：问题场景+模式预告+价值承诺 | 开篇（最后补写） | ☐ |
| E2 | 多实例证明模式普遍性 | ≥2 处不同位置的源码实例（STATE 对象 + createSignal 信号） | 节 2.2（STATE 实例1）、节 2.3（createSignal 实例2） | ☐ |
| E3 | 模式命名框格式规范 | 模式名称+问题+解决方案+源码锚点 | 章末模式提炼 | ☐ |
| E4 | 读者对话感 | "我们"而非"用户"，有设问和预告 | 全章 | ☐ |
| E5 | 关键信息突出 | 加粗标注，表格展示对比 | 节 2.4（对比表）、节 2.5（适用范围表） | ☐ |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

## 章节骨架

### 开篇（hunter 模式预告）
> ⛔ 开篇禁止直接引用源码。在全章初稿完成后最后补写。

- 开篇公式：问题场景（"一个 Agent 系统需要共享会话状态、模型选择、权限设置等几十种全局信息——如何在 CLI 环境下管理这些状态？"）+ 模式预告（"模块级单例（Module Singleton）——Claude Code 用 1762 行代码、212 个导出函数实现了一个零依赖的全局状态管理方案"）+ 价值承诺（"读完本章，我们将理解这个看似朴素的选择背后的工程考量，以及何时该用、何时该换"）
- 目标：150-200 字

### 节 2.1：问题——1762 行状态模块为何拒绝依赖注入？
- 核心主张：`src/bootstrap/state.ts` 用模块级变量 + get/set 函数对管理全局状态，而非依赖注入（DI）容器——这个选择基于 CLI 环境的"单进程、单会话"约束（对应验收场景 C3、C4）
- 源码锚点：`src/bootstrap/state.ts:45` — `type State = {`（State 类型定义起始）；`src/bootstrap/state.ts:429` — `const STATE: State = getInitialState()`（全局状态实例）
- 概念层次：定义（State 类型是什么）→ 设计意图（为什么用模块级变量而非 DI）→ 实际案例（212 个导出函数的组织方式）
- 权衡分析：模块级变量 vs 依赖注入——CLI 环境下单进程单会话，DI 的多实例隔离能力无用武之地；但代价是测试时需要 mock 模块或提供 reset 函数
- 包含元素：代码片段（State 类型前 15 行 + STATE 实例化，≤20行）+ 代码片段（get/set 配对示例，≤10行）
- **⛔ 内容密度规划**：
  - 引入段要点：说明"入口文件背后的状态模块是全书的基础设施——第1章说它存在，本章展开它怎么做"
  - 解读段要点：分析 State 类型 50+ 字段的设计——为什么全部放在一个对象而非多个 Store？注释"Resolve symlinks in cwd"等工程细节
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1200-1500 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch02_singleton_vs_di、ch02_function_count
- 向后引用：bootstrap 存在性"详见第1章"，全局状态如何被查询引擎使用"详见第 6 章"

### 节 2.2：源码实例 1——STATE 对象与 get/set 函数对
- 核心主张：`STATE` 对象（`src/bootstrap/state.ts:429`）通过 `getInitialState()` 初始化，212 个导出函数中大部分是 get/set 配对——这是一种"显式访问接口"策略，暴露读/写函数而非直接暴露 STATE 对象本身（对应验收场景 ch02_source_anchors）
- 源码锚点：
  - `src/bootstrap/state.ts:429` — `const STATE: State = getInitialState()`（已验证）
  - `src/bootstrap/state.ts:500` — `export function getOriginalCwd(): string`（已验证）
  - `src/bootstrap/state.ts:515` — `export function setOriginalCwd(cwd: string): void`（已验证）
  - `src/bootstrap/state.ts:846` — `export function setMainLoopModelOverride(`（已验证）
  - `src/bootstrap/state.ts:435-450` — `regenerateSessionId`（展示 STATE 直接变异，已验证）
- 概念层次：设计意图（为什么不直接 export STATE）→ 实际案例（get/set 配对如何控制访问粒度）
- 权衡分析：封装粒度——get/set 函数对允许精确控制读写权限（只读属性只有 get），但增加了代码量（212 个函数）
- 包含元素：
  - **Mermaid 图**：STATE 对象的 get/set 访问模式（调用方 → get/set 函数 → STATE 对象）
  - 代码片段：getOriginalCwd + setOriginalCwd 配对（≤10行）
  - 代码片段：regenerateSessionId 展示 STATE 直接变异（≤15行）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"212 个导出函数不是杂乱无章，而是精心设计的访问接口层"
  - 解读段要点：分析 getOriginalCwd/setOriginalCwd 的读写分离设计，以及 regenerateSessionId 为什么直接操作 STATE（注释中的"planSlugCache"清理逻辑）
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1500-2000 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch02_source_anchors、ch02_concurrency_note

### 节 2.3：源码实例 2（变体）——createSignal 的事件订阅模式
- 核心主张：state.ts 不仅用 get/set 管理状态值，还用 `createSignal`（`src/utils/signal.ts`）提供事件通知能力——这是模块级单例模式的第二个变体：**无状态信号**（对应验收场景 ch02_multi_instance_proof）
- 源码锚点：
  - `src/bootstrap/state.ts:481` — `const sessionSwitched = createSignal<[id: SessionId]>()`（已验证）
  - `src/bootstrap/state.ts:24` — `import { createSignal } from 'src/utils/signal.js'`（已验证）
  - `src/utils/signal.ts` — createSignal 完整实现（约 30 行，已验证）
- 概念层次：定义（createSignal 是什么——纯事件信号，无存储状态）→ 设计意图（为什么不直接用 EventEmitter）→ 实际案例（sessionSwitched 信号的使用）
- 权衡分析：createSignal vs EventEmitter——createSignal 更轻量（~30 行 vs Node.js EventEmitter 的数百行），支持类型安全的参数，但不支持 once/removeListener 等高级功能
- 包含元素：
  - 代码片段：createSignal 源码（全文件约 30 行，可完整展示）
  - 代码片段：sessionSwitched 信号在 state.ts 中的使用（≤10行）
  - **对比表格**：createSignal vs EventEmitter vs RxJS（维度：大小、类型安全、功能、适用场景）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"212 个函数中有一个与众不同的——它不是读写状态，而是通知事件"
  - 解读段要点：分析 createSignal 的注释——"Distinct from a store... there is no snapshot, no getState"——为什么需要区分"信号"和"存储"
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1000-1300 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch02_multi_instance_proof
- 关键区别说明：实例 1（STATE 对象）是"有状态的模块级单例"，实例 2（createSignal）是"无状态的模块级事件总线"——两者共享"模块级"这一注册位置，但解决的问题不同

### 节 2.4：模式剖析——模块级单例的两个变体
- 核心主张：Claude Code 的全局状态管理可以归纳为两个变体——**有状态单例**（STATE 对象 + get/set）和**无状态信号**（createSignal）——它们共享"模块作用域即容器"这一核心做法，但在访问模式上完全不同（对应验收场景 ch02_pattern_box_format）
- 源码锚点：引用节 2.2 和 2.3 的锚点
- 概念层次：模式定义 → 变体对比 → 每个变体的关键区别
- 权衡分析：统一容器（DI）vs 分散模块（Claude Code 选择）——分散避免了中心化配置文件，但增加了学习成本
- 包含元素：
  - **对比表格**：变体 | 存储状态 | 访问方式 | 通知能力 | 实例
  - 模式命名框：模块级单例（Module Singleton）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"两个变体看似不同，实则是同一模式的两面"
  - 解读段要点：分析为什么同一个文件中出现了两种不同的状态管理模式
  - 散文与代码比例目标：全散文 + 表格
- 估计字数：800-1000 字
- 对应 spec 场景：ch02_pattern_box_format

### 节 2.5：适用范围——何时用/不用模块级单例
- 核心主张：模块级单例适合"单进程、单会话、读写比高"的 CLI 场景，不适合"多实例隔离、运行时依赖替换、跨进程共享"的服务端场景（对应验收场景 ch02_applicability_table）
- 源码锚点：引用节 2.2 的 STATE 和节 2.3 的 createSignal
- 概念层次：适用场景 → 不适用场景 → 替代方案
- 权衡分析：N/A（本节本身就是权衡分析）
- 包含元素：
  - **适用范围表**：场景 | 适用(✓/✗) | 理由 | 替代方案
- **⛔ 内容密度规划**：
  - 引入段要点：预告"模式不是银弹——CLI 和服务端的需求完全不同"
  - 解读段要点：表格中的每行附带具体理由
  - 散文与代码比例目标：全散文
- 估计字数：400-600 字
- 对应 spec 场景：ch02_applicability_table

### 节 2.6：权衡与局限——单线程假设与测试困境
- 核心主张：模块级单例的前提假设是"单线程主循环"（Bun 主线程），如果引入 Web Worker 或多进程，模块级变量的共享语义将失效。此外，212 个函数的测试依赖 reset/mock 机制，state.ts 提供了 `regenerateSessionId` 等重置函数作为补偿（对应验收场景 ch02_tradeoffs_and_limits、ch02_concurrency_note）
- 源码锚点：`src/bootstrap/state.ts:435` — `regenerateSessionId`（重置函数示例，已验证）
- 概念层次：前提假设（单线程）→ 测试困境（如何 mock）→ 补偿机制
- 权衡分析：本节分析模块级单例模式本身的权衡
- 包含元素：无代码（纯分析散文）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"每个模式都有前提假设——违反假设就会出问题"
  - 解读段要点：分析 Bun 单线程假设、测试隔离问题、reset 函数的局限性
  - 散文与代码比例目标：全散文
- 估计字数：500-700 字
- 对应 spec 场景：ch02_tradeoffs_and_limits、ch02_concurrency_note

### 节 2.7：与已知模式的对话——Module Singleton vs Singleton Pattern vs Service Locator
- 核心主张：Claude Code 的模块级单例与 GoF 的 Singleton Pattern（单例模式）和 Martin Fowler 的 Service Locator（服务定位器）有共同祖先——"全局唯一的访问点"——但模块级单例依赖模块系统的加载机制而非运行时锁或注册表（对应验收场景 ch02_known_pattern_dialogue）
- 源码锚点：引用节 2.2 和 2.3 的实例
- 概念层次：已知模式描述 → 共同点 → 不同点 → 演化动机
- 权衡分析：N/A
- 包含元素：
  - **对比表格**：模式 | 唯一性保证 | 访问方式 | 测试隔离 | Claude Code 中的实例
- **⛔ 内容密度规划**：
  - 引入段要点：预告"这个模式在经典设计模式中有亲戚——但实现方式完全不同"
  - 解读段要点：分析 GoF Singleton（运行时锁）vs Service Locator（注册表查找）vs Module Singleton（模块加载机制）的本质差异
  - 散文与代码比例目标：全散文
- 估计字数：600-800 字
- 对应 spec 场景：ch02_known_pattern_dialogue

### 章末：模式提炼（hunter 风格）
- 核心模式：**模块级单例（Module Singleton）**
  - 模式名称：模块级单例（Module Singleton）
  - 解决的问题：在单进程 CLI 环境中，如何用零依赖的方式管理全局共享状态？
  - 核心做法：利用模块系统的加载机制保证唯一性，用 get/set 函数对封装访问接口，用 createSignal 提供事件通知
  - 前置条件：单进程执行环境、模块系统保证单次加载、不需要运行时替换依赖
  - 源码证据：`src/bootstrap/state.ts:429`（STATE）、`src/bootstrap/state.ts:481`（sessionSwitched signal）、`src/utils/signal.ts`（createSignal）

### 章末：你能做什么
预规划 5-8 条行动建议：
1. 用 `grep -c "export function" src/bootstrap/state.ts` 验证本章提到的 212 个导出函数数量
2. 阅读 `src/bootstrap/state.ts:45-100` 的 State 类型定义，数一数有多少个字段与你预期的一致
3. 在自己的 CLI 项目中，对比 DI 容器和模块级变量的启动性能差异
4. 用 `grep -n "createSignal" src/ -r` 搜索 createSignal 在代码库中的所有使用位置，感受信号的渗透程度
5. 追踪 `getOriginalCwd()` 的一个调用方，理解模块级函数如何替代 DI 注入
6. 思考你的 Agent 项目是否满足"单进程、单会话"前提——如果不满足，Module Singleton 是否仍然适用
7. 从第 3 章开始，理解构建时 DCE 如何与模块级单例配合实现零成本特性隔离

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图2-1 | STATE 对象的 get/set 访问模式 | Mermaid flowchart | 调用方 → get/set 函数对 → STATE 对象（封装层） | 2.2 |
| 图2-2 | 模块级单例两个变体对比 | ASCII box-arrow | STATE（有状态）vs createSignal（无状态）的架构位置 | 2.4 |

## 写作顺序

1. ⛔ **确认完整 spec 与风格规则**：重新读取完整 spec 文件和 DESIGN.md 中的 STYLE_ID=hunter，确认全部 16 项 spec 要素已在 plan 中反映
2. 先写节 2.1（问题——1762 行状态模块的设计选择，逻辑起点）
3. 然后写节 2.2（STATE 对象与 get/set 函数对，依赖 2.1 建立的模式概念）
4. 写节 2.3（createSignal 变体，与 2.2 形成对比）
5. 写节 2.4（模式剖析——两个变体的统一框架）
6. 写节 2.5（适用范围表）和节 2.6（权衡与局限）
7. 写节 2.7（与已知模式的对话）
8. 写章末模式提炼和"你能做什么"
9. 写图表时先画 ASCII 草图，确认结构后再转 Mermaid
10. **最后补写开篇**（此时知道全章讲了什么，才能写出符合 hunter 风格的模式预告）

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
sed -n '45p' src/bootstrap/state.ts        # type State = {
sed -n '429p' src/bootstrap/state.ts       # const STATE: State = getInitialState()
sed -n '435,450p' src/bootstrap/state.ts   # regenerateSessionId
sed -n '500p' src/bootstrap/state.ts       # getOriginalCwd
sed -n '515p' src/bootstrap/state.ts       # setOriginalCwd
sed -n '846p' src/bootstrap/state.ts       # setMainLoopModelOverride
sed -n '1635p' src/bootstrap/state.ts      # setIsRemoteMode
sed -n '481p' src/bootstrap/state.ts       # sessionSwitched signal
sed -n '24p' src/bootstrap/state.ts        # createSignal import
cat src/utils/signal.ts                    # createSignal 完整实现
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`/Users/liuweiping/repos/claude-code-sourcemap/specs/ch02.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✅ | 章节意图 |
| 2 | 通用约束·必须 | ✅ | 通用约束 |
| 3 | 通用约束·禁止 | ✅ | 通用约束 |
| 4 | 风格专属必须 | ✅ | 风格专属写作要求 → 风格专属必须 |
| 5 | 风格专属禁止 | ✅ | 风格专属写作要求 → 风格专属禁止 |
| 6 | 章节结构 | ✅ | 章节骨架（hunter 模式预告→问题→实例1→实例2→剖析→适用范围→权衡→对话→你能做什么） |
| 7 | 源码引用格式 | ✅ | 章节骨架 → 每节锚点格式（src/相对路径:行号） |
| 8 | 图表格式 | ✅ | 图表清单（Mermaid + ASCII） |
| 9 | 边界路径 | ✅ | 通用约束 → 排除范围（3 项排除） |
| 10 | 验收优先级 | ✅ | Spec 验收场景清单（E>A>B=C>D） |
| 11 | 验收场景·A 源码锚点组 | ✅ | Spec 验收场景清单 A（4 项） |
| 12 | 验收场景·B 语言精确性组 | ✅ | Spec 验收场景清单 B（3 项） |
| 13 | 验收场景·C 结构完整性组 | ✅ | Spec 验收场景清单 C（6 项） |
| 14 | 验收场景·D 图表与代码组 | ✅ | Spec 验收场景清单 D（5 项） |
| 15 | 验收场景·E 风格符合性组 | ✅ | Spec 验收场景清单 E（5 项） |
| 16 | 排除范围 | ✅ | 通用约束 → 排除范围（3 项） |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

**⛔ 如有任何 ☐ 未勾选，不得提交 plan，必须修复后重新审查。**

全部 ✅ 已勾选。
