# 第 5 章写作计划

**批次**：批次 2（来自 plans/batch.md）
**章节**：ch05 — REPL 界面架构——5000 行组件的交互设计
**风格**：hunter

## 章节意图

拆解 src/screens/REPL.tsx（5005 行）的组件架构：基于 Ink（React for CLI）的消息虚拟化滚动、工具权限弹窗（PermissionRequest）、后台任务面板、远程会话（useRemoteSession / useDirectConnect / useSSHSession）和推测执行（Speculation）的设计。读者读完后能理解"单文件巨组件"在强内聚场景下的工程合理性，并在自己的 CLI UI 中复用 Ink 的关键模式。

## 通用约束

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 开篇不得直接引用源码路径或行号
- 不得出现"介绍了…"、"描述了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测
- 不展开 components/ 目录下 144 个组件的完整分析

**排除范围**：
- src/assistant/、src/ssh/、src/server/、src/proactive/（stub）
- src/buddy/、src/voice/（UI 细节）
- Ink 框架本身的源码分析（仅分析 REPL.tsx 对 Ink 的使用方式）

## ⛔ 风格专属写作要求

**开篇格式**：hunter 模式预告（150-200字）：问题场景 + 模式预告 + 价值承诺。⛔ 不得直接引用源码。描述"5000 行单文件组件，竟然比拆散成 50 个文件更易维护"的工程悖论，预告"强内聚巨组件（Mega-Component）"模式。

**分节组织**：
`## [模式预告开篇]` → `## 问题` → `## 源码实例 1：消息渲染与交互输入` → `## 源码实例 2：权限弹窗与后台任务` → `## 模式剖析：强内聚巨组件` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 模式提炼` → `## 你能做什么`

**章末结构**：模式提炼（3-5 个命名模式，五层结构），然后"你能做什么"（5-8 条行动建议）。

**风格专属必须**：
- 展示至少 2 处源码实例，证明"强内聚巨组件"模式的普遍性
- 有适用范围表（何时拆分、何时保持单文件）
- 包含与已知业界模式的对话（对比"微前端"等拆分模式）
- 模式有中文+英文命名
- 章节总长 2000-3000 字

**风格专属禁止**：
- 不只讲 REPL.tsx 的功能列表（无法证明是"模式"）
- 不忽略单文件巨组件的缺陷
- 不出现纯功能罗列而没有源码佐证

**通用写作铁律**：
- ⛔ 源码第一：每个断言附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块有引入段+解读段
- ⛔ 数据驱动：具体行数、具体组件名
- ⛔ 开篇铁律：开篇在初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/screens/REPL.tsx | 行60-65：远程会话 hooks 导入 | 50-80 |
| 2 | src/screens/REPL.tsx | 行54：PermissionRequest 导入 | 50-60 |
| 3 | src/screens/REPL.tsx | 行215：handleSpeculationAccept | 210-230 |
| 4 | src/screens/REPL.tsx | 行618-625：AppState selectors | 615-630 |
| 5 | src/state/AppStateStore.ts | AppState 类型定义 | 1-80 |
| 6 | src/services/PromptSuggestion/speculation.ts | 推测执行入口 | 1-50 |

验证文件存在：
```bash
ls src/screens/REPL.tsx src/state/AppStateStore.ts src/services/PromptSuggestion/speculation.ts
```

## Spec 验收场景清单

**验收优先级**：E > A > B = C > D

### A. 源码锚点组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效性 | 每处引用存在于实际文件 | 节 5.1-5.4 | ☐ |
| A2 | 推断有标注 | 推断标注「（推断）」 | 所有节 | ☐ |
| A3 | 不引用排除范围 | 无 src/assistant/ 等路径 | 写作前 checklist | ☐ |
| A4 | 不重复其他章内容 | 详见第 X 章代替重复 | 所有节 | ☐ |

### B. 语言精确性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 无"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 具体行数/组件名 | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现标注原文 | 全章 | ☐ |

### C. 结构完整性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | 每概念至少两层递进 | 节 5.1-5.4 | ☐ |
| C2 | ⛔ hunter 结构 | ≥2实例+适用范围表+与已知模式对话+中英文命名 | 节 5.2-5.3 | ☐ |
| C3 | 核心主张可提炼 | 可从开篇提炼1-2句答案 | 开篇节 | ☐ |
| C4 | 设计权衡有说明 | 有"选A不选B"说明 | 权衡与局限节 | ☐ |
| C5 | 章末行动建议 | 5-8条行动建议 | 你能做什么节 | ☐ |
| C6 | 章节间链接 | 章末向后链接+向前链接 | 全章 | ☐ |

### D. 图表与代码组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | ≥3个图表 | 图表清单 | ☐ |
| D2 | 图表标注格式 | **图5-X：标题** | 所有图表 | ☐ |
| D3 | 代码片段长度 | ≤25行，前后有说明 | 所有代码块 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号 | 节 5.1 | ☐ |
| D5 | ⛔ hunter 图表 | 架构关系图+适用场景决策图 | 图5-1, 5-3 | ☐ |

### E. 风格符合性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 150-200字，不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有预告和设问 | 全章 | ☐ |
| E3 | 关键信息突出 | 加粗+表格 | 全章 | ☐ |

## 章节骨架

### 开篇（最后补写）
> ⛔ hunter 模式预告：150-200字，问题场景+模式预告+价值承诺，不引源码
- 可选题词：可选用 Ink 文档中的引言

### 节 5.1：消息渲染与交互输入架构
- 核心主张：REPL.tsx 用 Ink 的 Box/Text 组件实现消息虚拟化列表 + PromptInput 的输入处理
- 源码锚点：`src/screens/REPL.tsx:54` — PermissionRequest 导入；`src/screens/REPL.tsx:618` — AppState selectors
- 概念层次：① Ink 组件树的结构 ② 消息列表的渲染策略 ③ AppState 与 React 状态的双轨
- 权衡分析：为什么用 Ink (React for CLI) 而非直接写 ANSI 转义序列？→ 声明式 UI + 自动 diff 更新，详见第 1 章
- 包含元素：ASCII 组件树（图5-1）+ 代码片段（≤20行）
- **⛔ 内容密度规划**：
  - 引入段：终端渲染不等于 console.log，Ink 把"终端当浏览器 DOM 用"的类比
  - 解读段：AppState 与 React Context 的双轨设计原因（SDK 模式和 headless 模式需要在 React 上下文外访问状态）
- 估计字数：600-800字（散文≥65%）
- 对应 spec 场景：A1、C1、D1

### 节 5.2：模式实例 1 — 权限弹窗的生命周期
- 核心主张：PermissionRequest 是一个阻塞流程的 modal 组件，通过 Promise + resolve 回调与 queryLoop 通信
- 源码锚点：`src/screens/REPL.tsx:54` — PermissionRequest 导入；`src/components/permissions/PermissionRequest.js`
- 概念层次：① 弹窗的触发（工具执行前）② 用户决策（一次/永久/拒绝）③ 结果回传到 queryLoop
- 权衡分析：为什么不用全局 store 而用 Promise？→ 保持工具执行的线性控制流
- 包含元素：Mermaid sequenceDiagram（权限请求时序，图5-2）
- **⛔ 内容密度规划**：
  - 引入段：CLI 中的"弹窗"设计挑战——终端不像浏览器有 DOM overlay，Ink 如何实现模态交互
  - 解读段：Promise-based 回调与 async generator 的配合——阻塞 queryLoop 直到用户响应
- 估计字数：500-700字（散文≥60%）
- 对应 spec 场景：A1、C2、D3

### 节 5.3：模式实例 2 — 推测执行（Speculation）
- 核心主张：REPL.tsx 在用户输入之前预先执行可能的操作，通过 handleSpeculationAccept 实现"接受或丢弃"
- 源码锚点：`src/screens/REPL.tsx:215` — handleSpeculationAccept；`src/services/PromptSuggestion/speculation.ts:1`
- 概念层次：① 推测执行的触发时机 ② 预测结果的暂存 ③ 用户确认后的合并或丢弃
- 权衡分析：推测执行的风险——如果预测错误，需要回滚；如何设计"安全可丢弃"的推测操作
- 包含元素：flowchart（推测执行状态机，图5-3）
- **⛔ 内容密度规划**：
  - 引入段（与实例1的区别）：实例1是"阻塞等待用户"，实例2是"不等用户先跑"——两种截然相反的交互策略，却服务于同一个目标：减少用户感知延迟
  - 解读段：推测执行是 speculative execution 在 CLI 场景的应用，类比 CPU 分支预测
- 估计字数：400-600字（散文≥65%）
- 对应 spec 场景：A1、C2

### 节 5.4：模式剖析——强内聚巨组件
- 核心主张：5005 行单文件是刻意的内聚决策，不是技术债——所有交互逻辑在同一个上下文中可见
- 包含元素：模式命名框（强内聚巨组件 Mega-Component，五层结构）+ 适用范围表
- 估计字数：400-500字
- 对应 spec 场景：C2、C3

### 节 5.5：权衡与局限
- 权衡：单文件可读性下降 vs 减少跨文件状态共享的认知负担
- 估计字数：200-300字
- 对应 spec 场景：C4

### 节 5.6：与已知模式的对话
- 对比：微前端拆分（Micro-Frontend）与 Mega-Component 的权衡；React 组件拆分原则的局限
- 估计字数：200-300字
- 对应 spec 场景：C2

### 模式提炼
- 模式 1：强内聚巨组件（Mega-Component）
- 模式 2：Promise 门控（Promise Gate）——阻塞异步流程等待 UI 事件
- 模式 3：乐观推测执行（Optimistic Speculation）

### 你能做什么（5-8 条）
1. 评估 CLI 组件的拆分边界：状态共享频率高的组件保持单文件
2. 用 Promise + resolve 回调实现 CLI 模态交互，而非全局状态轮询
3. 在 Agent CLI 中设计"可安全丢弃"的推测操作，降低预测失败的回滚成本
4. 用 AppState + useSyncExternalStore 实现 React 上下文外可访问的状态
5. 通过 Ink 的 Box/Text 组件替代手写 ANSI 转义序列，实现声明式 CLI UI
6. 为长列表消息实现虚拟滚动，避免终端输出过多行导致渲染卡顿

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图5-1 | REPL.tsx 组件架构 | ASCII box-arrow | 核心子组件树（PromptInput/MessageList/PermissionRequest/BackgroundTasks） | 5.1 |
| 图5-2 | 权限弹窗时序 | Mermaid sequenceDiagram | queryLoop→PermissionRequest→用户决策→resume | 5.2 |
| 图5-3 | 推测执行状态机 | Mermaid flowchart | 触发推测→暂存结果→用户确认→合并/丢弃 | 5.3 |

## 写作顺序

1. ⛔ 读取完整 spec（specs/ch05.spec）和 DESIGN.md STYLE_ID=hunter
2. 先写节 5.1（消息渲染架构）
3. 再写节 5.2（权限弹窗实例）
4. 写节 5.3（推测执行实例，明确说明与5.2的区别）
5. 写图表（先 ASCII 草图，再转 Mermaid）
6. 写节 5.4（模式剖析）+ 5.5（权衡）+ 5.6（对话）
7. 写模式提炼 + 你能做什么
8. 最后补写开篇

## 写作前 checklist

```bash
sed -n '50,70p' src/screens/REPL.tsx     # 关键 hooks/组件导入
sed -n '210,220p' src/screens/REPL.tsx   # handleSpeculationAccept
sed -n '615,630p' src/screens/REPL.tsx   # AppState selectors
ls src/services/PromptSuggestion/speculation.ts  # 推测执行入口
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：specs/ch05.spec

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束·通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束·通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属·必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属·禁止 |
| 6 | 章节结构 | ✓ | 章节骨架 |
| 7 | 源码引用格式 | ✓ | 各节·源码锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束·排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束·排除范围 |

**验收场景覆盖统计**：A(4/4) + B(3/3) + C(6/6) + D(5/5) + E(3/3) = **21/21**
