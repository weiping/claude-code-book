# 第 11 章写作计划

**批次**：批次 3a（来自 plans/batch.md）
**章节**：ch11 — 模型自动选择——Opus、Sonnet 与 Haiku 的路由逻辑
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

揭示 Claude Code 如何通过 `src/utils/model/model.ts` 中的函数集合实现三层模型路由：`getSmallFastModel()`（Haiku，高频轻量）、`getMainLoopModel()`（Sonnet，主力开发）、`getBestModel()`（Opus，深度推理），以及 `setMainLoopModelOverride` 和 `mainLoopModelForSession` 如何实现覆盖机制。读者读完后能在自己的多模型 Agent 系统中复用这一路由模式。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- effort/thinking 深度控制（第 12 章）
- 具体模型的 API 参数、定价、上下文窗口大小
- src/assistant/、src/ssh/、src/server/、src/proactive/（排除范围 stub）

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例 1` → `## 源码实例 2（变体）` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号
- 全章不得混用其他风格的写作手法

**通用写作铁律**：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字
- ⛔ 术语标准化：首次出现技术术语时标注原文
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/utils/model/model.ts | getSmallFastModel/getMainLoopModel/getBestModel 三函数 | 行 36-115 |
| 2 | src/utils/model/model.ts | getUserSpecifiedModelSetting 优先级注释（5层优先级） | 行 60-92 |
| 3 | src/bootstrap/state.ts | getMainLoopModelOverride/setMainLoopModelOverride | 行 838-870 |
| 4 | src/utils/model/model.ts | getDefaultOpusModel/getDefaultHaikuModel 环境变量覆盖 | 行 100-150 |

验证文件存在：
```bash
ls src/utils/model/model.ts src/bootstrap/state.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用实际存在 | 章节骨架各节 | ☐ |
| A2 | 推断有标注 | 推断有"（推断）"标注 | 全章 | ☐ |
| A3 | 不引用排除范围 | 没有 effort/thinking/stub 引用 | 排除范围声明 | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 12 章"代替 effort 内容 | 深度控制处 | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 用具体模型名（Haiku/Sonnet/Opus），不用"轻/重量级" | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 全章 | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|------------|------|
| C1 | 概念递进层次 | hunter: 问题→实例→模式剖析递进 | 节 11.1→6.2→6.3→6.4 | ☐ |
| C2 | hunter 专属结构 | 模式命名框（五层结构）存在 | 节 11.4 | ☐ |
| C3 | 核心主张可提炼 | 可提炼"分层模型路由"模式 | 章末 | ☐ |
| C4 | 设计权衡有说明 | 有覆盖机制的优先级分析 | 节 11.6 | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末 | ☐ |
| C6 | 章节间链接 | 向第 12 章链接 | 各节 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|------------|------|
| D1 | 图表可渲染且达标 | 三层路由对比表 + Mermaid 图 | 图表清单 | ☐ |
| D2 | 图表标注格式 | 上方有 `**图 N-X：标题**` | 图表清单 | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 源码实例节 | ☐ |
| D4 | 源码注释引用 | 先引优先级注释+行号，再展开 | 节 11.2 | ☐ |
| D5 | hunter 专属图表 | 适用范围表（✓/✗ 格式） | 节 11.5 | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|------------|------|
| E1 | hunter 开篇格式 | 150-200字，三要素，不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有设问句 | 全章 | ☐ |
| E3 | 关键信息突出 | 三层路由对比表格存在 | 节 11.1 | ☐ |

## 章节骨架

### 开篇（hunter 模式预告，最后补写）
- 问题场景：当 Agent 需要分类一个任务（用 Haiku）但需要深度推理另一个（用 Opus）——硬编码模型名会让这套逻辑散落在各处。我们在代码库里找到了一个反复出现的解决方案。
- 模式预告：**分层模型路由**（Tiered Model Router）——用函数封装模型选择逻辑，调用方只说"给我小模型"或"给我最好的模型"
- 价值承诺：读完本章，你能设计自己的多模型路由层，让 Agent 在成本和性能之间自动权衡
- ⛔ 禁止直接引用源码

### 节 11.1：问题——多模型 Agent 的路由困境
- 核心主张：硬编码模型名会在 Agent 系统中产生两个问题：无法切换模型版本，无法按任务复杂度动态选择
- 源码锚点：`src/utils/model/model.ts:36` — getSmallFastModel（Haiku 路由函数入口）
- 概念层次：
  - **层次1 定义**：三层路由——轻量/主力/最强，各对应不同任务复杂度
  - **层次2 设计意图**：调用方用语义（"我需要小模型"）而非具体名（"claude-haiku-3-5-xxx"）
- 权衡分析：函数封装 vs 常量定义——函数可以包含运行时逻辑（环境变量/覆盖），常量不能
- 包含元素：三层路由对比表

| 函数 | 对应模型层 | 典型场景 | 源码位置 |
|------|-----------|---------|---------|
| getSmallFastModel() | Haiku | 内容分类、简单填充 | model.ts:36 |
| getMainLoopModel() | Sonnet | 主循环推理 | model.ts:92 |
| getBestModel() | Opus | 复杂深度推理 | model.ts:100 |

- 估计字数：800-1000 字
- 对应 spec 场景：C1、D1（三层路由对比表）

### 节 11.2：源码实例 1——getMainLoopModel 的五层优先级
- 核心主张：`getMainLoopModel` 实现了一个 5 层优先级链：会话内覆盖 > 启动时 flag > 环境变量 > 用户设置 > 内置默认
- 源码锚点：
  - `src/utils/model/model.ts:64` — 优先级注释（5 层顺序）
  - `src/utils/model/model.ts:92` — getMainLoopModel 函数实现
  - `src/utils/model/model.ts:60` — getUserSpecifiedModelSetting（聚合前 4 层）
- 概念层次：
  - **层次1 实例**：getUserSpecifiedModelSetting 的级联 if 链
  - **层次2 模式特征**：每层都有明确的 fallthrough 语义——找到就返回，否则继续向下
- 权衡分析：为什么用函数而非 YAML 配置——运行时需要读取 env 和 state，配置文件无法动态
- 包含元素：getUserSpecifiedModelSetting 代码片段（≤20行）
- 估计字数：900-1100 字
- 对应 spec 场景：A1（源码引用）、D4（注释引用）

### 节 11.3：源码实例 2（变体）——覆盖机制的工程实现
- 核心主张：`setMainLoopModelOverride`（运行时覆盖）和 `getSmallFastModel`（环境变量覆盖）展示了同一模式在两个层次的实现
- 源码锚点：
  - `src/bootstrap/state.ts:838` — getMainLoopModelOverride
  - `src/bootstrap/state.ts:846` — setMainLoopModelOverride
  - `src/utils/model/model.ts:36` — `process.env.ANTHROPIC_SMALL_FAST_MODEL || getDefaultHaikuModel()`
- 概念层次：
  - **层次1 实例**：state.ts 中的会话级覆盖（/model 命令触发）
  - **层次2 对比**：环境变量覆盖（更低优先级）vs 会话覆盖（更高优先级）
- 权衡分析：为什么覆盖存储在 state.ts 而非 model.ts——覆盖是全局状态，model.ts 是纯函数
- 估计字数：800-1000 字
- 对应 spec 场景：A1、C2（hunter 专属结构）

### 节 11.4：模式剖析——分层模型路由（Tiered Model Router）
- 模式命名框：
  ```
  模式名称：分层模型路由（Tiered Model Router）
  问题：Agent 系统需要根据任务复杂度选择不同能力/成本的模型，硬编码模型名导致系统僵化
  解决方案：将模型选择封装为语义化函数（getSmallFast/getMainLoop/getBest），内部实现优先级链
  前置条件：系统使用多个不同能力的模型；调用方无需关心具体模型版本
  源码锚点：src/utils/model/model.ts:36-115
  ```
- 估计字数：600-800 字

### 节 11.5：适用范围

| 场景 | 适用 | 理由/替代方案 |
|------|------|------------|
| 系统有 2+ 个不同能力层的模型 | ✓ | 分层语义使切换模型版本只需改一处 |
| 任务有明确的轻/重分级 | ✓ | 分类/summarize 用轻量，推理/写作用重量 |
| 需要运行时覆盖模型选择 | ✓ | 覆盖机制可以注入到优先级链 |
| 只有一个模型 | ✗ | 直接使用常量即可，封装无意义 |
| 模型选择需要用户输入才能决定 | ✗ | 改用 Strategy 模式（用户选择注入） |
| 需要 A/B 测试不同模型 | ✗ | 改用特性开关 + 随机分流 |

- 估计字数：400-600 字

### 节 11.6：权衡与局限
- 适用边界：层数 ≤3 时清晰；层数 >5 时建议改用路由规则表（rule-based router）
- 潜在风险：优先级链文档化不足会导致覆盖行为难以预测
- 性能影响：env 读取在冷路径，memoize 可缓解
- 替代方案：rule-based router（根据 task type/complexity 查表选择模型）
- 估计字数：500-700 字

### 节 11.7：与已知模式的对话
- 对比：Chain of Responsibility（职责链）
- 相同点：优先级链，找到就停止
- 不同点：职责链中处理器可以"传递"；模型路由中每层是覆盖而非处理
- 另一对比：Strategy 模式——Router 是静态选择（基于优先级），Strategy 是动态注入（调用方决定）
- 估计字数：400-600 字

### 章末结构：模式提炼（hunter）

**模式 1：分层模型路由（Tiered Model Router）**
- 解决问题：多模型 Agent 需要按任务复杂度动态选择模型
- 核心做法：用语义化函数（getSmall/getMain/getBest）封装，内部实现优先级链
- 前置条件：有多个不同能力层的模型；任务有明确的复杂度分级
- 源码证据：`src/utils/model/model.ts:36-115`

**模式 2：优先级覆盖链（Priority Override Chain）**
- 解决问题：不同来源（flag/env/设置/默认）的配置需要明确的优先级
- 核心做法：cascade if 链，从高优先级向低优先级检查，找到就返回
- 前置条件：覆盖来源数量固定；优先级关系稳定
- 源码证据：`src/utils/model/model.ts:60-92`

**模式 3：全局状态隔离的运行时覆盖（Runtime Override via Global State）**
- 解决问题：运行时临时覆盖需要跨越模块边界但不影响持久配置
- 核心做法：覆盖值存入全局 state（非持久化），路由函数查询 state 而非直接读配置
- 前置条件：覆盖生命周期 = 会话生命周期
- 源码证据：`src/bootstrap/state.ts:838-870`

### 章末：你能做什么

1. 在自己的多模型系统中，用语义化函数封装模型选择（getSmall/getMain/getBest），而非散落各处的硬编码字符串
2. 实现 5 层优先级链：代码内覆盖 > 会话覆盖 > 环境变量 > 配置文件 > 默认值
3. 将"运行时模型覆盖"存入会话级全局状态，而非写回持久化配置
4. 设计覆盖机制时，在代码注释中明确写出优先级顺序（参考 model.ts:64 的注释格式）
5. 当模型层数超过 3 时，改用路由规则表（`{ taskType: model }` 查表）替代 if 链
6. 用环境变量作为模型覆盖的最低优先级，让测试环境可以不修改代码就替换模型

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图 6-1 | 三层模型路由对比表 | Markdown 表格 | Haiku/Sonnet/Opus 函数+场景+锚点对比 | 6.1 |
| 图 6-2 | getMainLoopModel 五层优先级链 | Mermaid flowchart | 从"会话覆盖"到"内置默认"的 5 层 cascade | 6.2 |

## 写作顺序

1. ⛔ 确认 STYLE_ID=hunter + 读取完整 spec（`specs/ch11.spec`）
2. 先写节 11.2（源码实例1，五层优先级链）
3. 再写节 11.3（源码实例2，覆盖机制两层对比）
4. 写节 11.1（问题节 + 三层路由对比表）
5. 写节 11.4-6.7
6. 画图表
7. 写章末模式提炼
8. 写"你能做什么"
9. 最后补写开篇

## 写作前 checklist

```bash
sed -n '36p' src/utils/model/model.ts    # getSmallFastModel
sed -n '92p' src/utils/model/model.ts    # getMainLoopModel
sed -n '100p' src/utils/model/model.ts   # getBestModel
sed -n '64p' src/utils/model/model.ts    # 优先级注释
sed -n '838p' src/bootstrap/state.ts     # getMainLoopModelOverride
sed -n '846p' src/bootstrap/state.ts     # setMainLoopModelOverride
```

## ⛔ Spec 一致性声明

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束 |
| 3 | 通用约束·禁止 | ✓ | 通用约束 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求 |
| 6 | 章节结构 | ✓ | 章节骨架 |
| 7 | 源码引用格式 | ✓ | 各节锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单头部 |
| 11 | 验收场景·A | ✓ | A 组表格 |
| 12 | 验收场景·B | ✓ | B 组表格 |
| 13 | 验收场景·C | ✓ | C 组表格 |
| 14 | 验收场景·D | ✓ | D 组表格 |
| 15 | 验收场景·E | ✓ | E 组表格 |
| 16 | 排除范围 | ✓ | 通用约束→排除范围 |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 3/3 = **21/21**
