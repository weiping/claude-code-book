# 第 1 章写作计划

**批次**：批次 1a（来自 plans/batch.md）
**章节**：ch01 — 系统总体架构与技术栈——Claude Code 的结构地图
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

为全书提供导航地图：读者读完后应能说清 Claude Code 的三层架构（交互层、Harness 控制层、模型层）由哪些子系统组成、各子系统如何连接、代码库的物理组织逻辑是什么，以及 Bun/TypeScript/Ink 等技术选型各自解决了什么工程问题。不是功能介绍，而是让读者在深入任何单一子系统之前，先建立可定位的全局坐标系。对应 DESIGN.md 一句话主张：建立 Claude Code 完整架构认知——三层架构与十大子系统职责边界、技术选型动机、源码组织逻辑。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**（从 spec 约束·必须 前 4 条提取）：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**（从 spec 约束·禁止 前 4 条提取）：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- src/assistant/（Assistant 模式，stub 未实现）
- src/ssh/（SSH 功能，stub 未实现）
- src/server/（服务端组件，stub 未实现）
- src/proactive/（主动建议，stub 未实现）
- src/buddy/、src/voice/（UI 细节，与 Harness 主题关联弱）
- src/components/ 渲染细节（144 个 Ink 组件的 UI 实现）
- TypeScript 语法讲解、React 基础、Bun 运行时基础
- 功能使用教程

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例1` → `## 源码实例2` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号（开篇是问题场景+模式预告+价值承诺，150-200字）
- 全章不得混用其他风格的写作手法

**通用写作铁律**（所有风格通用，写作期间必须遵守）：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据，禁止基于 AI 记忆写作
- ⛔ 代码无裸露：禁止"标题→代码→下一个标题"，每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文（中文（English Full Name, ABBR））
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写，禁止在开篇直接引用源码

## 前置阅读清单
（按阅读顺序排列，不超过 6 个文件）

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/main.tsx | 启动序列（profileCheckpoint、feature()、Commander.js、launchRepl） | 行 1-100, 580-860 |
| 2 | src/Tool.ts | Tool 接口定义 + buildTool 工厂函数 | 行 362-433, 783-792 |
| 3 | src/bootstrap/state.ts | 全局状态管理函数（getOriginalCwd 等） | 行 500, 846-848 |
| 4 | src/Task.ts | TaskType 定义（7 种任务类型） | 行 6-20 |
| 5 | src/QueryEngine.ts | QueryEngine 类定义 | 行 184-250 |
| 6 | src/entrypoints/sdk/coreTypes.ts | HOOK_EVENTS 数组（27 个 hook 事件） | 行 25-53 |

验证文件存在：
```bash
ls src/main.tsx src/Tool.ts src/bootstrap/state.ts src/Task.ts src/QueryEngine.ts src/entrypoints/sdk/coreTypes.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用应存在于实际文件中 | 节 1.1-1.7（骨架每节均有锚点） | ☐ |
| A2 | 引用排除范围时报错 | 不引用 src/assistant/、src/ssh/、src/server/、src/proactive/ | 骨架全部节（已避开排除范围） | ☐ |
| A3 | 不引用排除范围 | 没有任何引用落在排除范围中 | 骨架全部节 | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 M 章"代替重复 | 节 1.1（bootstrap 用"详见第2章"）、节 1.1（feature() 用"详见第3章"） | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全章 | ☐ |
| B2 | 数字具体化 | 使用精确数字（如"58 个工具目录"、"4683 行 main.tsx"、"27 个 hook 事件"） | 节 1.2（目录统计）、节 1.3（技术栈数字） | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注（中文（English, ABBR）） | 节 1.1-1.7（每个术语首次出现时标注） | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）全景图存在 | 包含一个图表展示十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）连接关系 | 节 1.2（Mermaid 架构图） | ☐ |
| C2 | ⛔ 风格专属结构 | hunter：模式预告开篇→问题→源码实例→模式剖析→适用范围→权衡与局限→与已知模式对话→你能做什么 | 骨架整体结构与 spec 一致 | ☐ |
| C3 | 每个子系统有向后引用 | "深入分析见第 N 章"格式 | 节 1.4-1.6（每个子系统介绍后附引用） | ☐ |
| C4 | 技术选型权衡说明 | Bun/TypeScript/Ink 每项有"选 A 不选 B"理由 | 节 1.3（技术选型权衡） | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末"你能做什么" | ☐ |
| C6 | 章节间链接 | 章末向后链接 + 章节内向前链接 | 节 1.4-1.6 + 章末 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表数量达标 | ≥3 个图表（ASCII 架构图 + Mermaid 启动序列 + 目录结构示意） | 节 1.2（架构图）、节 1.1（启动序列）、节 1.2（目录结构） | ☐ |
| D2 | 图表标注格式 | 上方 `**图 1-X：标题**`，下方说明文字 | 节 1.1-1.2（每个图表） | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 节 1.1（启动代码片段）、节 1.4（buildTool 代码片段） | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 节 1.1（profileCheckpoint 注释）、节 1.4（buildTool 注释） | ☐ |
| D5 | ⛔ 风格专属图表 | hunter：适用范围表 + 模式命名框 | 节 1.7（适用范围表）、章末（模式命名框） | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 开篇不引用源码，含三要素：问题场景+模式预告+价值承诺 | 开篇（最后补写） | ☐ |
| E2 | 多实例证明模式普遍性 | ≥2 处不同位置的源码实例，说明关键区别 | 节 1.4（buildTool 实例1）、节 1.4（TaskType 实例2） | ☐ |
| E3 | 模式命名框格式规范 | 模式名称+问题+解决方案+源码锚点 | 章末模式提炼 | ☐ |
| E4 | 读者对话感 | "我们"而非"用户"，有设问和预告 | 全章 | ☐ |
| E5 | 关键信息突出 | 加粗标注，表格展示对比 | 节 1.3（技术选型对比表）、节 1.2（子系统表） | ☐ |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

## 章节骨架

### 开篇（hunter 模式预告）
> ⛔ 开篇禁止直接引用源码。在全章初稿完成后最后补写。

- 开篇公式：问题场景（"在一个拥有 2,000 个源码文件、4683 行入口文件的代码库中，如何快速建立架构认知？"）+ 模式预告（"分层注册表（Layered Registry）模式——Claude Code 反复使用同一模式管理工具、命令、Hook 和 Task"）+ 价值承诺（"读完本章，我们将获得一张完整的架构导航图和一种识别复杂系统结构的方法"）
- 目标：150-200 字
- 可选题词："The architecture of a large system is the structure of the simpler systems that compose it."（推断格言，非源码引用）

### 节 1.1：问题——如何在一个 50 万行代码库中建立全局坐标系？
- 核心主张：Claude Code 的架构不是一个自顶向下设计的系统，而是一个 CLI 工具在功能压力下被迫演化的结果——"分层注册表"是贯穿全局的组织模式（对应验收场景 E1、C2）
- 源码锚点：`src/main.tsx:12` — `profileCheckpoint('main_tsx_entry');`（已验证存在）
- 概念层次：设计意图（为什么入口文件同时做启动优化和导入）→ 实际案例（启动序列）
- 权衡分析：为什么把启动检查点放在入口文件最顶部而不是统一的 bootstrap 函数中？——因为在模块加载阶段就需要标记时间戳，bootstrap 函数此时还未执行
- 包含元素：代码片段（profileCheckpoint + startKeychainPrefetch 特性门控，≤15行）+ Mermaid 时序图（启动序列：profileCheckpoint → startMdmRawRead → startKeychainPrefetch → Commander.js 解析 → launchRepl）
- **⛔ 内容密度规划**：
  - 引入段要点：说明"入口文件是理解架构的第一个线索"，预告将看到启动时序中的分层模式
  - 解读段要点：解释每个 profileCheckpoint 的工程意图（性能度量），以及 feature('KAIROS') 如何实现零成本的特性隔离
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1200-1500 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch01_source_anchors、ch01_concrete_numbers
- 向后引用：bootstrap 详细实现"详见第 2 章"，feature() 机制"详见第 3 章"

### 节 1.2：源码实例 1——十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）全景图与目录约定
- 核心主张：Claude Code 的 37 个顶层目录映射到 三层架构（交互层/Harness/模型层）下的子系统，它们通过"目录即注册表"约定组织——每个目录下的文件自动注册为该子系统的一部分（对应验收场景 C1、C3）
- 源码锚点：
  - `src/Tool.ts:783` — `export function buildTool<D extends AnyToolDef>(def: D): BuiltTool<D>`（已验证）
  - `src/Task.ts:6-13` — TaskType 7 种类型定义（已验证）
  - `src/QueryEngine.ts:184` — `export class QueryEngine {`（已验证）
  - `src/entrypoints/sdk/coreTypes.ts:25-53` — HOOK_EVENTS 27 个事件（已验证）
  - `src/types/hooks.ts:5-6` — HookEvent 类型导入（已验证）
- 概念层次：定义（每个子系统是什么）→ 设计意图（为什么这样分）→ 实际案例（工具目录中的 58 个工具）
- 权衡分析：为什么用目录约定而非显式注册表？——降低新增功能的门槛（只需创建目录和文件），但牺牲了编译期完整性检查
- 包含元素：
  - **Mermaid 架构图**：十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）（REPL/UI、查询引擎、Tool 系统、Hook 引擎、Task 系统、Swarm、上下文工程）的连接关系
  - **ASCII 目录结构图**：展示 src/ 下的 37 个目录如何分组到 三层架构与十大子系统
  - **表格**：子系统 | 目录 | 核心文件 | 章节引用
- **⛔ 内容密度规划**：
  - 引入段要点：预告"目录结构就是架构文档"，展示如何从目录名推断子系统职责
  - 解读段要点：逐个子系统解释职责边界，每个子系统附向后引用到详细章节
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1500-2000 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch01_subsystem_diagram、ch01_forward_links、ch01_concrete_numbers
- 向后引用：Tool 系统"详见第 14 章"，Hook 引擎"详见第 24 章"，Task 系统"详见第 29 章"，Swarm"详见第 33 章"

### 节 1.3：源码实例 2——技术选型的三重约束
- 核心主张：Bun/TypeScript/Ink 三项技术选型分别解决了"启动延迟"、"类型安全 + 生态兼容"和"终端 UI 表达力"三个正交约束——每一项选型都排除了替代方案（对应验收场景 ch01_tech_tradeoffs）
- 源码锚点：
  - `src/main.tsx:21` — `import { feature } from 'bun:bundle';`（已验证，证明 Bun 特定 API 使用）
  - `src/main.tsx:80` — `const assistantModule = feature('KAIROS') ? require(...) : null;`（已验证，DCE 用法）
  - `src/Tool.ts:783-792` — buildTool 返回值展开（TypeScript 类型推导）
- 概念层次：设计意图（为什么选 A 不选 B）→ 实际案例（每项选型在源码中的证据）
- 权衡分析：
  - Bun vs Node：Bun 提供 `bun:bundle` 的构建时 DCE 和原生 TypeScript 执行，代价是锁定 Bun 生态
  - TypeScript vs 纯 JS：类型安全使 buildTool 工厂对 58+ 工具的类型一致性提供编译保证，代价是类型复杂度
  - Ink vs blessed/ink-ui：Ink 用 React 组件模型渲染终端 UI（144 个组件），代价是引入 React 运行时
- 包含元素：
  - **对比表格**：选型维度 | Bun | Node（替代方案）| 决策理由
  - 代码片段：feature('KAIROS') 的 DCE 用法（≤10 行）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"技术选型不是随意选择，每一项都对应一个被放弃的替代方案"
  - 解读段要点：分析每项选型的"选 A 不选 B"逻辑，用源码证据支撑
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1000-1300 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch01_tech_tradeoffs、ch01_source_anchors

### 节 1.4：模式剖析——分层注册表模式的四个变体
- 核心主张："目录即注册表"模式在 Claude Code 中有四个变体——Tool（buildTool 工厂 + 目录约定）、Command（目录约定 + feature flag 门控）、Hook（HOOK_EVENTS 常量数组 + AsyncHookRegistry）、Task（TaskType 联合类型 + 目录约定）——它们共享同一模式骨架，但注册机制各不相同（对应验收场景 ch01_multi_instance_proof、E2）
- 源码锚点：
  - `src/Tool.ts:783-792` — buildTool 工厂（Tool 注册机制，已验证）
  - `src/Task.ts:6-13` — TaskType 7 种类型（Task 注册机制，已验证）
  - `src/entrypoints/sdk/coreTypes.ts:25-53` — HOOK_EVENTS 27 个事件（Hook 注册机制，已验证）
  - `src/utils/hooks/AsyncHookRegistry.ts:1-3` — AsyncHookRegistry 导入 HookEvent（已验证）
- 概念层次：定义（模式是什么）→ 模式变体对比 → 每个变体的关键区别
- 权衡分析：统一注册表 vs 分散注册表——Claude Code 选择分散（每个子系统独立注册），降低了耦合但增加了学习成本
- 包含元素：
  - **对比表格**：子系统 | 注册机制 | 入口类型 | 实例数量 | 章节引用
  - 代码片段：buildTool 工厂代码 + TaskType 类型定义（各 ≤10 行）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"四个子系统用了同一个模式，但注册方式不同——这是理解 Claude Code 架构的关键"
  - 解读段要点：逐个分析四个变体的注册机制差异，解释为什么同一个模式演化出不同变体
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1200-1500 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch01_multi_instance_proof、ch01_pattern_box

### 节 1.5：适用范围——何时用/不用分层注册表
- 核心主张：分层注册表适合"功能种类多、独立性强、需要动态扩展"的场景，不适合"功能间强依赖、需要严格编译检查"的场景（对应验收场景 ch01_applicability_table）
- 源码锚点：引用节 1.4 的四个变体作为实例
- 概念层次：适用场景 → 不适用场景 → 替代方案
- 权衡分析：N/A（本节本身就是权衡分析）
- 包含元素：
  - **适用范围表**：场景 | 适用(✓/✗) | 理由 | 替代方案
- **⛔ 内容密度规划**：
  - 引入段要点：预告"模式不是银弹——知道什么时候不用同样重要"
  - 解读段要点：表格中的每行附带具体理由
  - 散文与代码比例目标：全散文
- 估计字数：400-600 字
- 对应 spec 场景：ch01_applicability_table

### 节 1.6：权衡与局限——注册表模式的代价
- 核心主张：分层注册表牺牲了编译期完整性检查，Claude Code 用 TypeScript 类型推导（buildTool）和 Zod 运行时验证来弥补——但仍有盲区（对应验收场景 ch01_tradeoffs_and_limits）
- 源码锚点：`src/Tool.ts:783` — buildTool 工厂的 `as BuiltTool<D>` 类型断言（已验证）
- 概念层次：模式代价 → 补偿机制 → 残留盲区
- 权衡分析：本节分析注册表模式本身的权衡
- 包含元素：无代码（纯分析散文）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"每个模式都有代价，关键是如何补偿"
  - 解读段要点：分析 buildTool 的 `as` 类型断言——它意味着类型安全依赖开发者纪律而非编译器强制
  - 散文与代码比例目标：全散文
- 估计字数：500-700 字
- 对应 spec 场景：ch01_tradeoffs_and_limits

### 节 1.7：与已知模式的对话——分层注册表 vs Plugin Pattern vs Service Locator
- 核心主张：Claude Code 的分层注册表与 GoF 的 Plugin Pattern（插件模式）和 Martin Fowler 的 Service Locator（服务定位器）有共同祖先——"通过间接层解耦使用者与具体实现"——但分层注册表多了一个约束：注册发生在目录级别而非代码级别（对应验收场景 ch01_known_pattern_dialogue）
- 源码锚点：引用节 1.4 的四个变体
- 概念层次：已知模式描述 → 共同点 → 不同点 → 演化动机
- 权衡分析：N/A
- 包含元素：
  - **对比表格**：模式 | 注册方式 | 发现机制 | 编译检查 | Claude Code 中的实例
- **⛔ 内容密度规划**：
  - 引入段要点：预告"这个模式在经典设计模式中有亲戚"
  - 解读段要点：对比 Plugin Pattern（代码级注册）vs Service Locator（运行时查找）vs 分层注册表（目录级注册）
  - 散文与代码比例目标：全散文
- 估计字数：600-800 字
- 对应 spec 场景：ch01_known_pattern_dialogue

### 章末：模式提炼（hunter 风格）
- 核心模式：**分层注册表（Layered Registry）**
  - 模式名称：分层注册表（Layered Registry）
  - 解决的问题：在功能种类多、独立性强的大型系统中，如何让新功能的加入不需要修改核心代码？
  - 核心做法：每个功能子系统用目录约定作为隐式注册表，配合工厂函数或联合类型做类型收窄
  - 前置条件：子系统间松耦合、功能可按目录独立组织
  - 源码证据：`src/Tool.ts:783`（buildTool）、`src/Task.ts:6-13`（TaskType）、`src/entrypoints/sdk/coreTypes.ts:25-53`（HOOK_EVENTS）

### 章末：你能做什么
预规划 5-8 条行动建议（写作前列出方向，写作后补全细节）：
1. 用 `ls -d src/*/ | wc -l` 快速统计项目的子系统数量，验证你的架构直觉
2. 选取你最感兴趣的子系统目录（如 `src/tools/`），用 `ls` 列出其中的文件数量，与本章提到的数字交叉验证
3. 在自己的 Agent 项目中，尝试用"目录即注册表"约定组织功能模块，对比显式注册表的维护成本
4. 追踪 `src/main.tsx:12` 的 profileCheckpoint 到 `launchRepl`，画出你自己理解的启动序列图
5. 对比 Claude Code 的分层注册表与你项目中的注册/发现机制，写下三处异同
6. 阅读 `src/Tool.ts:783-792` 的 buildTool 实现，思考 `as BuiltTool<D>` 类型断言在你项目中是否可接受
7. 用 `grep -rn "HookEvent" src/ | wc -l` 感受 Hook 系统在代码库中的渗透程度
8. 从第 2 章开始，按篇章顺序深入每个子系统的实现细节

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图1-1 | Claude Code 启动序列 | Mermaid sequenceDiagram | profileCheckpoint → startMdmRawRead → startKeychainPrefetch → imports → Commander.js → launchRepl | 1.1 |
| 图1-2 | 十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）连接关系 | Mermaid flowchart | REPL/UI → 查询引擎 → Tool/Hook/Task/Swarm → 上下文工程 | 1.2 |
| 图1-3 | 源码目录到子系统映射 | ASCII box-arrow | 37 个 src/ 子目录按功能分组到 三层架构与十大子系统 | 1.2 |
| 图1-4 | 分层注册表四变体对比 | 表格 | 子系统/注册机制/入口类型/实例数量 | 1.4 |

## 写作顺序

1. ⛔ **确认完整 spec 与风格规则**：重新读取完整 spec 文件和 DESIGN.md 中的 STYLE_ID=hunter，确认全部 16 项 spec 要素已在 plan 中反映
2. 先写节 1.1（问题——入口文件与启动序列，逻辑起点）
3. 然后写节 1.2（十大子系统（交互层4个 + Harness控制层十大 + 模型层3个 = 三层架构全景）全景图，依赖 1.1 建立的入口文件认知）
4. 写节 1.3（技术选型，依赖 1.2 建立的子系统划分）
5. 写节 1.4（模式剖析——四变体对比，依赖 1.1-1.3 建立的具体案例）
6. 写节 1.5（适用范围表）和节 1.6（权衡与局限）
7. 写节 1.7（与已知模式的对话）
8. 写章末模式提炼和"你能做什么"
9. 写图表时先画 ASCII 草图，确认结构后再转 Mermaid
10. **最后补写开篇**（此时知道全章讲了什么，才能写出符合 hunter 风格的模式预告）

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
sed -n '12p' src/main.tsx        # profileCheckpoint
sed -n '20p' src/main.tsx        # startKeychainPrefetch
sed -n '21p' src/main.tsx        # feature import
sed -n '80p' src/main.tsx        # KAIROS feature flag
sed -n '209p' src/main.tsx       # profileCheckpoint imports_loaded
sed -n '586p' src/main.tsx       # profileCheckpoint function_start
sed -n '783,792p' src/Tool.ts    # buildTool factory
sed -n '6,13p' src/Task.ts       # TaskType
sed -n '184p' src/QueryEngine.ts # QueryEngine class
sed -n '25,53p' src/entrypoints/sdk/coreTypes.ts  # HOOK_EVENTS
sed -n '1,3p' src/utils/hooks/AsyncHookRegistry.ts # AsyncHookRegistry imports
sed -n '5,6p' src/types/hooks.ts # HookEvent import
sed -n '500p' src/bootstrap/state.ts # getOriginalCwd
sed -n '846,848p' src/bootstrap/state.ts # setMainLoopModelOverride
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`/Users/liuweiping/repos/claude-code-sourcemap/specs/ch01.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✅ | 章节意图 |
| 2 | 通用约束·必须 | ✅ | 通用约束 |
| 3 | 通用约束·禁止 | ✅ | 通用约束 |
| 4 | 风格专属必须 | ✅ | 风格专属写作要求 → 风格专属必须 |
| 5 | 风格专属禁止 | ✅ | 风格专属写作要求 → 风格专属禁止 |
| 6 | 章节结构 | ✅ | 章节骨架（hunter 模式预告→问题→实例1→实例2→剖析→适用范围→权衡→对话→你能做什么） |
| 7 | 源码引用格式 | ✅ | 章节骨架 → 每节锚点格式（src/相对路径:行号） |
| 8 | 图表格式 | ✅ | 图表清单（Mermaid + ASCII + 表格） |
| 9 | 边界路径 | ✅ | 通用约束 → 排除范围（7 项排除） |
| 10 | 验收优先级 | ✅ | Spec 验收场景清单（E>A>B=C>D） |
| 11 | 验收场景·A 源码锚点组 | ✅ | Spec 验收场景清单 A（4 项） |
| 12 | 验收场景·B 语言精确性组 | ✅ | Spec 验收场景清单 B（3 项） |
| 13 | 验收场景·C 结构完整性组 | ✅ | Spec 验收场景清单 C（6 项） |
| 14 | 验收场景·D 图表与代码组 | ✅ | Spec 验收场景清单 D（5 项） |
| 15 | 验收场景·E 风格符合性组 | ✅ | Spec 验收场景清单 E（5 项） |
| 16 | 排除范围 | ✅ | 通用约束 → 排除范围（8 项） |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

**⛔ 如有任何 ☐ 未勾选，不得提交 plan，必须修复后重新审查。**

全部 ✅ 已勾选。
