# 第 8 章写作计划

**批次**：批次 2（来自 plans/batch.md）
**章节**：ch08 — IDE 集成——Bridge 协议与 LSP 服务
**风格**：hunter

## 章节意图

分析 Claude Code 如何通过 Bridge 协议（src/bridge/，32 个文件）桥接 VS Code 等 IDE，通过 LSP 服务（src/services/lsp/，7 个文件）提供语言服务协议支持。读者读完后能理解 Bridge 模式如何让同一个 Agent 同时服务终端和 IDE 两种交互界面，并能在自己的 Agent 项目中复用这一"双界面桥接"模式。

## 通用约束

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 开篇不得直接引用源码路径或行号
- 不得出现"介绍了…"、"描述了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测
- 不深入 Bridge 的每个文件（架构概览，不是逐文件分析）

**排除范围**：
- src/assistant/、src/ssh/、src/server/、src/proactive/（stub）
- src/buddy/、src/voice/（UI 细节）
- LSP 协议本身的规范讲解（仅分析 Claude Code 如何使用 LSP）

## ⛔ 风格专属写作要求

**开篇格式**：hunter 模式预告（150-200字）：问题场景 + 模式预告 + 价值承诺。⛔ 不得直接引用源码。描述"同一个 Agent 如何同时服务终端用户和 IDE 用户"的挑战，预告"双界面桥接（Dual-Interface Bridge）"模式。

**分节组织**：
`## [模式预告开篇]` → `## 问题` → `## 源码实例 1：Bridge 主循环` → `## 源码实例 2：LSP 服务集成` → `## 模式剖析：双界面桥接` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 模式提炼` → `## 你能做什么`

**章末结构**：模式提炼（3-5 个命名模式，五层结构）+ 你能做什么（5-8 条行动建议）。

**风格专属必须**：
- 展示至少 2 处源码实例，证明模式的普遍性
- 有适用范围表
- 包含与已知业界模式的对话（对比 IPC、LSP、Bridge 模式）
- 模式有中文+英文命名
- 章节总长 2000-3000 字

**风格专属禁止**：
- 不只展示一个例子
- 不忽略 Bridge 通信的缺陷（延迟、双向同步）
- 不出现纯架构描述而没有源码佐证

**通用写作铁律**：
- ⛔ 源码第一：每个断言附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块有引入段+解读段
- ⛔ 数据驱动：具体文件数（32个bridge文件，7个lsp文件）
- ⛔ 开篇铁律：开篇在初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/bridge/bridgeMain.ts | runBridgeLoop() 函数，行141起 | 141-200 |
| 2 | src/bridge/bridgeMain.ts | BackoffConfig 类型，行59 | 59-100 |
| 3 | src/bridge/bridgeEnabled.ts | Bridge 是否启用的判断逻辑 | 1-50 |
| 4 | src/bridge/bridgeMessaging.ts | 消息格式与通信协议 | 1-60 |
| 5 | src/services/lsp/LSPServerManager.ts | LSP 服务器管理 | 1-60 |
| 6 | src/services/lsp/LSPClient.ts | LSP 客户端封装 | 1-50 |

验证文件存在：
```bash
ls src/bridge/bridgeMain.ts src/bridge/bridgeEnabled.ts src/services/lsp/LSPServerManager.ts
```

## Spec 验收场景清单

**验收优先级**：E > A > B = C > D

### A. 源码锚点组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效性 | 每处引用存在于实际文件 | 节 8.1-8.4 | ☐ |
| A2 | 推断有标注 | 推断标注「（推断）」 | 所有节 | ☐ |
| A3 | 不引用排除范围 | 无排除范围路径 | 写作前 checklist | ☐ |
| A4 | 不重复其他章内容 | 详见第 X 章 | 所有节 | ☐ |

### B. 语言精确性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 无"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 具体文件数/函数名 | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现标注原文 | 全章 | ☐ |

### C. 结构完整性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | 每概念至少两层递进 | 节 8.1-8.4 | ☐ |
| C2 | ⛔ hunter 结构 | ≥2实例+适用范围表+与已知模式对话+中英文命名 | 节 8.2-8.3 | ☐ |
| C3 | 核心主张可提炼 | 可从开篇提炼1-2句答案 | 开篇节 | ☐ |
| C4 | 设计权衡有说明 | 有"选A不选B"说明 | 权衡与局限节 | ☐ |
| C5 | 章末行动建议 | 5-8条行动建议 | 你能做什么节 | ☐ |
| C6 | 章节间链接 | 章末向后链接+向前链接 | 全章 | ☐ |

### D. 图表与代码组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | ≥3个图表 | 图表清单 | ☐ |
| D2 | 图表标注格式 | **图8-X：标题** | 所有图表 | ☐ |
| D3 | 代码片段长度 | ≤25行，前后有说明 | 所有代码块 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号 | 节 8.1 | ☐ |
| D5 | ⛔ hunter 图表 | 架构关系图+适用场景决策图 | 图8-1, 8-3 | ☐ |

### E. 风格符合性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 150-200字，不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有预告和设问 | 全章 | ☐ |
| E3 | 关键信息突出 | 加粗+表格 | 全章 | ☐ |

## 章节骨架

### 开篇（最后补写）
> ⛔ hunter 模式预告：150-200字，问题场景+模式预告+价值承诺，不引源码

### 节 8.1：Bridge 主循环——Agent 与 IDE 的通信基础
- 核心主张：runBridgeLoop() 是一个带指数退避的持久连接循环，通过 UDS/WebSocket 桥接 IDE 和 Agent
- 源码锚点：`src/bridge/bridgeMain.ts:141` — runBridgeLoop 函数；`src/bridge/bridgeMain.ts:59` — BackoffConfig 类型
- 概念层次：① Bridge 的连接模型 ② 消息格式 ③ 指数退避重连策略
- 权衡分析：为什么用 UDS（Unix Domain Socket）而非 HTTP？→ 本机通信延迟更低，无跨网络鉴权开销
- 包含元素：ASCII 架构图（终端 + IDE 双通道，图8-1）+ 代码片段（bridgeMain.ts:141-165，≤25行）
- **⛔ 内容密度规划**：
  - 引入段：同一个进程如何同时响应终端用户的 Ink 界面和 VS Code 的 Bridge 消息？
  - 解读段：bridgeMain.ts 的注释说明了 Bridge 与 REPL 的关系——Bridge 不是替代 REPL，而是并行运行
- 估计字数：600-800字（散文≥65%）
- 对应 spec 场景：A1、C1、D3、D4

### 节 8.2：模式实例 1 — Bridge 权限回调
- 核心主张：bridgePermissionCallbacks.ts 把 IDE 端的权限决策路由到 Agent 的权限系统，形成跨进程权限代理
- 源码锚点：`src/bridge/bridgePermissionCallbacks.ts` — 权限回调实现
- 概念层次：① 权限请求从 Agent 流向 IDE ② IDE 用户决策 ③ 结果回传 Agent
- 权衡分析：为什么不在 Bridge 端直接处理权限？→ 权限决策必须在 Agent 进程内执行，IDE 只是展示层
- 包含元素：Mermaid sequenceDiagram（Bridge 权限代理时序，图8-2）
- **⛔ 内容密度规划**：
  - 引入段：在 IDE 中，用户确认权限的 UI 在 VS Code 端，但真正的权限检查在 Agent 端——这两个进程如何协同？
  - 解读段：这与第 15 章的 PermissionMode 形成完整闭环——详见第 15 章（写作时添加交叉引用）
- 估计字数：500-600字（散文≥60%）
- 对应 spec 场景：A1、C2、D3

### 节 8.3：模式实例 2 — LSP 服务集成
- 核心主张：LSPServerManager 管理语言服务器的生命周期，LSPClient 通过标准 JSON-RPC 协议与编辑器通信
- 源码锚点：`src/services/lsp/LSPServerManager.ts:1` — 服务器管理；`src/services/lsp/LSPClient.ts:1` — 客户端封装
- 概念层次：① LSP 协议简介（JSON-RPC over stdio）② Claude Code 如何复用 LSP 获取诊断信息 ③ 与 Bridge 的关系
- 权衡分析：为什么使用标准 LSP 而非自定义协议？→ 复用所有支持 LSP 的编辑器
- 包含元素：flowchart（LSP 集成数据流，图8-3）
- **⛔ 内容密度规划**：
  - 引入段（与实例1的区别）：实例1是 Agent→IDE 的自定义 Bridge 协议，实例2是 Agent→Language Server 的标准 LSP 协议——两种不同的双向通信机制
  - 解读段：LSP 给 Claude Code 带来了什么？主要是代码诊断（错误/警告）和文件跳转能力，让 Agent 能感知代码质量
- 估计字数：400-600字（散文≥65%）
- 对应 spec 场景：A1、C2、D3

### 节 8.4：模式剖析——双界面桥接
- 核心主张：Bridge + LSP 形成"双界面桥接"模式：同一 Agent 进程同时暴露 TUI 和 IDE 两个界面
- 包含元素：模式命名框（双界面桥接 Dual-Interface Bridge）+ 适用范围表
- 估计字数：400-500字
- 对应 spec 场景：C2、C3

### 节 8.5：权衡与局限
- 权衡：Bridge 增加了进程复杂度，调试时需要追踪两条消息路径
- 估计字数：200-300字
- 对应 spec 场景：C4

### 节 8.6：与已知模式的对话
- 对比：GoF Bridge 模式（抽象与实现分离）；IPC（进程间通信）的多种方案
- 估计字数：200-300字
- 对应 spec 场景：C2

### 模式提炼
- 模式 1：双界面桥接（Dual-Interface Bridge）
- 模式 2：协议中立代理（Protocol-Neutral Proxy）——LSP 标准协议复用

### 你能做什么（5-8 条）
1. 为 Agent CLI 设计 Bridge 接口，让 IDE 和终端共享同一个 Agent 后端
2. 用 UDS 替代 HTTP 实现本机进程间通信，降低延迟
3. 集成 LSP 协议让 Agent 感知代码诊断信息，而无需自己解析源码
4. 在 Bridge 层实现指数退避重连，让 IDE 扩展在 Agent 重启后自动恢复
5. 将权限决策保留在 Agent 进程内，Bridge 只负责 UI 展示和消息转发
6. 为 Bridge 消息定义结构化格式（JSON Schema），便于多语言 IDE 插件接入

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图8-1 | Bridge 双通道架构 | ASCII box-arrow | 终端(Ink REPL) + IDE(VS Code) 通过 Bridge 连接同一 Agent 进程 | 8.1 |
| 图8-2 | Bridge 权限代理时序 | Mermaid sequenceDiagram | IDE→Bridge→Agent→权限系统→决策→回传 | 8.2 |
| 图8-3 | LSP 集成数据流 | Mermaid flowchart | Agent→LSPClient→LSPServerManager→Language Server→诊断结果 | 8.3 |

## 写作顺序

1. ⛔ 读取完整 spec（specs/ch08.spec）和 DESIGN.md STYLE_ID=hunter
2. 先写节 8.1（Bridge 主循环）
3. 再写节 8.2（Bridge 权限回调）
4. 写节 8.3（LSP 集成，明确说明与8.2的区别）
5. 写图表（先 ASCII 草图，再转 Mermaid）
6. 写节 8.4（模式剖析）+ 8.5（权衡）+ 8.6（对话）
7. 写模式提炼 + 你能做什么
8. 最后补写开篇

## 写作前 checklist

```bash
sed -n '59,65p' src/bridge/bridgeMain.ts      # BackoffConfig 类型
sed -n '141,165p' src/bridge/bridgeMain.ts    # runBridgeLoop 入口
ls src/bridge/bridgePermissionCallbacks.ts    # 权限回调文件
ls src/services/lsp/LSPServerManager.ts src/services/lsp/LSPClient.ts
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：specs/ch08.spec

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束·通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束·通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属·必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属·禁止 |
| 6 | 章节结构 | ✓ | 章节骨架 |
| 7 | 源码引用格式 | ✓ | 各节·源码锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束·排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束·排除范围 |

**验收场景覆盖统计**：A(4/4) + B(3/3) + C(6/6) + D(5/5) + E(3/3) = **21/21**
