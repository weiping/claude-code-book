# 第 3 章写作计划

**批次**：批次 1b（来自 plans/batch.md）
**章节**：ch03 — 构建时特性开关——bun:bundle feature() 的死代码消除
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

揭示 Claude Code 如何用 `bun:bundle` 的 `feature()` 函数在构建时消除死代码，从而替代运行时 if/else 判断实现零成本特性隔离。读者读完后能理解 DCE（Dead Code Elimination）在生产 CLI 中的工程价值，并能将"构建时门控"模式用于自己的项目。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**（从 spec 约束·必须 前 4 条提取）：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**（从 spec 约束·禁止 前 4 条提取）：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- src/assistant/ 内部实现（feature('KAIROS') 的门控内容）
- Bun 构建系统的完整源码（只分析 feature() 的使用模式）
- src/ssh/、src/server/、src/proactive/（排除范围 stub）

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例 1` → `## 源码实例 2（变体）` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号（开篇是问题场景+模式预告+价值承诺，150-200字）
- 全章不得混用其他风格的写作手法

**通用写作铁律**（所有风格通用，写作期间必须遵守）：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据，禁止基于 AI 记忆写作
- ⛔ 代码无裸露：禁止"标题→代码→下一个标题"，每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文（中文（English Full Name, ABBR)）
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写，禁止在开篇直接引用源码

## 前置阅读清单
（按阅读顺序排列，不超过 6 个文件）

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/main.tsx | feature() 调用列表（行 76、80、171、337、548 等） | 行 76-81, 171, 337, 548-577 |
| 2 | src/utils/bunFeatures.ts | feature() 模拟实现（完整文件约 50 行） | 全文件 |
| 3 | src/main.tsx | feature() 条件分支中的 require 用法 | 行 76-81, 548-577 |
| 4 | src/main.tsx | process.env 运行时条件（作为 DCE 对比组） | 行 250, 293-296, 408-411 |

验证文件存在：
```bash
ls src/main.tsx src/utils/bunFeatures.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用应存在于实际文件中 | 节 3.1-3.7（骨架每节均有锚点） | ☐ |
| A2 | 推断有标注 | 无源码直接依据的结论标注「（推断）」 | 骨架全部节 | ☐ |
| A3 | 不引用排除范围 | 不深入 src/assistant/ 内部实现 | 骨架全部节（KAIROS 只引用门控行，不深入 assistant 目录） | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 M 章"代替重复 | 节 3.1（第1章概述用"详见第1章"） | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全章 | ☐ |
| B2 | 实际 flag 列举具体化 | 18 个 flag 名称均来自实际 feature() 调用 | 节 3.2（flag 清单表格） | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 节 3.1-3.7（DCE、feature flag 等术语首次出现时标注） | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 概念递进层次 | 按定义→设计意图→实际案例递进 | 节 3.1-3.4（每节覆盖至少两层） | ☐ |
| C2 | ⛔ 风格专属结构 | hunter：模式预告开篇→问题→源码实例→模式剖析→适用范围→权衡与局限→与已知模式对话→你能做什么 | 骨架整体结构与 spec 一致 | ☐ |
| C3 | DCE 原理说明 | DCE 在构建阶段移除未激活分支，运行时 if/else 保留全部分支 | 节 3.1（DCE vs 运行时对比） | ☐ |
| C4 | 设计权衡有说明 | 构建时门控 vs 运行时 if/else 的选 A 不选 B 理由 | 节 3.1、节 3.5（权衡分析） | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末"你能做什么" | ☐ |
| C6 | 章节间链接 | 章末向后链接 + 章节内向前链接 | 节 3.1（向前引用第1章）+ 章末（向后引用第 6 章） | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表可渲染且达标 | ≥1 个 Mermaid 图 | 节 3.1（DCE vs 运行时流程图）、节 3.3（flag 分类图） | ☐ |
| D2 | 图表标注格式 | 上方 `**图 3-X：标题**`，下方说明文字 | 节 3.1、3.3（每个图表） | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 节 3.1（feature 条件 require 代码）、节 3.2（bunFeatures 代码） | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 节 3.1（feature 注释）、节 3.2（bunFeatures 注释） | ☐ |
| D5 | ⛔ 风格专属图表 | hunter：适用范围表 + 模式命名框 | 节 3.5（适用范围表）、章末（模式命名框） | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 开篇不引用源码，含三要素：问题场景+模式预告+价值承诺 | 开篇（最后补写） | ☐ |
| E2 | 多实例证明模式普遍性 | ≥2 处不同位置的源码实例（模块级 require + 条件初始化） | 节 3.2（模块级 require 实例1）、节 3.3（条件初始化实例2） | ☐ |
| E3 | 模式命名框格式规范 | 模式名称+问题+解决方案+源码锚点 | 章末模式提炼 | ☐ |
| E4 | 读者对话感 | "我们"而非"用户"，有设问和预告 | 全章 | ☐ |
| E5 | 关键信息突出 | 加粗标注，表格展示对比 | 节 3.2（flag 清单表）、节 3.4（变体对比表） | ☐ |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

## 章节骨架

### 开篇（hunter 模式预告）
> ⛔ 开篇禁止直接引用源码。在全章初稿完成后最后补写。

- 开篇公式：问题场景（"一个 CLI 工具有 18 个特性开关——如果全部用运行时 if/else，每个分支的代码、依赖和安全风险都会进入最终二进制"）+ 模式预告（"构建时门控（Build-time Gate）——Claude Code 在编译阶段就把不需要的分支从产物中抹去"）+ 价值承诺（"读完本章，我们将掌握一种零运行时成本的特性隔离技术"）
- 目标：150-200 字

### 节 3.1：问题——18 个特性开关的运行时成本
- 核心主张：`src/main.tsx` 中有 18 个 `feature()` 调用，覆盖从 AI 助手模式（KAIROS）到 MCP 协议（CHICAGO_MCP）等关键功能。如果这些判断都在运行时执行，每个分支的代码、依赖和潜在安全风险都会进入最终二进制——**构建时 DCE 解决的就是"不需要的代码根本不存在"这个问题**（对应验收场景 ch03_dce_explanation）
- 源码锚点：
  - `src/main.tsx:35` — `import { feature } from 'bun:bundle'`（已验证）
  - `src/main.tsx:80` — `const assistantModule = feature('KAIROS') ? require('./assistant/index.js') : null`（已验证）
  - `src/main.tsx:76` — `const coordinatorModeModule = feature('COORDINATOR_MODE') ? require('./coordinator/coordinatorMode.ts') : null`（已验证）
- 概念层次：定义（DCE 是什么——构建阶段移除未激活分支）→ 设计意图（为什么不用运行时 if/else）→ 实际案例（KAIROS 门控如何让 Assistant 模块从产物中消失）
- 权衡分析：构建时 DCE vs 运行时 if/else——DCE 消除了运行时开销和安全风险，但代价是 flag 变更需要重新构建
- 包含元素：
  - **Mermaid 流程图**：DCE vs 运行时 if/else 的对比（左侧：构建时 DCE → 产物只含活跃分支；右侧：运行时 if/else → 产物含全部分支）
  - 代码片段：KAIROS 和 COORDINATOR_MODE 的条件 require（≤15行）
- **⛔ 内容密度规划**：
  - 引入段要点：说明"第1章提到了 feature() 但没展开——本章就是展开"
  - 解读段要点：分析三元运算符 `feature('X') ? require(...) : null` 的 DCE 语义——当 feature 为 false 时，`require(...)` 分支在构建后被完全移除
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1200-1500 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch03_dce_explanation、ch03_source_anchors
- 向后引用：feature() 概述"详见第1章"

### 节 3.2：源码实例 1——模块级条件 require 的 5 种形态
- 核心主张：`feature()` 在 `src/main.tsx` 中的使用有 5 种不同形态——模块级条件 require（行 76-81）、函数内条件分支（行 337）、条件对象初始化（行 548-577）、条件行为控制（行 647）、条件环境检测（行 1571）——它们共享同一 DCE 模式，但触发时机和代码位置不同（对应验收场景 ch03_flags_list、ch03_multi_instance_proof）
- 源码锚点：
  - `src/main.tsx:76` — COORDINATOR_MODE 模块级 require（已验证）
  - `src/main.tsx:80-81` — KAIROS 模块级 require（已验证）
  - `src/main.tsx:171` — TRANSCRIPT_CLASSIFIER 模块级 require（已验证）
  - `src/main.tsx:337` — TRANSCRIPT_CLASSIFIER 函数内条件分支（已验证）
  - `src/main.tsx:548` — DIRECT_CONNECT 条件对象初始化（已验证）
  - `src/main.tsx:577` — SSH_REMOTE 条件对象初始化（已验证）
  - `src/main.tsx:647` — LODESTONE 条件行为控制（已验证）
- 概念层次：分类（5 种形态）→ 每种形态的特征 → 关键区别
- 权衡分析：模块级 vs 函数内——模块级 require 在加载时就决定是否引入依赖（更激进的隔离），函数内分支在运行时才执行（更灵活但隔离较弱）
- 包含元素：
  - **表格**：18 个 feature flag 清单（Flag 名 | 使用行号 | 形态 | 门控模块）
  - 代码片段：条件对象初始化（DIRECT_CONNECT + SSH_REMOTE，≤15行）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"18 个 flag 不是同一种用法——它们有 5 种不同的门控形态"
  - 解读段要点：逐个分析 5 种形态的 DCE 效果差异
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1500-2000 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch03_flags_list、ch03_multi_instance_proof

### 节 3.3：源码实例 2（变体）——bunFeatures.ts 的运行时回退
- 核心主张：`src/utils/bunFeatures.ts` 提供了 `feature()` 的运行时模拟——当不在 Bun 环境中运行时（如测试、开发环境），这个 50 行的模块用 JavaScript 对象模拟构建时 DCE。这是"构建时门控"模式的第二个变体：**运行时回退（Runtime Fallback）**（对应验收场景 ch03_multi_instance_proof）
- 源码锚点：
  - `src/utils/bunFeatures.ts:25` — `export function feature(featureName: string): boolean`（已验证，完整实现）
  - `src/utils/bunFeatures.ts:14-17` — DEFAULT_FEATURES 配置（已验证）
- 概念层次：定义（bunFeatures 是什么——feature() 的运行时替代品）→ 设计意图（为什么需要替代品——非 Bun 环境下无法使用 `bun:bundle`）→ 实际案例（DEFAULT_FEATURES 默认关闭所有 flag）
- 权衡分析：运行时回退 vs 编译错误——Claude Code 选择回退而非报错，因为测试环境（Node.js）需要能运行源码
- 包含元素：
  - 代码片段：bunFeatures.ts 完整实现（约 50 行，可完整展示——这是唯一可以完整展示的源码文件）
  - **对比表格**：Bun 环境（真 DCE）vs 非 Bun 环境（运行时回退）的行为差异
- **⛔ 内容密度规划**：
  - 引入段要点：预告"如果 feature() 是构建时魔法，那这个 50 行文件就是魔法的后备方案"
  - 解读段要点：分析 DEFAULT_FEATURES 中 KAIROS/COORDINATOR_MODE 默认为 false 的工程意义——默认关闭确保非 Bun 环境不会意外启用门控功能
  - 散文与代码比例目标：至少 2 句散文/行代码
- 估计字数：1000-1300 字（其中散文 ≥ 60%）
- 对应 spec 场景：ch03_multi_instance_proof
- 关键区别说明：实例 1（main.tsx 的 feature 调用）是"构建时 DCE 的使用者"，实例 2（bunFeatures.ts）是"DCE 不可用时的替代实现"——两者是同一模式的"生产路径 vs 回退路径"

### 节 3.4：模式剖析——构建时门控的两个变体
- 核心主张："构建时门控"在 Claude Code 中有两个变体——**构建时消除**（Bun feature() → 死代码消失）和**运行时回退**（bunFeatures.ts → JavaScript 对象模拟）——它们共享"通过 feature() 函数隐藏分支细节"的接口，但实现机制完全不同（对应验收场景 ch03_pattern_box_format）
- 源码锚点：引用节 3.2 和 3.3 的锚点
- 概念层次：模式定义 → 变体对比 → 关键区别
- 权衡分析：统一接口 vs 分裂实现——调用方只需 `feature('X')` 不需要关心背后是 DCE 还是运行时回退，但增加了调试难度
- 包含元素：
  - **对比表格**：变体 | 执行时机 | 代码消除 | 依赖引入 | 适用环境
  - 模式命名框：构建时门控（Build-time Gate）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"同一个 feature() 函数背后，隐藏着两种完全不同的机制"
  - 解读段要点：分析为什么需要运行时回退——测试环境、开发调试、源码重建场景
  - 散文与代码比例目标：全散文 + 表格
- 估计字数：800-1000 字
- 对应 spec 场景：ch03_pattern_box_format

### 节 3.5：适用范围——何时用/不用构建时门控
- 核心主张：构建时门控适合"功能模块独立、需要零运行时成本、flag 变更频率低"的场景，不适合"需要运行时动态切换、flag 依赖运行时输入、模块间强耦合"的场景（对应验收场景 ch03_applicability_table）
- 源码锚点：引用节 3.2 的 flag 列表作为实例
- 概念层次：适用场景 → 不适用场景 → 替代方案
- 权衡分析：N/A（本节本身就是权衡分析）
- 包含元素：
  - **适用范围表**：场景 | 适用(✓/✗) | 理由 | 替代方案
- **⛔ 内容密度规划**：
  - 引入段要点：预告"构建时门控不是万能的——知道什么时候用运行时配置同样重要"
  - 解读段要点：表格中每行附带具体理由和替代方案（如环境变量、配置文件、Feature Flag 服务）
  - 散文与代码比例目标：全散文
- 估计字数：400-600 字
- 对应 spec 场景：ch03_applicability_table

### 节 3.6：权衡与局限——锁死在 Bun 生态
- 核心主张：`bun:bundle feature()` 的最大局限是**平台锁定**——这个 API 是 Bun 运行时特有的，其他 JavaScript 运行时（Node.js、Deno）没有等价物。Claude Code 用 bunFeatures.ts 作为补偿，但运行时回退无法提供真 DCE 效果。此外，flag 变更需要重新构建（无法运行时切换），这降低了开发调试的效率（对应验收场景 ch03_tradeoffs_and_limits）
- 源码锚点：`src/utils/bunFeatures.ts:25` — feature() 模拟（补偿机制的证据）
- 概念层次：平台锁定代价 → 补偿机制 → 残留局限
- 权衡分析：本节分析构建时门控模式本身的权衡
- 包含元素：无代码（纯分析散文）
- **⛔ 内容密度规划**：
  - 引入段要点：预告"每个模式都有代价——这个模式的代价是绑定 Bun 生态"
  - 解读段要点：分析平台锁定的具体影响——迁移到 Node.js 需要 Webpack/Terser 配置替代方案
  - 散文与代码比例目标：全散文
- 估计字数：500-700 字
- 对应 spec 场景：ch03_tradeoffs_and_limits

### 节 3.7：与已知模式的对话——Build-time Gate vs Feature Flag vs Strategy Pattern
- 核心主张：构建时门控与业界常见的 Feature Flag（特性标记）模式和 GoF 的 Strategy Pattern（策略模式）有本质区别——Feature Flag 在运行时切换分支，Strategy Pattern 在代码层面替换算法，而构建时门控在编译阶段**消除**分支。这种"编译时确定性"是它与其他两种模式的核心差异（对应验收场景 ch03_known_pattern_dialogue）
- 源码锚点：引用节 3.2 和 3.3 的实例
- 概念层次：已知模式描述 → 共同点 → 不同点 → 演化动机
- 权衡分析：N/A
- 包含元素：
  - **对比表格**：模式 | 切换时机 | 代码消除 | 运行时开销 | 典型工具
- **⛔ 内容密度规划**：
  - 引入段要点：预告"这个模式与 Feature Flag 名字相似但本质不同——区别在于'什么时候做决定'"
  - 解读段要点：分析三种模式在"决定时机"维度的本质差异——构建时 vs 运行时 vs 代码组织
  - 散文与代码比例目标：全散文
- 估计字数：600-800 字
- 对应 spec 场景：ch03_known_pattern_dialogue

### 章末：模式提炼（hunter 风格）
- 核心模式：**构建时门控（Build-time Gate）**
  - 模式名称：构建时门控（Build-time Gate）
  - 解决的问题：如何让不需要的特性在编译后完全不存在——不是运行时跳过，而是字节码层面消除？
  - 核心做法：用 `feature('FLAG')` 三元运算符包裹条件分支，构建器在编译时求值并移除 false 分支
  - 前置条件：构建工具支持 DCE、flag 值在构建时确定、非 Bun 环境有运行时回退方案
  - 源码证据：`src/main.tsx:80`（KAIROS 门控）、`src/main.tsx:76`（COORDINATOR_MODE 门控）、`src/utils/bunFeatures.ts:25`（运行时回退）

### 章末：你能做什么
预规划 5-8 条行动建议：
1. 用 `grep -o "feature('[A-Z_]*')" src/main.tsx | sort -u` 列出所有 feature flag，验证本章提到的 18 个
2. 对比 `src/main.tsx:80` 的 KAIROS 门控和 `src/main.tsx:250` 的 process.env 检查，理解构建时 vs 运行时的代码差异
3. 在自己的项目中，尝试用 Webpack 的 `DefinePlugin` 或 Vite 的 `define` 实现类似的构建时门控
4. 阅读 `src/utils/bunFeatures.ts` 完整实现，理解运行时回退的设计约束
5. 思考你的项目中有多少 if/else 分支可以变成构建时门控——哪些分支在构建时就能确定？
6. 追踪 `feature('TRANSCRIPT_CLASSIFIER')` 的两个使用位置（行 171 和 337），理解同一个 flag 在不同门控形态下的 DCE 效果差异
7. 对比 Bun 的 `feature()` 与 C 语言的 `#ifdef` 预处理指令——它们的共同祖先是什么？
8. 从第 6 章开始，理解查询引擎如何使用全局状态（第2章）和特性开关（本章）协同工作

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图3-1 | DCE vs 运行时 if/else 对比 | Mermaid flowchart | 左侧：feature() → 构建时消除 → 产物小；右侧：if/else → 运行时判断 → 产物大 | 3.1 |
| 图3-2 | 18 个 feature flag 分类 | ASCII box-arrow | 按形态分组：模块级 require / 函数内分支 / 条件初始化 / 行为控制 / 环境检测 | 3.2 |

## 写作顺序

1. ⛔ **确认完整 spec 与风格规则**：重新读取完整 spec 文件和 DESIGN.md 中的 STYLE_ID=hunter，确认全部 16 项 spec 要素已在 plan 中反映
2. 先写节 3.1（问题——18 个特性开关的运行时成本，逻辑起点）
3. 然后写节 3.2（5 种门控形态，依赖 3.1 建立的 DCE 概念）
4. 写节 3.3（bunFeatures.ts 运行时回退，与 3.2 形成对比）
5. 写节 3.4（模式剖析——两个变体的统一框架）
6. 写节 3.5（适用范围表）和节 3.6（权衡与局限）
7. 写节 3.7（与已知模式的对话）
8. 写章末模式提炼和"你能做什么"
9. 写图表时先画 ASCII 草图，确认结构后再转 Mermaid
10. **最后补写开篇**（此时知道全章讲了什么，才能写出符合 hunter 风格的模式预告）

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
sed -n '35p' src/main.tsx        # import { feature } from 'bun:bundle'
sed -n '76p' src/main.tsx        # COORDINATOR_MODE
sed -n '80,81p' src/main.tsx     # KAIROS
sed -n '171p' src/main.tsx       # TRANSCRIPT_CLASSIFIER require
sed -n '337p' src/main.tsx       # TRANSCRIPT_CLASSIFIER function
sed -n '548p' src/main.tsx       # DIRECT_CONNECT
sed -n '577p' src/main.tsx       # SSH_REMOTE
sed -n '647p' src/main.tsx       # LODESTONE
cat src/utils/bunFeatures.ts     # 完整实现
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`/Users/liuweiping/repos/claude-code-sourcemap/specs/ch03.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✅ | 章节意图 |
| 2 | 通用约束·必须 | ✅ | 通用约束 |
| 3 | 通用约束·禁止 | ✅ | 通用约束 |
| 4 | 风格专属必须 | ✅ | 风格专属写作要求 → 风格专属必须 |
| 5 | 风格专属禁止 | ✅ | 风格专属写作要求 → 风格专属禁止 |
| 6 | 章节结构 | ✅ | 章节骨架（hunter 模式预告→问题→实例1→实例2→剖析→适用范围→权衡→对话→你能做什么） |
| 7 | 源码引用格式 | ✅ | 章节骨架 → 每节锚点格式（src/相对路径:行号） |
| 8 | 图表格式 | ✅ | 图表清单（Mermaid + ASCII） |
| 9 | 边界路径 | ✅ | 通用约束 → 排除范围（3 项排除） |
| 10 | 验收优先级 | ✅ | Spec 验收场景清单（E>A>B=C>D） |
| 11 | 验收场景·A 源码锚点组 | ✅ | Spec 验收场景清单 A（4 项） |
| 12 | 验收场景·B 语言精确性组 | ✅ | Spec 验收场景清单 B（3 项） |
| 13 | 验收场景·C 结构完整性组 | ✅ | Spec 验收场景清单 C（6 项） |
| 14 | 验收场景·D 图表与代码组 | ✅ | Spec 验收场景清单 D（5 项） |
| 15 | 验收场景·E 风格符合性组 | ✅ | Spec 验收场景清单 E（5 项） |
| 16 | 排除范围 | ✅ | 通用约束 → 排除范围（3 项） |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 5/5 = **23/23**

**⛔ 如有任何 ☐ 未勾选，不得提交 plan，必须修复后重新审查。**

全部 ✅ 已勾选。
