# 第 39 章写作计划

**批次**：批次 9b（来自 plans/batch.md）
**章节**：ch39 — 权限处理器三态——interactive/coordinator/swarmWorker 的策略差异
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/hooks/toolPermission/handlers/` 目录下 interactive/coordinator/swarmWorker 三种处理器的策略差异，以及它们如何在保持相同的"权限检查"接口下实现不同的行为。

## 通用约束
**排除范围**：第 37 章三层防线（不重复）、第 36 章 permissionSync（不展开）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「上下文策略分叉」（Context-Aware Strategy Fork）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（interactiveHandler vs swarmWorkerHandler），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/hooks/toolPermission/handlers/interactiveHandler.ts | 函数签名(57行)、不返回Promise注释(53行)、clearClassifierIndicator(86行) | 44-90 |
| 2 | src/hooks/toolPermission/handlers/coordinatorHandler.ts | 注释说明(17-24行)、handleCoordinatorPermission(26行) | 全文 |
| 3 | src/hooks/toolPermission/handlers/swarmWorkerHandler.ts | handleSwarmWorkerPermission(40行)、fallthrough注释(38行) | 38-80 |

```bash
ls src/hooks/toolPermission/handlers/
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：同样的工具使用请求，在 main agent 中弹出确认对话框，在 coordinator 中先等 hook 再弹框，在 swarm worker 中则通过邮箱转发给 Leader——三种处理器，三种策略，相同的接口。

### 节 39.1：问题
- 核心主张：**相同的"应该用这个工具吗"问题，在不同的 Agent 角色下答案的获取方式完全不同——交互式 UI、自动化钩子检查、跨进程协商**
- 源码锚点：`src/hooks/toolPermission/handlers/coordinatorHandler.ts:17`（注释说明 coordinator 的行为差异）
- 包含元素：**图39-1** — 三种处理器策略对比表
- 估计字数：600-800字

### 节 39.2：源码实例1 — interactiveHandler（UI 弹框，非 Promise）
- 核心主张：**interactiveHandler 是同步设置回调的——它不返回 Promise，而是设置 UI 队列，让 React 异步渲染确认对话框**
- 源码锚点：
  - `src/hooks/toolPermission/handlers/interactiveHandler.ts:53`（注释："This function does NOT return a Promise -- it sets up callbacks"）
  - `src/hooks/toolPermission/handlers/interactiveHandler.ts:57`（`function handleInteractivePermission(`）
  - `src/hooks/toolPermission/handlers/interactiveHandler.ts:86`（`clearClassifierIndicator` — 分类器 UI 指示器清理）
- 概念层次：非 Promise 的设计（React 状态更新是异步的，设置队列比 await 更合适）、clearClassifierIndicator 的 UX 意义
- 估计字数：900-1100字

### 节 39.3：源码实例2 — swarmWorkerHandler（转发给 Leader）
- 核心主张：**swarmWorkerHandler 尝试通过邮箱将权限请求转发给 Leader——如果失败（如 Leader 不可用），注释说明"fall through to interactive handling"**
- 源码锚点：
  - `src/hooks/toolPermission/handlers/swarmWorkerHandler.ts:38`（注释："so the caller can fall through to interactive handling"）
  - `src/hooks/toolPermission/handlers/swarmWorkerHandler.ts:40`（`async function handleSwarmWorkerPermission(`）
- 概念层次：优雅降级（Leader 不可用时 fallthrough 到 interactive）、邮箱转发的异步性
- 估计字数：700-900字

### 节 39.4：模式剖析
- 核心主张：**三种处理器的本质是"上下文决定策略"——Agent 的角色（main/coordinator/swarmWorker）决定了权限获取方式**
- 估计字数：500-700字

### 节 39.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 同一功能在不同运行上下文需要不同行为 | ✓ | 策略分叉封装上下文差异 | 到处写 if (isSwarmWorker)（难维护）|
| 需要优雅降级（首选失败时自动 fallback）| ✓ | swarmWorker fallthrough 设计 | 抛出异常（用户体验差）|
| 上下文判断逻辑非常复杂 | ✗（谨慎）| 三个 handler 难以测试完整的交互 | 统一函数 + context 参数（但复杂）|

### 节 39.6：权衡与局限
- 三个文件分散了相关逻辑、coordinator 和 interactive 有部分重复代码（hook 检查逻辑）
- 估计字数：500-700字

### 节 39.7：与已知模式的对话
- 与 GoF 策略模式：最接近——可替换的算法族；差异是策略通常在构造时选择，本模式在每次调用时根据 Agent 角色动态路由

### 章末：模式提炼
```
模式名称：上下文策略分叉（Context-Aware Strategy Fork）
问题：同一操作（权限检查）在不同 Agent 上下文（main/coordinator/swarmWorker）需要完全不同的实现
解决方案：三个独立的 handler 函数，调用方根据 Agent 角色路由到对应 handler，每个 handler 可独立演化
前置条件：能明确区分 Agent 角色（getAgentType/isSwarmWorker 等谓词）
源码锚点：src/hooks/toolPermission/handlers/interactiveHandler.ts:53 (非Promise设计注释)
```

### 章末：你能做什么
1. 将"相同功能、不同上下文"的分叉逻辑提取到独立的 handler 函数，而非在单一函数内 if/else
2. 为非 Promise 的异步 UI 设置函数写明确注释（"does NOT return a Promise"），防止调用方误用 await
3. 为转发型处理器（swarmWorker）实现 fallthrough 降级，而非在失败时抛出异常

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图39-1 | 三种权限处理器策略对比 | Markdown 表格 | interactive/coordinator/swarmWorker 的触发条件/实现方式/降级策略 | 35.1 |

## 写作前 checklist
```bash
sed -n '17p' src/hooks/toolPermission/handlers/coordinatorHandler.ts  # 注释
sed -n '53p' src/hooks/toolPermission/handlers/interactiveHandler.ts  # 非Promise注释
sed -n '57p' src/hooks/toolPermission/handlers/interactiveHandler.ts  # 函数签名
sed -n '38p' src/hooks/toolPermission/handlers/swarmWorkerHandler.ts  # fallthrough注释
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
