# 第 7 章写作计划

**批次**：批次 3a（来自 plans/batch.md）
**章节**：ch07 — 斜杠命令系统——103 个命令的注册、加载与执行
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

揭示 Claude Code 如何通过"目录即注册表"约定 + 动态加载 + feature flag 门控实现 103 个斜杠命令的一致性管理。读者读完后能将这一模式用于自己的可扩展 CLI 设计，知道如何用目录结构替代显式注册表。（注：实际命令目录数以源码 `find src/commands -maxdepth 1 -type d | wc -l` 统计为准，当前为 94 个子目录）

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- 每个斜杠命令的具体业务逻辑实现
- src/assistant/、src/ssh/、src/server/、src/proactive/（排除范围 stub）
- 第 6 章已讲解的输入分流路由逻辑

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例 1` → `## 源码实例 2（变体）` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号
- 全章不得混用其他风格的写作手法

**通用写作铁律**：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字（94 个目录）
- ⛔ 术语标准化：首次出现技术术语时标注原文
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/commands.ts | COMMANDS memoize 数组（内置命令静态注册）+ feature flag 门控 | 行 59-120, 258-350 |
| 2 | src/commands.ts | loadAllCommands 异步聚合函数 + getCommands 可用性过滤 | 行 449-530 |
| 3 | src/commands.ts | meetsAvailabilityRequirement 可用性检查 | 行 417-450 |
| 4 | src/utils/plugins/loadPluginCommands.ts | 插件命令加载函数（目录扫描逻辑） | 行 1-80 |
| 5 | src/commands/clear/index.ts | 典型命令模块结构（isEnabled + handler） | 行 1-50 |

验证文件存在：
```bash
ls src/commands.ts src/utils/plugins/loadPluginCommands.ts src/commands/clear/index.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用实际存在 | 章节骨架各节 | ☐ |
| A2 | 推断有标注 | 推断有"（推断）"标注 | 全章 | ☐ |
| A3 | 不引用排除范围 | 没有任何引用落在 stub 或排除范围中 | 排除范围声明 | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 6 章"代替分流路由重复 | 斜杠命令路由处 | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 命令数量用实际统计值（94）而非"103" | 节 7.1 | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 全章 | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 概念递进层次 | hunter: 问题→实例→模式剖析递进 | 节 7.1→5.2→5.3→5.4 | ☐ |
| C2 | hunter 专属结构 | 模式命名框（五层：名+问题+方案+条件+锚点） | 节 7.4 模式剖析 | ☐ |
| C3 | 核心主张可提炼 | 可提炼"目录即注册表"模式 | 章末模式提炼 | ☐ |
| C4 | 设计权衡有说明 | 有"选 A 不选 B"分析（目录 vs 显式注册表） | 节 7.6 权衡 | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末"你能做什么" | ☐ |
| C6 | 章节间链接 | 章末向后/向前链接 | 各节章末 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表可渲染且达标 | Mermaid 标注正确，数量 ≥2 | 图表清单 | ☐ |
| D2 | 图表标注格式 | 上方有 `**图 N-X：标题**` | 图表清单 | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 源码实例节 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开 | 源码实例节 | ☐ |
| D5 | hunter 专属图表 | 适用范围表（✓/✗ 格式）存在 | 节 7.5 | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | hunter 开篇格式 | 150-200字，三要素不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有设问句，有预告文字 | 全章 | ☐ |
| E3 | 关键信息突出 | 关键结论加粗，对比用表格 | 全章 | ☐ |

## 章节骨架

### 开篇（hunter 模式预告，最后补写）
- 问题场景：想象你要维护一个有 94 个命令的 CLI，每次新增命令都要去修改一个中央注册文件——随着命令增加，这个文件会成为一个巨大的冲突热点，每个新命令的 PR 都要改它
- 模式预告：代码库用一个反复出现的模式解决了这个问题——"目录即注册表"（Directory-as-Registry）
- 价值承诺：读完本章，你能复用这一模式，让命令的新增变成"新建目录"而非"修改注册文件"
- ⛔ 禁止直接引用源码

### 节 7.1：问题——94 个命令的注册困境
- 核心主张：显式注册表（一个文件列出所有命令）随命令数量增长会成为永久性冲突热点
- 源码锚点：`src/commands.ts:258` — COMMANDS memoize 数组，静态列出所有内置命令
- 概念层次：
  - **层次1 定义**：什么是"注册表"问题——当前 COMMANDS 数组枚举 94 个内置命令模块
  - **层次2 设计意图**：为什么 Claude Code 把命令分成了"内置 COMMANDS 数组"和"目录扫描加载"两套机制
- 权衡分析：内置命令用静态数组（类型安全），技能和插件用目录扫描（可扩展）——两套机制并存的设计选择
- 包含元素：COMMANDS 数组片段（≤20行，展示几个典型命令的模块引用）
- 内容密度规划：
  - 引入段：从"如果你要给团队新增一个斜杠命令"的场景引入问题
  - 解读段：分析 COMMANDS 数组的结构，说明其优缺点
  - 散文与代码比例：70% 散文
- 估计字数：800-1000 字
- 对应 spec 场景：C1（概念递进）、B2（数据驱动：94个命令）

### 节 7.2：源码实例 1——feature flag 门控的目录扫描
- 核心主张：`src/commands.ts` 中，12 个命令通过 `feature()` 门控——目录存在但命令不可见，取决于构建时特性开关
- 源码锚点：
  - `src/commands.ts:59` — `import { feature } from 'bun:bundle'`
  - `src/commands.ts:70` — `const assistantCommand = feature('KAIROS') ? require(...) : null`
  - `src/commands.ts:411` — 注释："This runs before `isEnabled()` so that provider-gated commands are hidden"
- 概念层次：
  - **层次1 实例**：feature flag 条件导入（3-5 个典型例子）
  - **层次2 模式特征**：构建时消除死代码（第 3 章讲过 bun:bundle feature()，此处引用但不重复）
- 权衡分析：为什么用 feature flag 而非 isEnabled()——feature flag 在构建时消除代码，isEnabled 在运行时过滤
- 包含元素：feature flag 门控代码片段（≤15行）+ Mermaid 两层过滤示意图（**图 5-1**）
- 内容密度规划：
  - 引入段：先建立"门控"的概念——为什么一个命令可以"存在但不可见"
  - 解读段：分析 feature flag vs isEnabled 的分工，说明为什么两层都需要
  - 散文与代码比例：60% 散文
- 估计字数：1000-1200 字
- 对应 spec 场景：A1（源码引用）、A3（不引排除范围）

### 节 7.3：源码实例 2（变体）——loadAllCommands 的异步聚合
- 核心主张：`loadAllCommands` 把 4 个来源（内置、技能目录、工作流、插件）并行加载并合并——扩展点不在文件而在来源列表
- 源码锚点：
  - `src/commands.ts:449` — `const loadAllCommands = memoize(async (cwd: string) => {...})`
  - `src/commands.ts:460` — `await Promise.all([getSkills(cwd), getPluginCommands(), ...])`
  - `src/commands.ts:468` — `return [...bundledSkills, ...builtinPluginSkills, ...skillDirCommands, ...pluginSkills, ...COMMANDS()]`
- 概念层次：
  - **层次1 实例**：Promise.all 并行加载 4 个来源
  - **层次2 模式特征**：新增来源只需在聚合函数中加一行 spread——扩展无需修改任何现有模块
- 权衡分析：为什么用 memoize——命令加载代价高（I/O），用 memoize 避免重复加载；清除 cache 的时机（/login、/logout）
- 包含元素：loadAllCommands 函数代码（≤25行，展示聚合逻辑）
- 内容密度规划：
  - 引入段：从"如果要新增一种命令来源（如企业内部库）"引入扩展点的概念
  - 解读段：逐行分析 spread 合并的设计含义
  - 散文与代码比例：60% 散文
- 估计字数：900-1100 字
- 对应 spec 场景：A1（源码引用）、C2（hunter 专属结构）

### 节 7.4：模式剖析——目录即注册表（Directory-as-Registry）
- 核心主张：提炼可命名的工程模式
- 包含元素：
  - **模式命名框**：
    ```
    模式名称：目录即注册表（Directory-as-Registry）
    问题：可扩展 CLI 的命令集随时间增长，显式注册表成为永久性合并冲突热点
    解决方案：用目录结构作为隐式注册表——每个目录是一个命令，运行时动态扫描而非静态枚举
    前置条件：命令之间相互独立（无依赖关系）；命令可以通过约定的文件结构自描述
    源码锚点：src/commands.ts:449-470（loadAllCommands 聚合函数）
    ```
- 估计字数：600-800 字
- 对应 spec 场景：C2（hunter 专属模式命名框）、C3（核心主张）

### 节 7.5：适用范围

| 场景 | 适用 | 理由/替代方案 |
|------|------|------------|
| 命令集开放扩展（第三方可添加） | ✓ | 第三方只需新建目录，无需修改核心代码 |
| 命令之间相互独立 | ✓ | 目录结构无法表达命令间的依赖关系 |
| 命令数量 > 20 且持续增长 | ✓ | 小型项目显式注册表更清晰 |
| 命令有严格的加载顺序依赖 | ✗ | 改用显式注册表（可控制顺序） |
| 命令需要注册时传参（如共享上下文） | ✗ | 目录扫描无法传构造参数，改用 Factory 模式 |
| 命令数量 ≤10 且不增长 | ✗ | 显式枚举更清晰，无需目录约定 |

- 估计字数：400-600 字
- 对应 spec 场景：D5（适用范围表）

### 节 7.6：权衡与局限
- 核心要点：
  - **适用边界**：命令目录 ≥10 时优势明显，<5 时引入无谓复杂度
  - **潜在风险**：约定依赖文档——新人不知道"目录=命令"时，目录结构变成隐式契约
  - **性能影响**：文件系统扫描是 I/O 操作，需要 memoize 避免重复（`src/commands.ts:449`）
  - **替代方案**：显式注册表（类型安全但不可扩展）、IoC 容器（可扩展但复杂）
- 估计字数：500-700 字
- 对应 spec 场景：C4（设计权衡）

### 节 7.7：与已知模式的对话
- 对比目标：GoF Plugin（插件）模式 + POSA Microkernel（微内核）模式
- 对比要点：
  - **与 Plugin 相同**：都允许在不修改核心代码的情况下扩展功能
  - **与 Plugin 不同**：Plugin 模式通常有显式注册步骤（register()）；Directory-as-Registry 通过约定完全隐式
  - **与 Microkernel 相同**：核心（commands.ts）+ 可插拔插件（子目录）的结构
  - **Claude Code 的选择**：选择最简形式——没有插件接口，只有目录约定，符合"Convention over Configuration"
- 估计字数：400-600 字
- 对应 spec 场景：C2（hunter 与已知模式对话节）

### 章末结构：模式提炼（hunter）

**模式 1：目录即注册表（Directory-as-Registry）**
- 解决的问题：显式注册表在大型 CLI 中成为合并冲突热点
- 核心做法：约定目录结构替代显式枚举，运行时动态扫描
- 前置条件：命令相互独立；可通过目录约定自描述
- 源码证据：`src/commands.ts:449-470`

**模式 2：特性开关门控（Feature Flag Gating）**
- 解决的问题：某些命令只在特定部署环境或实验性状态下可用
- 核心做法：`feature()` 构建时门控（消除死代码） + `isEnabled()` 运行时门控（条件显示）
- 前置条件：使用 Bun bundle 构建；feature flag 在构建时已知
- 源码证据：`src/commands.ts:59,70` + `src/commands.ts:411`

**模式 3：懒加载聚合（Lazy Aggregation）**
- 解决的问题：命令加载是 I/O 密集操作，不能在模块初始化时执行
- 核心做法：`memoize(async (cwd) => Promise.all([...来源]))` 首次调用时并行加载，结果缓存
- 前置条件：命令集在运行期间不频繁变更；有明确的缓存清除时机
- 源码证据：`src/commands.ts:449`

### 章末：你能做什么

1. 在自己的 CLI 项目中，将命令目录约定为"每个子目录=一个命令"，通过动态 import 加载
2. 区分"构建时不可见"（feature flag）和"运行时不可用"（isEnabled）的两类门控场景，按需选择
3. 用 `memoize` 包裹命令加载函数，避免每次调用都重扫文件系统
4. 定义命令扩展点时，优先用"目录+约定"而非"注册函数"，降低新增命令的摩擦
5. 对内置核心命令保留显式数组（类型安全）；对第三方扩展命令使用目录扫描（可扩展性）
6. 设计"来源聚合"时，新来源用 spread 合并，避免修改聚合函数内部逻辑

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图 5-1 | 命令两层门控机制 | Mermaid flowchart | 展示 feature flag（构建时）→ isEnabled（运行时）两层过滤，最终到 getCommands 可见列表 | 5.2 |
| 图 5-2 | loadAllCommands 四来源聚合 | Mermaid flowchart | 4 个并行来源（内置/技能/工作流/插件）→ Promise.all → 合并列表 | 5.3 |

## 写作顺序

1. ⛔ 确认 STYLE_ID=hunter + 重新读取完整 spec（`specs/ch07.spec`）
2. 先写节 7.2（源码实例1，feature flag 门控）
3. 再写节 7.3（源码实例2，loadAllCommands 聚合）
4. 写节 7.1（问题节，铺垫）
5. 写节 7.4-5.7（模式剖析、适用范围、权衡、对话）
6. 画图表（先 ASCII 草图，确认结构后转 Mermaid）
7. 写章末模式提炼
8. 写"你能做什么"
9. 最后补写开篇

## 写作前 checklist

```bash
# 验证核心行号
sed -n '258p' src/commands.ts    # COMMANDS memoize 函数
sed -n '449p' src/commands.ts    # loadAllCommands 函数
sed -n '59p' src/commands.ts     # feature import
sed -n '70p' src/commands.ts     # assistantCommand feature gate
sed -n '411p' src/commands.ts    # isEnabled 注释
# 统计实际命令数
find src/commands -maxdepth 1 -type d | wc -l
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`${PROJECT_ROOT}/specs/ch07.spec`

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束 |
| 3 | 通用约束·禁止 | ✓ | 通用约束 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求 |
| 6 | 章节结构 | ✓ | 章节骨架（节7.1→5.7）|
| 7 | 源码引用格式 | ✓ | 章节骨架各节锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束→排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单头部 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束→排除范围 |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 3/3 = **21/21**
