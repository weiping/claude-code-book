# 第 37 章写作计划

**批次**：批次 10a（来自 plans/batch.md）
**章节**：ch37 — 第37章：Plugin 生命周期——发现、安装、更新与卸载
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/services/plugins/PluginInstallationManager.ts` 如何在后台自动检查并安装插件，以及 pending/installing/installed/failed 四个状态如何映射到 AppState 的 UI 更新。

## 通用约束
**排除范围**：Marketplace 协议（第38章）、插件包结构（第39章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「后台安装事件驱动 UI 更新」（Background Install Event-Driven UI）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（performBackgroundPluginInstallations + updateMarketplaceStatus），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单
| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/services/plugins/PluginInstallationManager.ts | performBackgroundPluginInstallations(60行)、updateMarketplaceStatus(30行)、安装状态事件 switch(104行) | 全文 |

```bash
wc -l src/services/plugins/PluginInstallationManager.ts
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：插件安装是一个耗时的后台操作——下载、验证、注册需要几秒甚至几十秒。Claude Code 用事件驱动模型把安装进度实时映射到 UI 状态，而不是阻塞等待完成。

### 节 37.1：问题
- 核心主张：**后台安装操作与 UI 之间需要松耦合——安装器不应该知道 UI 的实现细节，UI 也不应该阻塞等待安装完成**
- 源码锚点：`src/services/plugins/PluginInstallationManager.ts:2`（注释：背景安装管理器，映射安装事件到 AppState 更新）
- 包含元素：**图37-1** — 安装状态流转（pending→installing→installed/failed）和 UI 更新时序

### 节 37.2：源码实例1 — performBackgroundPluginInstallations（事件流消费）
- 核心主张：**performBackgroundPluginInstallations 先 diff 找出需要安装的内容，然后消费安装事件流，每个事件更新 AppState**
- 源码锚点：`src/services/plugins/PluginInstallationManager.ts:60、66（diff）、83（初始化pending）、104（switch events）`

### 节 37.3：源码实例2 — updateMarketplaceStatus（AppState 更新模式）
- 核心主张：**updateMarketplaceStatus 是一个 AppState 更新工厂——按 name 定位、spread 更新、不修改其他字段**
- 源码锚点：`src/services/plugins/PluginInstallationManager.ts:30、40-43`

### 节 37.4-37.7：模式剖析/适用范围/权衡/与已知模式对话
- 模式：事件驱动状态更新、安装状态机、后台操作透明化
- 与 Observer 模式：安装器是发布者，AppState 更新器是订阅者

### 章末：模式提炼
```
模式名称：后台安装事件驱动 UI 更新（Background Install Event-Driven UI）
问题：后台安装操作需要实时反映进度到 UI，但不能阻塞主线程
解决方案：安装器产生事件流（installing/installed/failed），消费方 switch 处理每个事件更新 AppState
前置条件：安装操作可以表达为事件序列
源码锚点：src/services/plugins/PluginInstallationManager.ts:60 (performBackgroundPluginInstallations), :104 (switch events)
```

### 章末：你能做什么
1. 用事件流而非回调传递安装进度，让消费方按需处理各阶段事件
2. 在安装开始前先计算 diff（找出需要安装的内容），一次性初始化 pending 状态，避免 UI 闪烁
3. 安装完成后自动刷新插件列表，修复"插件刚安装但工具还不可见"的 race condition

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图37-1 | 插件安装状态机 | Mermaid stateDiagram | pending→installing→installed/failed | 37.1 |

## 写作前 checklist
```bash
sed -n '2p' src/services/plugins/PluginInstallationManager.ts   # 注释
sed -n '30p' src/services/plugins/PluginInstallationManager.ts  # updateMarketplaceStatus
sed -n '60p' src/services/plugins/PluginInstallationManager.ts  # performBackgroundPluginInstallations
sed -n '104p' src/services/plugins/PluginInstallationManager.ts # switch events
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
