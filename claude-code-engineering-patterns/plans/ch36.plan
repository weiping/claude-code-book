# 第 36 章写作计划

**批次**：批次 8b（来自 plans/batch.md）
**章节**：ch36 — permissionSync——多智能体间的权限同步协议
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/utils/swarm/permissionSync.ts`（928行）在并发 Agent 场景中如何保持 Leader 和多个 Teammate 之间的权限状态一致性，以及乐观锁或消息广播在此的应用。

## 通用约束
**排除范围**：leaderPermissionBridge（第 34 章）、第33-36章全局权限系统、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「邮箱消息传递权限协商」（Mailbox-Based Permission Negotiation）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（请求流程 + 响应流程），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/utils/swarm/permissionSync.ts | 文件头部注释(1-18行，6步流程)、SwarmPermissionRequestSchema(47-100行)、SwarmPermissionRequest 类型 | 1-100 |
| 2 | src/utils/swarm/permissionSync.ts | lockfile 使用(27行)、writeToMailbox调用 | 关键行 |

```bash
head -100 src/utils/swarm/permissionSync.ts
grep -n "lockfile\|writeToMailbox\|readMailbox\|pollMailbox" src/utils/swarm/permissionSync.ts | head -15
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：当 3 个并发的 Teammate Agent 同时遇到需要权限确认的操作，如何避免 3 个对话框同时弹给用户？Claude Code 的答案是：通过邮箱系统路由权限请求——所有请求排队通过 Leader 的 UI，用户只需要面对一个入口。

### 节 36.1：问题
- 核心主张：**分布式权限同步的核心挑战是"多写一读"——多个 Teammate 可能同时发起权限请求，但 Leader UI 是单一入口，并发请求必须序列化**
- 源码锚点：`src/utils/swarm/permissionSync.ts:1`（文件头部注释，完整的 6 步流程描述）
- 包含元素：**图36-1** — Mermaid 序列图，Worker→mailbox→Leader→mailbox→Worker 的 6 步流程
- 估计字数：600-800字

### 节 36.2：源码实例1 — SwarmPermissionRequestSchema（消息格式的工程决策）
- 核心主张：**SwarmPermissionRequestSchema 用 Zod 定义消息协议——id/workerId/toolName/status/resolvedBy 这些字段的精心设计，体现了"消息即合约"的思想**
- 源码锚点：
  - `src/utils/swarm/permissionSync.ts:47`（SwarmPermissionRequestSchema Zod 定义）
  - `src/utils/swarm/permissionSync.ts:61`（toolName/toolUseId/description/input 字段）
  - `src/utils/swarm/permissionSync.ts:69`（permissionSuggestions/status/resolvedBy/resolvedAt/feedback/updatedInput/permissionUpdates）
  - `src/utils/swarm/permissionSync.ts:27`（`import * as lockfile` — 文件系统锁）
- 概念层次：status 枚举（pending/approved/rejected）、resolvedBy 字段（worker vs leader 解析差异）、lockfile 防并发写入
- 估计字数：900-1100字

### 节 36.3：源码实例2 — writeToMailbox（异步消息传递实现）
- 核心主张：**permissionSync 通过文件系统 mailbox（writeToMailbox）而非共享内存传递权限请求——文件系统持久化保证进程崩溃后请求不丢失**
- 源码锚点：
  - `src/utils/swarm/permissionSync.ts:43`（`import { writeToMailbox }` from teammateMailbox.js）
  - `src/utils/swarm/permissionSync.ts:7`（注释："Workers send permission requests to the leader's mailbox"）
- 概念层次：文件系统 vs 内存队列的权衡（持久化 vs 速度）、mailbox 路径的命名约定
- 估计字数：700-900字

### 节 36.4：模式剖析
- 核心主张：**邮箱权限协商的本质是"请求-响应通过持久化队列解耦"——发送方和接收方不需要同时在线，持久化文件保证可靠性**
- 估计字数：500-700字

### 节 36.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 多 Agent 并发发起权限请求 | ✓ | mailbox 自然队列化，避免 UI 并发弹框 | 共享内存队列（但跨进程不可用）|
| 需要进程崩溃后恢复请求 | ✓ | 文件系统持久化，进程重启后消息仍在 | 内存队列（崩溃丢失）|
| 低频权限请求（<1/秒）| ✓ | 文件系统 IO 开销可接受 | N/A |
| 高频权限请求（>10/秒）| ✗（谨慎）| 文件系统读写 IO 成为瓶颈（推断）| 内存队列 |

### 节 36.6：权衡与局限
- 文件系统 IO 延迟（mailbox 轮询周期）、lockfile 竞争（高并发时可能 retry）、消息积压时的清理策略
- 估计字数：500-700字

### 节 36.7：与已知模式的对话
- 与 EIP 消息通道（Message Channel）：最接近——发送者和接收者通过持久化通道解耦；差异是 EIP 通常用消息中间件，本模式用文件系统
- 与 GoF 观察者：相同——状态变化通知；不同——观察者是推送，本模式是轮询（Worker 轮询 mailbox 等待响应）

### 章末：模式提炼
```
模式名称：邮箱权限协商（Mailbox-Based Permission Negotiation）
问题：多 Agent 并发发起权限请求，Leader UI 是单入口，需要序列化处理
解决方案：通过文件系统 mailbox 路由请求，Worker 写入请求，Leader 读取处理，Worker 轮询响应
前置条件：有共享的文件系统（同机器），mailbox 路径约定明确（按 agentName 路由）
源码锚点：src/utils/swarm/permissionSync.ts:1 (6步流程注释), :47 (SwarmPermissionRequestSchema)
```

### 章末：你能做什么
1. 用文件系统 mailbox 实现跨进程异步消息传递，获得自然的持久化和进程隔离
2. 用 Zod 定义消息协议 Schema，让发送方和接收方有统一的类型合约
3. 在消息中包含 resolvedBy 字段，区分"被 Leader 解析"和"被 Worker 自己解析"的不同路径
4. 用 lockfile 防止多个 Worker 同时写入 mailbox 时的文件竞争
5. 设计 status 状态机（pending→approved/rejected），让消息有明确的生命周期

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图36-1 | 权限协商 6 步流程 | Mermaid sequenceDiagram | Worker→Leader mailbox→Leader UI→Worker mailbox→Worker | 32.1 |

## 写作前 checklist
```bash
sed -n '1p' src/utils/swarm/permissionSync.ts   # 文件说明
sed -n '4p' src/utils/swarm/permissionSync.ts   # 6步流程注释开始
sed -n '27p' src/utils/swarm/permissionSync.ts  # lockfile 导入
sed -n '47p' src/utils/swarm/permissionSync.ts  # SwarmPermissionRequestSchema
sed -n '61p' src/utils/swarm/permissionSync.ts  # toolName/toolUseId
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
