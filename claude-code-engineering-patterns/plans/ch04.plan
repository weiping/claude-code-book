# 第 4 章写作计划

**批次**：批次 2a（来自 plans/batch.md）
**章节**：ch04 — 用户输入分流——`processUserInput` 的路由决策树
**风格**：hunter

## 章节意图（从 spec `## 意图` 提取）

追踪用户输入从进入系统到完成分流的完整路径：斜杠命令、普通消息、特殊模式三条分支的判断逻辑，以及"路由决策先于任何业务逻辑"这一架构原则如何体现在 `src/utils/processUserInput/processUserInput.ts` 中。读者读完后能在自己的 Agent 系统中复用这一输入分流模式。

## 通用约束（从 spec `## 约束` 提取，不含风格专属项）

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 不得引用排除范围中的 stub 或未实现模块
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（必须标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容（用交叉引用代替）

**排除范围**（从 spec `## 排除范围` 提取）：
- 斜杠命令注册与加载细节（第 5 章）
- QueryEngine 主循环实现（第 8 章）
- src/assistant/、src/ssh/、src/server/、src/proactive/（排除范围 stub）

## ⛔ 风格专属写作要求

**开篇格式**：hunter — 模式预告（150-200字，问题场景+模式预告+价值承诺），⛔ 开篇禁止直接引用源码

**分节组织**：hunter — `## [模式预告开篇]` → `## 问题` → `## 源码实例 1` → `## 源码实例 2（变体）` → `## 模式剖析` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 你能做什么`

**章末结构**：hunter — 模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据）

**风格专属必须**：
- ⛔ 每节开篇格式与 hunter 风格匹配（模式预告，不引用源码）
- ⛔ 展示至少 2 处源码实例，证明模式的普遍性
- 包含适用范围表（什么场景用/不用该模式）
- 包含权衡与局限分析
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）

**风格专属禁止**：
- ⛔ 开篇不得直接引用源码路径或行号（开篇是问题场景+模式预告+价值承诺，150-200字）
- 全章不得混用其他风格的写作手法

**通用写作铁律**：
- ⛔ 源码第一：每个关于代码行为的断言必须附 `路径:行号` 证据，禁止基于 AI 记忆写作
- ⛔ 代码无裸露：禁止"标题→代码→下一个标题"，每个代码块必须有引入段+解读段（至少 2 句/行代码）
- ⛔ 数据驱动：不用"很多"/"通常"等模糊词，用具体数字或源码常量名
- ⛔ 术语标准化：首次出现技术术语时标注原文（中文（English Full Name, ABBR））
- ⛔ 开篇铁律：开篇在全章初稿完成后最后补写，禁止在开篇直接引用源码

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/utils/processUserInput/processUserInput.ts | processUserInput 函数签名（参数列表）和三条分流分支 | 行 85-160, 460-560 |
| 2 | src/utils/processUserInput/processUserInput.ts | processUserInputBase 函数的路由决策树 | 行 281-340 |
| 3 | src/utils/processUserInput/processSlashCommand.tsx | 斜杠命令路由入口 | 行 1-60 |
| 4 | src/utils/processUserInput/processBashCommand.tsx | Bash 命令路由入口 | 行 1-50 |
| 5 | src/utils/processUserInput/processTextPrompt.ts | 普通文本路由入口 | 行 1-50 |

验证文件存在：
```bash
ls src/utils/processUserInput/processUserInput.ts \
   src/utils/processUserInput/processSlashCommand.tsx \
   src/utils/processUserInput/processBashCommand.tsx \
   src/utils/processUserInput/processTextPrompt.ts
```

## Spec 验收场景清单

**验收优先级**：E（风格符合性）> A（源码锚点）> B（语言精确性）= C（结构完整性）> D（图表与代码）

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用应存在于实际文件中 | 章节骨架各节 | ☐ |
| A2 | 推断有标注 | 每处推断有"（推断）"标注 | 全章 | ☐ |
| A3 | 不引用排除范围 | 没有任何引用落在排除范围中 | 排除范围声明 | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 M 章"代替重复 | 斜杠命令/QueryEngine 处 | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| B1 | 禁止空洞表述 | 不存在"介绍了"/"描述了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | "很多"/"通常"等模糊词已替换为具体数字 | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现时有原文标注 | 全章 | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| C1 | 概念递进层次 | hunter: 问题→实例→模式剖析递进 | 节 4.1→4.2→4.3→4.4 | ☐ |
| C2 | hunter 专属结构 | 模式命名框（五层：名+问题+方案+条件+锚点） | 节 4.4 模式剖析 | ☐ |
| C3 | 核心主张可提炼 | 可提炼"路由决策先于业务逻辑"模式 | 章末模式提炼 | ☐ |
| C4 | 设计权衡有说明 | 有"选 A 不选 B 的原因"分析 | 节 4.6 权衡与局限 | ☐ |
| C5 | 章末行动建议 | 5-8 条行动动词开头的建议 | 章末"你能做什么" | ☐ |
| C6 | 章节间链接 | 章末向后链接 + 章节内向前链接 | 各节章末 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| D1 | 图表可渲染且达标 | Mermaid 标注正确，数量 ≥2 | 图表清单 | ☐ |
| D2 | 图表标注格式 | 上方有 `**图 N-X：标题**`，下方有说明 | 图表清单 | ☐ |
| D3 | 代码片段长度 | ≤25 行，前后有说明 | 源码实例节 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开工程逻辑 | 源码实例节 | ☐ |
| D5 | hunter 专属图表 | 适用范围表（✓/✗ 格式）存在 | 节 4.5 适用范围 | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求（从 spec 提取） | 对应 plan 节 | 覆盖 |
|---|--------|----------------------|-------------|------|
| E1 | hunter 开篇格式 | 150-200字，三要素：问题场景+模式预告+价值承诺，不引用源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"而非"用户"，有预告句和设问句 | 全章 | ☐ |
| E3 | 关键信息突出 | 关键结论加粗，对比信息用表格 | 全章 | ☐ |

## 章节骨架

### 开篇（hunter 模式预告，最后补写）
- 可选题词：`// Runs before attachment extraction so this path matches the slash-command path` — 注释本身揭示了路由的时序铁律
- 开篇三要素（150-200字）：
  - **问题场景**：Agent 系统收到一条字符串，这条字符串可能是命令、普通消息、触发词——在你决定如何处理它之前，必须先判断它"是什么"。一个常见的错误是：在分流判断之前就开始附件提取、权限检查、状态变更——这让分流本身变成了有副作用的操作，几乎无法测试。
  - **模式预告**：代码库中有一个反复出现的模式——"前置路由"：先判断类型，再懒加载处理器，完全无副作用。
  - **价值承诺**：读完本章，你能识别并复用这一模式，构建自己的多输入-类型 Agent 分流器。
- ⛔ 禁止直接引用源码

### 节 4.1：问题——Agent 输入的二义性困境
- 核心主张：同一个字符串 `/help` 在不同 mode 下有完全不同的含义——这要求分流逻辑必须先于所有副作用执行
- 源码锚点：`src/utils/processUserInput/processUserInput.ts:85` — 函数签名 mode 参数；`src/utils/processUserInput/processUserInput.ts:145` — mode 判断优先于业务
- 概念层次：
  - **层次1 定义**：PromptInputMode 的三种值（prompt/bash/special）及其语义差异
  - **层次2 设计意图**：为什么需要在函数入口就决定分流路径，而非在各处理器内部判断
- 权衡分析：为什么不在 processSlashCommand 内部再做 mode 检查——会造成 N×M 的判断组合爆炸
- 包含元素：PromptInputMode 类型定义代码片段（≤15行）
- 内容密度规划：
  - 引入段：说明为什么 Agent 输入分流是反直觉的（输入不是"类型化"的，只是字符串）
  - 解读段：分析 mode 参数出现在函数签名最顶层的工程含义（不是实现细节，是设计宣言）
  - 散文与代码比例：70% 散文
- 估计字数：800-1000 字
- 对应 spec 场景：C1（概念递进）、C4（权衡分析）

### 节 4.2：源码实例 1——三条分支的实际路由
- 核心主张：`processUserInputBase` 的路由逻辑呈现严格的优先级链：Ultraplan 特殊模式 → Bash 命令 → 斜杠命令 → 普通文本
- 源码锚点：
  - `src/utils/processUserInput/processUserInput.ts:469` — Ultraplan 分支（模式识别+特性开关）
  - `src/utils/processUserInput/processUserInput.ts:517` — Bash 分支（mode === 'bash'）
  - `src/utils/processUserInput/processUserInput.ts:536` — 斜杠命令分支（startsWith('/')）
  - `src/utils/processUserInput/processUserInput.ts:554` — 普通文本分支（兜底）
- 概念层次：
  - **层次1 实例**：4 个 if 分支的代码，各约 5-8 行
  - **层次2 模式特征**：懒加载（dynamic import）在每个分支内，意味着主路由函数无需提前加载任何处理器
- 权衡分析：为什么是 if-chain 而非 strategy pattern——分支间有优先级且互斥，chain 比 strategy map 更清晰
- 包含元素：
  - 路由逻辑代码片段（≤25行，展示4个分支的判断条件）
  - Mermaid 决策树图（**图 4-1**）
- 内容密度规划：
  - 引入段：先建立"为什么需要优先级链而非并列判断"的概念
  - 解读段：逐分析每个判断条件的设计含义（为什么 Ultraplan 最先，为什么 bash 用 mode 而非内容匹配）
  - 散文与代码比例：60% 散文
- 估计字数：1000-1400 字
- 对应 spec 场景：A1（源码引用有效性）、D1-D3（图表与代码）、E2（读者对话感）

### 节 4.3：源码实例 2（变体）——懒加载处理器的设计
- 核心主张：每条分支内用 `dynamic import` 懒加载对应处理器，这不是性能优化，是架构隔离——路由层不依赖处理器的实现细节
- 源码锚点：
  - `src/utils/processUserInput/processUserInput.ts:480` — `const { processSlashCommand } = await import('./processSlashCommand.js')`
  - `src/utils/processUserInput/processUserInput.ts:518` — `const { processBashCommand } = await import('./processBashCommand.js')`
- 概念层次：
  - **层次1 实例**：两个 dynamic import 的对比（相同模式，不同目标）
  - **层次2 模式特征**：处理器对路由层完全透明，router 不知道 slash handler 的内部实现
- 权衡分析：为什么不在文件顶部 static import——防止处理器模块的副作用（如注册逻辑）在路由阶段提前执行
- 包含元素：两个 dynamic import 的并排对比代码（≤20行）
- 内容密度规划：
  - 引入段：先问"为什么不直接 import？"引出懒加载的动机
  - 解读段：解析 dynamic import 如何实现处理器与路由层的物理隔离
  - 散文与代码比例：65% 散文
- 估计字数：800-1000 字
- 对应 spec 场景：A1（源码引用有效性）、C2（hunter 专属结构）

### 节 4.4：模式剖析——前置路由（Pre-dispatch Router）
- 核心主张：提炼可命名的工程模式："前置路由"——在所有副作用之前完成输入类型判断
- 包含元素：
  - **模式命名框**（五层结构）：
    ```
    模式名称：前置路由（Pre-dispatch Router）
    问题：Agent 接收多类型输入（命令/普通文本/特殊模式），需要在执行任何副作用前确定处理路径
    解决方案：将类型判断提升到函数入口，以参数（mode）或内容特征（startsWith）为唯一依据，懒加载对应处理器
    前置条件：输入类型判断逻辑可以静态表达（无需 I/O）；处理器之间互斥且优先级明确
    源码锚点：src/utils/processUserInput/processUserInput.ts:469-554
    ```
  - 模式适用范围表（✓/✗ 格式，见节 4.5）
- 估计字数：600-800 字
- 对应 spec 场景：C2（hunter 专属模式命名框）、C3（核心主张可提炼）

### 节 4.5：适用范围
- 包含元素：适用范围表

| 场景 | 适用 | 理由/替代方案 |
|------|------|------------|
| 输入有多种明确语义类型 | ✓ | 类型可以静态区分，无需 I/O |
| 各类型处理路径完全互斥 | ✓ | 不存在"先执行 A 再执行 B"的序列需求 |
| 类型判断依赖外部状态（如数据库） | ✗ | 此时改用 Command Bus 或 Strategy Pattern |
| 类型之间有共享前置步骤 | ✗ | 改用 Template Method，抽出共享前置步骤 |
| 需要运行时动态增加类型 | ✗ | 改用插件注册表（详见第 37 章 Plugin 系统） |

- 估计字数：400-600 字
- 对应 spec 场景：D5（hunter 专属适用范围表）

### 节 4.6：权衡与局限
- 核心要点：
  - **适用边界**：分支数 ≤6 时 if-chain 清晰，超过后考虑 strategy map
  - **潜在风险**：优先级链的顺序决定行为，调整顺序会产生 breaking change
  - **性能影响**：dynamic import 引入微量异步开销，在高频场景需评估
  - **替代方案**：Command Bus（EventBridge）适用于需要中间件的场景
- 估计字数：500-700 字
- 对应 spec 场景：C4（设计权衡）

### 节 4.7：与已知模式的对话
- 对比目标：GoF Chain of Responsibility（职责链）模式
- 对比要点：
  - **相同点**：都是将请求沿链传递，直到某个处理器接受
  - **不同点**：GoF Chain 中处理器可以"传递"请求给下一个；processUserInput 中分支互斥——一旦匹配即处理，无需传递
  - **Claude Code 的选择**：使用 if-chain 而非 handler 链表，原因是优先级语义更清晰，且不需要处理器之间的引用关系
- 估计字数：400-600 字
- 对应 spec 场景：C2（hunter 专属结构中的"与已知模式的对话"节）

### 章末结构：模式提炼（hunter）

**模式 1：前置路由（Pre-dispatch Router）**
- 模式名：前置路由（Pre-dispatch Router）
- 解决的问题：多类型输入在执行业务逻辑前需要确定处理路径
- 核心做法：将类型判断提升到函数入口（mode 参数 + 内容特征），懒加载对应处理器
- 前置条件：类型判断逻辑无 I/O 副作用；处理路径互斥
- 源码证据：`src/utils/processUserInput/processUserInput.ts:469-554`

**模式 2：懒加载隔离（Lazy-load Isolation）**
- 模式名：懒加载隔离（Lazy-load Isolation）
- 解决的问题：路由层不应依赖处理器的实现细节（防止副作用泄漏）
- 核心做法：在每个分支内使用 dynamic import 加载处理器，而非在模块顶部 static import
- 前置条件：处理器是独立模块，加载本身无副作用
- 源码证据：`src/utils/processUserInput/processUserInput.ts:480,518`

**模式 3：参数化模式分流（Parameterized Mode Dispatch）**
- 模式名：参数化模式分流（Parameterized Mode Dispatch）
- 解决的问题：同一输入字符串在不同 mode 下语义不同
- 核心做法：mode 作为显式参数传入分流函数，而非从全局状态读取
- 前置条件：mode 在调用方已知（不需要运行时推断）
- 源码证据：`src/utils/processUserInput/processUserInput.ts:85`（函数签名 mode 参数）

### 章末：你能做什么

预规划 6 条行动建议（写作后补全细节）：
1. 在自己的 Agent 系统中，将输入类型判断提升到统一入口函数（而非分散在各处理器内）
2. 用显式 mode 参数替代从全局状态推断输入模式的做法
3. 为每种输入类型编写独立的处理器模块，通过 dynamic import 在路由时懒加载
4. 当分支数超过 6 时，考虑将 if-chain 重构为 strategy map（key=类型，value=处理器工厂）
5. 在路由函数的单元测试中，验证每个分支的路由判断，而非测试业务逻辑（各处理器独立测试）
6. 避免在路由层执行任何 I/O 或状态变更——保持路由函数的副作用自由

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图 4-1 | processUserInput 路由决策树 | Mermaid flowchart | 展示从输入到四条分支（Ultraplan/Bash/Slash/Text）的判断条件和优先级 | 4.2 |
| 图 4-2 | 前置路由模式 vs 嵌套判断反模式 | 对比表格 | 两列：前置路由（✓）vs 处理器内部判断（✗），各 4 行对比 | 4.4 |

## 写作顺序

1. ⛔ 确认 STYLE_ID=hunter + 重新读取完整 spec（`specs/ch04.spec`）
2. 先写节 4.2（源码实例1，展示三条分支的实际代码）
3. 再写节 4.3（源码实例2，懒加载变体）
4. 写节 4.1（问题节，铺垫语境）
5. 写节 4.4-4.7（模式剖析、适用范围、权衡、对话）
6. 画图表（先 ASCII 草图，确认结构后转 Mermaid）
7. 写章末模式提炼（hunter 格式）
8. 写"你能做什么"（将模式转化为行动建议）
9. 最后补写开篇（此时全章内容已知）

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
# 验证核心行号
sed -n '85p' src/utils/processUserInput/processUserInput.ts   # processUserInput 函数入口
sed -n '469p' src/utils/processUserInput/processUserInput.ts  # Ultraplan 分支
sed -n '517p' src/utils/processUserInput/processUserInput.ts  # Bash 分支
sed -n '536p' src/utils/processUserInput/processUserInput.ts  # Slash 分支
sed -n '480p' src/utils/processUserInput/processUserInput.ts  # processSlashCommand dynamic import
sed -n '518p' src/utils/processUserInput/processUserInput.ts  # processBashCommand dynamic import
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：`${PROJECT_ROOT}/specs/ch04.spec`

### 对齐检查清单

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束 |
| 3 | 通用约束·禁止 | ✓ | 通用约束 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求 |
| 6 | 章节结构 | ✓ | 章节骨架（节4.1→4.7）|
| 7 | 源码引用格式 | ✓ | 章节骨架各节锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束→排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单头部 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束→排除范围 |

**验收场景覆盖统计**：A 组 4/4 + B 组 3/3 + C 组 6/6 + D 组 5/5 + E 组 3/3 = **21/21**
