# 第 4 章写作计划

**批次**：批次 1（来自 plans/batch.md）
**章节**：ch04 — CLI 入口与模式分叉——从 main.tsx 到 REPL / Headless / SDK
**风格**：hunter

## 章节意图

追踪 Claude Code 从进程启动到模式分叉的完整路径：main.tsx 的三路并行启动序列（MDM 配置读取 / Keychain 预读取 / import 求值重叠）、Commander.js 命令解析树的结构，以及交互模式 / Headless（-p/--print）/ SDK / Assistant（feature('KAIROS') 门控）四种运行模式的分叉逻辑。读者读完后能在自己的 Agent CLI 项目中复用这一"并行启动 + 模式分叉"模式。

## 通用约束

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 开篇不得直接引用源码路径或行号
- 不得出现"介绍了…"、"描述了…"、"讲解了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测（标注「（推断）」）
- 不得重复已在其他章节详细讲解的内容

**排除范围**：
- src/assistant/（stub 未实现）
- src/ssh/、src/server/、src/proactive/（stub）
- src/buddy/、src/voice/（UI 细节）
- TypeScript 语法讲解、React 基础、Bun 运行时基础

## ⛔ 风格专属写作要求

**开篇格式**：hunter 模式预告（150-200字）：问题场景 + 模式预告 + 价值承诺。⛔ 不得直接引用源码。描述 CLI 工具启动时"如何在 0 到第一个字符渲染之间并行做尽可能多的事"的工程挑战，预告"并行启动 + 模式分叉"模式。

**分节组织**：hunter 风格按模式实例命名：
`## [模式预告开篇]` → `## 问题：CLI 启动的工程挑战` → `## 源码实例 1：三路并行启动序列` → `## 源码实例 2：四种模式分叉` → `## 模式剖析：并行启动 + 模式分叉` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 模式提炼` → `## 你能做什么`

**章末结构**：模式提炼（3-5 个命名模式，五层结构：模式名→解决的问题→核心做法→前置条件→源码证据），然后接"你能做什么"（5-8 条行动动词开头的建议）。

**风格专属必须**：
- 展示至少 2 处源码实例，证明模式的普遍性（实例 2 明确指出与实例 1 的不同之处）
- 有适用范围表（场景→适用/不适用→理由→替代方案）
- 包含与已知业界模式的对话（至少引用一个 GoF/POSA/EIP 模式）
- 模式有中文+英文命名（3-5 字中文）
- 章节总长 2000-3000 字

**风格专属禁止**：
- 不只讲一个例子（无法证明是"模式"）
- 不忽略缺陷和风险
- 不出现纯理论讨论而没有源码佐证

**通用写作铁律**：
- ⛔ 源码第一：每个断言附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块有引入段+解读段
- ⛔ 数据驱动：不用模糊词，用具体数字
- ⛔ 开篇铁律：开篇在初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/main.tsx | 第1-25行：三路并行启动注释与副作用 | 1-100 |
| 2 | src/main.tsx | startDeferredPrefetches()：行388起 | 388-450 |
| 3 | src/main.tsx | feature('KAIROS') 条件导入：行78-81 | 78-100 |
| 4 | src/setup.ts | setup() 函数主体 | 1-100 |
| 5 | src/replLauncher.tsx | launchRepl() 入口 | 1-50 |
| 6 | src/cli/print.ts | headless 模式入口 | 1-50 |

验证文件存在：
```bash
ls src/main.tsx src/setup.ts src/replLauncher.tsx src/cli/print.ts
```

## Spec 验收场景清单

**验收优先级**：E > A > B = C > D

### A. 源码锚点组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效性 | 每处 `路径:行号` 引用存在于实际文件 | 节 4.1-4.4 | ☐ |
| A2 | 推断有标注 | 推断标注「（推断）」 | 所有节 | ☐ |
| A3 | 不引用排除范围 | 无 src/assistant/ 等排除路径 | 写作前 checklist | ☐ |
| A4 | 不重复其他章内容 | 用"详见第 X 章"代替 | 所有节 | ☐ |

### B. 语言精确性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 无"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 用具体行数/函数名替代模糊词 | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现标注原文 | 全章 | ☐ |

### C. 结构完整性组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | 每概念至少两层递进（定义→设计意图→案例） | 节 4.1-4.4 | ☐ |
| C2 | ⛔ hunter 结构完整性 | ≥2处实例+适用范围表+与已知模式对话+中英文命名 | 节 4.2-4.3 + 适用范围 + 对话节 | ☐ |
| C3 | 核心主张可提炼 | 可从开篇提炼1-2句答案 | 开篇节 | ☐ |
| C4 | 设计权衡有说明 | 有"选A不选B"说明 | 权衡与局限节 | ☐ |
| C5 | 章末行动建议 | 5-8条行动动词开头建议 | 你能做什么节 | ☐ |
| C6 | 章节间链接 | 章末向后链接+章节内向前链接 | 全章 | ☐ |

### D. 图表与代码组（铁律）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | ≥3个图表（章节2000-4000字） | 图表清单 | ☐ |
| D2 | 图表标注格式 | 上方 **图4-X：标题**，下方说明 | 所有图表 | ☐ |
| D3 | 代码片段长度 | ≤25行，前后有说明 | 所有代码块 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号，再展开 | 节 4.1 | ☐ |
| D5 | ⛔ hunter 图表 | 架构关系图(graph TD/LR)+适用场景决策图(flowchart) | 图 4-1, 4-3 | ☐ |

### E. 风格符合性组（⛔ 最高优先级）

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 150-200字，问题场景+模式预告+价值承诺，不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有预告和设问 | 全章 | ☐ |
| E3 | 关键信息突出 | 加粗+表格 | 全章 | ☐ |

## 章节骨架

### 开篇（最后补写）
> ⛔ 全章写完后才补写开篇。hunter 模式预告公式：
> "在代码库中，N 处地方用同样方式解决问题" + 模式名称预告（150-200字，问题场景+模式预告+价值承诺）

- 可选题词：`// 1. profileCheckpoint marks entry before heavy module evaluation begins` (src/main.tsx:2)

### 节 4.1：三路并行启动序列
- 核心主张：main.tsx 在 import 求值期间并行执行 MDM + Keychain 两路 I/O，以时间换性能（对应 C1、D4）
- 源码锚点：`src/main.tsx:2-20` — 三路并行的注释和副作用，`sed -n '2,20p' src/main.tsx` 确认存在
- 概念层次：① 定义（并行启动的三路任务）② 设计意图（import 求值期间的 ~135ms 窗口）③ 代码实例（profileCheckpoint + startMdmRawRead + startKeychainPrefetch）
- 权衡分析：为什么不用 Promise.all 而用模块顶层副作用？→ 利用 Bun 的模块求值阶段（import 是同步阻塞的，此时 CPU 已空闲）
- 包含元素：代码片段（main.tsx:12-20，≤15行）+ Mermaid 时序图（并行启动序列，图4-1）
- **⛔ 内容密度规划**：
  - 引入段：为什么 CLI 工具的启动性能很重要？用户在 150ms 内感受不到延迟，但超过就有感知——所以"找到那 150ms 并塞满它"
  - 解读段：注释第2-8行明确说明了设计意图，这是"自文档化"的示范——引用注释+解释工程决策
- 估计字数：500-700字（散文≥65%）
- 对应 spec 场景：A1、C1、D3、D4

### 节 4.2：模式实例 1 — 四种模式分叉
- 核心主张：commander.js 解析完参数后，根据 flag 和 feature() 门控将执行路径分叉到 4 个独立入口（对应 C2、E1）
- 源码锚点：`src/main.tsx:78-81` — feature('KAIROS') 条件导入；`src/main.tsx:388` — startDeferredPrefetches；`src/replLauncher.tsx:1` — launchRepl 入口
- 概念层次：① 模式分叉的条件逻辑 ② 每种模式的入口文件 ③ feature() 门控的编译期消除
- 权衡分析：为什么用 feature() 而非运行时 if/else？→ 构建时 DCE 消除，详见第 3 章
- 包含元素：Mermaid flowchart（四种模式分叉决策树，图4-2）+ 代码片段（≤20行）
- **⛔ 内容密度规划**：
  - 引入段：同一个 claude 命令如何变成四种完全不同的程序？这是大型 CLI 工具的核心工程问题
  - 解读段：分叉不仅是代码路径，每种模式有完全不同的 I/O 模型（TTY 渲染 vs NDJSON vs SDK API）
- 估计字数：600-800字（散文≥60%）
- 对应 spec 场景：A1、C2、D1、D2

### 节 4.3：模式实例 2 — deferred prefetch 的延迟优化
- 核心主张：首次渲染后才执行的 startDeferredPrefetches()，是并行启动模式在"渲染后阶段"的变体（证明模式普遍性）
- 源码锚点：`src/main.tsx:388` — startDeferredPrefetches 函数定义
- 概念层次：① prefetch 的触发时机 ② 与启动序列的区别（启动时并行 vs 渲染后并行）③ 具体预取内容
- 权衡分析：为什么首次渲染后才执行？→ 避免阻塞 event loop，用户看到界面比数据预取更重要
- 包含元素：ASCII 架构图（启动时间线，图4-3）
- **⛔ 内容密度规划**：
  - 引入段（与实例1的区别）：如果说实例1是"在 import 阶段抢时间"，那么 deferred prefetch 是"在渲染后补作业"——两者形成完整的时间线优化策略
  - 解读段：列出具体的 prefetch 内容（initUser、getUserContext、prefetchAwsCredentialsIfSafe 等），量化收益
- 估计字数：400-600字（散文≥65%）
- 对应 spec 场景：A1、C2、D3

### 节 4.4：模式剖析——并行启动 + 模式分叉
- 核心主张：提炼"并行启动"和"模式分叉"两个可命名工程模式
- 包含元素：模式命名框（中英文，五层结构）× 2 + 适用范围表（场景→适用/不适用→理由→替代方案）
- 估计字数：400-500字
- 对应 spec 场景：C2、C3

### 节 4.5：权衡与局限
- 核心主张：并行启动的适用边界——进程启动时有效，长时间运行进程收益递减
- 包含元素：表格（优点 vs 缺点 vs 适用边界）
- 估计字数：200-300字
- 对应 spec 场景：C4

### 节 4.6：与已知模式的对话
- 对比：GoF Facade 模式（main.tsx 是 Facade，隐藏了三路并行的复杂性）；Template Method（四种模式分叉的骨架方法）
- 估计字数：200-300字
- 对应 spec 场景：C2

### 模式提炼
- 模式 1：并行启动（Parallel Startup）—— 在进程初始化期间并行执行 I/O 密集型准备工作
- 模式 2：模式分叉（Mode Fork）—— 在参数解析后，根据 flag 将控制流永久分叉到独立的执行路径
- 对应 spec 场景：C2、C3

### 你能做什么（5-8 条）
1. 识别你的 CLI 工具中 import 求值阶段能并行执行的 I/O 操作
2. 在 commander.js 解析前就启动 keychain 读取、配置加载等阻塞操作
3. 为多模式 CLI 设计独立的入口文件，避免 headless 模式引入 TTY 相关依赖
4. 用 feature() 或编译期 const 替代运行时 if/else 做功能门控，实现死代码消除
5. 将重要但非阻塞的预取操作（如模型能力缓存）延迟到首次渲染后执行
6. 为启动序列添加 `profileCheckpoint` 记录，在优化时有数据支撑

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图4-1 | main.tsx 三路并行启动时序 | Mermaid sequenceDiagram | 进程启动→import求值→三路并行→REPL就绪的时序 | 4.1 |
| 图4-2 | 四种运行模式分叉决策树 | Mermaid flowchart TD | Commander解析→flag判断→4种模式入口 | 4.2 |
| 图4-3 | 启动完整时间线（含 deferred prefetch） | ASCII box-arrow | 冷启动→首次渲染→deferred prefetch完成 | 4.3 |

## 写作顺序

1. ⛔ 读取完整 spec（specs/ch04.spec）和 DESIGN.md STYLE_ID=hunter
2. 先写节 4.1（三路并行启动序列）
3. 再写节 4.2（模式分叉）
4. 写节 4.3（deferred prefetch）
5. 写图表（先 ASCII 草图确认结构，再转 Mermaid）
6. 写节 4.4（模式剖析）+ 节 4.5（权衡）+ 节 4.6（与已知模式的对话）
7. 写模式提炼 + 你能做什么
8. 最后补写开篇（此时知道全章讲了什么）

## 写作前 checklist

```bash
sed -n '2,20p' src/main.tsx       # 三路并行启动注释和代码
sed -n '78,81p' src/main.tsx      # KAIROS feature gate
sed -n '388,410p' src/main.tsx    # startDeferredPrefetches
ls src/replLauncher.tsx src/cli/print.ts  # 模式分叉入口文件存在
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：specs/ch04.spec

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束·通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束·通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属写作要求·风格专属必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属写作要求·风格专属禁止 |
| 6 | 章节结构 | ✓ | 章节骨架（hunter 结构） |
| 7 | 源码引用格式 | ✓ | 各节·源码锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束·排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束·排除范围 |

**验收场景覆盖统计**：A(4/4) + B(3/3) + C(6/6) + D(5/5) + E(3/3) = **21/21**
