# 第 20 章写作计划

**批次**：批次 5b（来自 plans/batch.md）
**章节**：ch20 — CLAUDE.md 注入——层级化指令的加载与优先级
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/utils/claudemd.ts`（1479行）如何从当前目录向上扫描 CLAUDE.md 文件、合并多层级指令，以及层级优先级规则如何在团队协作场景中防止指令冲突。读者读完后能理解"层级化配置扫描"模式，并能在自己的工具中实现类似的多层级配置合并机制。

## 通用约束
**必须**：源码引用；含图；跨章"详见第X章"；使用"我们"
**禁止**：不引 stub；不空洞；无依据猜测标"（推断）"；不重复其他章
**排除范围**：记忆系统三层架构（第 23 章）、提示词组装完整流程（第 19 章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「层级化配置扫描」（Hierarchical Config Scan）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（目录扫描 + 多层合并），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/utils/claudemd.ts | 4层内存结构注释(1-16行)、getMemoryFiles(790行)、processMemoryFile(618行)、isClaudeMdExcluded(540行) | 1-20, 540-570, 618-650, 790-950 |
| 2 | src/memdir/paths.ts | isAutoMemoryEnabled(30行)、getMemoryBaseDir(85行) | 全文 |

```bash
ls src/utils/claudemd.ts src/memdir/paths.ts
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已在骨架中覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：当你在 `/home/user/project/src/` 下运行 Claude Code，它会向上扫描 4 个目录层级的 CLAUDE.md 文件——托管级、用户级、项目级、子目录级——每一层都可以覆盖上一层的指令。

### 节 20.1：问题
- 核心主张：**团队协作中，不同层级的参与者需要不同粒度的 AI 指令控制——全局管理员/用户/项目/子目录各有权威范围**
- 源码锚点：`src/utils/claudemd.ts:4`（注释说明4层结构：Managed/User/Project/子目录）
- 概念层次：4层定义（Managed=/etc/claude-code/CLAUDE.md，User=~/.claude/CLAUDE.md，Project=CLAUDE.md，子目录）、设计意图（层级权威分离）、实际场景
- 包含元素：**图20-1** — 4层目录结构示意图
- 估计字数：600-800字

### 节 20.2：源码实例1 — getMemoryFiles 的扫描逻辑
- 核心主张：**getMemoryFiles 是 memoize 缓存的——会话内只扫描一次，避免每次 API 请求都读文件系统**
- 源码锚点：
  - `src/utils/claudemd.ts:790`（`export const getMemoryFiles = memoize(`）
  - `src/utils/claudemd.ts:804`（managedClaudeMd — 第一层扫描）
  - `src/utils/claudemd.ts:827`（userClaudeMd — 第二层扫描）
  - `src/utils/claudemd.ts:886`（projectPath = join(dir, 'CLAUDE.md') — 项目级扫描）
  - `src/utils/claudemd.ts:899`（dotClaudePath = join(dir, '.claude', 'CLAUDE.md') — .claude 子目录）
- 概念层次：memoize 缓存设计、4层扫描顺序、.claude/rules/*.md 的额外扫描
- 估计字数：800-1000字

### 节 20.3：源码实例2 — isClaudeMdExcluded + processMemoryFile（过滤与处理）
- 核心主张：**isClaudeMdExcluded 让用户可以在配置文件中声明"跳过哪些 CLAUDE.md"——这是安全边界机制，防止恶意目录注入指令**
- 源码锚点：
  - `src/utils/claudemd.ts:540`（isClaudeMdExcluded — 检查 claudeMdExcludes 配置）
  - `src/utils/claudemd.ts:618`（processMemoryFile — 读取并解析单个 CLAUDE.md 文件）
  - `src/utils/claudemd.ts:634`（`if (isClaudeMdExcluded(filePath)) return null`）
- 概念层次：排除机制语义、processMemoryFile 的 truncation 逻辑、安全考量
- 估计字数：700-900字

### 节 20.4：模式剖析
- 核心主张：**层级化配置扫描的本质是"距离决定优先级"——子目录比父目录更了解当前上下文，应该有更高的覆盖权**
- 估计字数：500-700字

### 节 20.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 多层级权威（管理员/用户/项目）| ✓ | 层级结构自然表达权威范围 | 单一配置文件（但无法分层）|
| 配置需要就近覆盖 | ✓ | 子目录覆盖父目录 = 就近原则 | 环境变量（但不可版本控制）|
| 配置需要随代码版本控制 | ✓ | CLAUDE.md 可以 git commit | 数据库存储（但不跟代码走）|
| 高频读取（每次请求都要）| ✗（谨慎）| 文件系统读取有 IO 成本，需要 memoize | 内存配置（但不持久）|

### 节 20.6：权衡与局限
- memoize 导致会话内更新 CLAUDE.md 不生效、层级过多导致优先级难以推断
- 估计字数：500-700字

### 节 20.7：与已知模式的对话
- 与 Maven/npm 配置合并（就近原则）：相同——子目录配置优先；不同——Maven 是 override，本模式是 append（多层内容都保留，只是顺序不同）
- 与 Git 配置（system/global/local 三层）：相同——层级权威和就近覆盖；几乎完全一致，本模式是这一思想在 AI 指令上的应用

### 章末：模式提炼
```
模式名称：层级化配置扫描（Hierarchical Config Scan）
问题：不同层级的用户（管理员/用户/项目）需要有不同粒度的 AI 指令控制权
解决方案：按 Managed→User→Project→子目录 顺序扫描，子目录内容追加在父目录之后（就近优先）
前置条件：配置文件有统一的命名约定（CLAUDE.md）和明确的目录层级语义
源码锚点：src/utils/claudemd.ts:790 (getMemoryFiles), src/utils/claudemd.ts:4 (4层注释)
```

### 章末：你能做什么
1. 为工具的配置文件设计4层结构（系统级/用户级/项目级/子目录级），层间权威范围明确
2. 用 memoize 缓存配置文件扫描结果，避免每次请求都读文件系统
3. 实现 claudeMdExcludes 类型的排除机制，防止恶意目录注入配置
4. 用目录层级而非用户角色决定优先级——距离当前工作目录越近，优先级越高
5. 保持配置文件扫描结果为追加模式（不覆盖），让父目录指令仍然生效

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图20-1 | CLAUDE.md 4层扫描结构 | Mermaid flowchart | Managed→User→Project→子目录的扫描路径 | 16.1 |

## 写作顺序
1.读spec+DESIGN.md；2.读前置文件；3.写16.1含图；4.写16.2；5.写16.3；6.写16.4；7.写16.5；8.写16.6；9.写16.7；10.章末；11.开篇

## 写作前 checklist
```bash
sed -n '4p' src/utils/claudemd.ts    # 4层注释
sed -n '540p' src/utils/claudemd.ts  # isClaudeMdExcluded
sed -n '618p' src/utils/claudemd.ts  # processMemoryFile
sed -n '634p' src/utils/claudemd.ts  # 排除检查
sed -n '790p' src/utils/claudemd.ts  # getMemoryFiles = memoize
sed -n '804p' src/utils/claudemd.ts  # managedClaudeMd
sed -n '886p' src/utils/claudemd.ts  # projectPath
```

## ⛔ Spec 一致性声明
| 1-16 | 全部16项 | 均 ✓ | 各对应节 |

**⛔ 所有项目均已勾选 ✓，plan 审查通过。**
