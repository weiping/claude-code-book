# 第 13 章写作计划

**批次**：批次 3（来自 plans/batch.md）
**章节**：ch13 — API 通信层——Anthropic 客户端、重试机制与提示词缓存策略
**风格**：hunter

## 章节意图

分析模型层与 Anthropic API 通信的完整工程实现：src/services/api/claude.ts（3419 行）如何封装流式/非流式 API 调用、多提供商适配（first-party / Bedrock / Vertex）和用量追踪；src/services/api/withRetry.ts 的指数退避重试；以及 addCacheBreakpoints() / splitSysPromptPrefix() 如何精确放置缓存断点以最大化提示词缓存命中率。读者读完后能掌握"提示词缓存优先"这一 Claude Code 降低 API 成本的核心工程手段。

## 通用约束

**通用必须**：
- 每个核心概念有至少一处 `相对路径:行号` 格式的源码引用
- 包含至少一个可渲染的 Mermaid 流程图或 ASCII 架构图
- 跨章节引用使用"详见第 X 章"格式，不重复解释
- 使用"我们"而非"用户"/"读者"建立对话感

**通用禁止**：
- 开篇不得直接引用源码路径或行号
- 不得出现"介绍了…"、"描述了…"等空洞表述
- 不得对源码中没有依据的设计意图做无标注的猜测
- 不重复第 9 章（queryLoop）、第 11 章（模型选择）、第 19 章（提示词装配）的内容
- 不深入 Bedrock / Vertex 的具体云平台集成细节

**排除范围**：
- src/assistant/、src/ssh/、src/server/、src/proactive/（stub）
- AWS Bedrock / Azure / GCP 的具体云平台集成细节
- Anthropic API 协议本身的规范讲解

## ⛔ 风格专属写作要求

**开篇格式**：hunter 模式预告（150-200字）：问题场景 + 模式预告 + 价值承诺。⛔ 不得直接引用源码。描述"每次 API 调用都可能消耗数万 token，如何把成本压到最低"的工程挑战，预告"提示词缓存优先（Cache-First Prompt）"模式。

**分节组织**：
`## [模式预告开篇]` → `## 问题` → `## 源码实例 1：提示词缓存断点策略` → `## 源码实例 2：指数退避重试` → `## 模式剖析：提示词缓存优先` → `## 适用范围` → `## 权衡与局限` → `## 与已知模式的对话` → `## 模式提炼` → `## 你能做什么`

**章末结构**：模式提炼（3-5 个命名模式，五层结构）+ 你能做什么（5-8 条行动建议）。

**风格专属必须**：
- 展示至少 2 处源码实例，证明模式的普遍性
- 有适用范围表
- 包含与已知业界模式的对话（HTTP 缓存控制 vs 提示词缓存的类比）
- 模式有中文+英文命名
- 章节总长 2000-3000 字

**风格专属禁止**：
- 不只展示 addCacheBreakpoints 一个例子
- 不忽略缓存的失效场景和成本边界
- 不出现纯接口描述而没有工程权衡分析

**通用写作铁律**：
- ⛔ 源码第一：每个断言附 `路径:行号` 证据
- ⛔ 代码无裸露：每个代码块有引入段+解读段
- ⛔ 数据驱动：具体行数/常量名（如 AUTOCOMPACT_BUFFER_TOKENS）
- ⛔ 开篇铁律：开篇在初稿完成后最后补写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/services/api/claude.ts | addCacheBreakpoints() 函数：行3063 | 3063-3120 |
| 2 | src/services/api/claude.ts | splitSysPromptPrefix()：行3222 | 3210-3250 |
| 3 | src/services/api/claude.ts | queryWithModel() / queryModelWithStreaming() | 544-600 |
| 4 | src/services/api/withRetry.ts | withRetry generator 完整实现 | 1-80 |
| 5 | src/services/api/claude.ts | buildSystemPromptBlocks() 或 usage 追踪 | 800-850 |
| 6 | src/utils/model/providers.ts | 多提供商检测逻辑 | 1-60 |

验证文件存在：
```bash
ls src/services/api/claude.ts src/services/api/withRetry.ts src/utils/model/providers.ts
sed -n '3063,3070p' src/services/api/claude.ts   # addCacheBreakpoints 存在
sed -n '256,260p' src/services/api/claude.ts     # withRetry 导入
```

## Spec 验收场景清单

**验收优先级**：E > A > B = C > D

### A. 源码锚点组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| A1 | 源码引用有效性 | 每处引用存在于实际文件 | 节 13.1-13.4 | ☐ |
| A2 | 推断有标注 | 推断标注「（推断）」 | 所有节 | ☐ |
| A3 | 不引用排除范围 | 无排除范围路径 | 写作前 checklist | ☐ |
| A4 | 不重复其他章内容 | 详见第 9/11/19 章 | 所有节 | ☐ |

### B. 语言精确性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| B1 | 禁止空洞表述 | 无"介绍了"等空洞动词 | 全章 | ☐ |
| B2 | 数据驱动 | 具体行数/函数名/常量名 | 全章 | ☐ |
| B3 | 术语标准化 | 首次出现标注原文 | 全章 | ☐ |

### C. 结构完整性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| C1 | 概念递进层次 | 每概念至少两层递进 | 节 13.1-13.4 | ☐ |
| C2 | ⛔ hunter 结构 | ≥2实例+适用范围表+与已知模式对话+中英文命名 | 节 13.2-13.3 | ☐ |
| C3 | 核心主张可提炼 | 可从开篇提炼1-2句答案 | 开篇节 | ☐ |
| C4 | 设计权衡有说明 | 有"选A不选B"说明 | 权衡与局限节 | ☐ |
| C5 | 章末行动建议 | 5-8条行动建议 | 你能做什么节 | ☐ |
| C6 | 章节间链接 | 章末向后链接+向前链接 | 全章 | ☐ |

### D. 图表与代码组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| D1 | 图表可渲染且达标 | ≥3个图表 | 图表清单 | ☐ |
| D2 | 图表标注格式 | **图13-X：标题** | 所有图表 | ☐ |
| D3 | 代码片段长度 | ≤25行，前后有说明 | 所有代码块 | ☐ |
| D4 | 源码注释引用 | 先引原注释+路径:行号 | 节 13.1 | ☐ |
| D5 | ⛔ hunter 图表 | 架构关系图+适用场景决策图 | 图13-1, 13-3 | ☐ |

### E. 风格符合性组

| # | 场景名 | 核心要求 | 对应 plan 节 | 覆盖 |
|---|--------|---------|-------------|------|
| E1 | ⛔ hunter 开篇格式 | 150-200字，不引源码 | 开篇节 | ☐ |
| E2 | 读者对话感 | "我们"，有预告和设问 | 全章 | ☐ |
| E3 | 关键信息突出 | 加粗+表格 | 全章 | ☐ |

## 章节骨架

### 开篇（最后补写）
> ⛔ hunter 模式预告：150-200字，问题场景+模式预告+价值承诺，不引源码
- 可选题词：来自 claude.ts:624 的注释（共享缓存数组防止重复 cache_edit）

### 节 13.1：提示词缓存断点——节省 API 成本的工程手段
- 核心主张：addCacheBreakpoints() 和 splitSysPromptPrefix() 通过精确放置缓存断点，让 Anthropic API 的提示词缓存命中最大化
- 源码锚点：`src/services/api/claude.ts:3063` — addCacheBreakpoints()；`src/services/api/claude.ts:3222` — splitSysPromptPrefix()
- 概念层次：① 什么是提示词缓存（Anthropic API 特性）② 缓存断点的放置策略 ③ splitSysPromptPrefix 的分裂逻辑
- 权衡分析：为什么要区分"可缓存的不变前缀"和"不可缓存的动态后缀"？→ 缓存命中需要字节级一致性，任何变化都会缓存失效
- 包含元素：ASCII 图（system prompt 缓存断点位置示意，图13-1）+ 代码片段（claude.ts:3063-3085，≤25行）
- **⛔ 内容密度规划**：
  - 引入段：每次 API 调用 system prompt 都相同，但输入 tokens 计费——如何让 Anthropic 认出"这部分我上次发过"？
  - 解读段：claude.ts:624 的注释警告了"共享数组的 splice 副作用"——这是精细缓存管理必须面对的陷阱
- 估计字数：600-800字（散文≥65%）
- 对应 spec 场景：A1、C1、D3、D4

### 节 13.2：模式实例 1 — Fork Subagent 的缓存共享
- 核心主张：Fork Subagent 通过 CacheSafeParams 与父级共享完全相同的 system prompt 字节，确保缓存前缀命中
- 源码锚点：`src/utils/model/providers.ts:1` — 多提供商检测；`src/services/api/claude.ts:1701` — addCacheBreakpoints 调用处
- 概念层次：① Fork Subagent 的缓存共享需求 ② CacheSafeParams 的设计约束 ③ 为什么子 Agent 不能自定义 system prompt
- 权衡分析：为了缓存而约束子 Agent 的 system prompt——这是成本优化和灵活性的显式权衡
- 包含元素：Mermaid flowchart（父子 Agent 缓存共享，图13-2）
- **⛔ 内容密度规划**：
  - 引入段：如果子 Agent 自定义了 system prompt，缓存就会失效——Claude Code 选择约束子 Agent 来换取缓存效率
  - 解读段：这是一个贯穿多个子系统的架构决策，详见第 9 章的 queryLoop 和第 19 章的提示词装配
- 估计字数：500-600字（散文≥60%）
- 对应 spec 场景：A1、C2、D3

### 节 13.3：模式实例 2 — 指数退避重试
- 核心主张：withRetry() 是一个 async generator，封装了 API 调用的指数退避重试、客户端复用和错误分类
- 源码锚点：`src/services/api/withRetry.ts:1` — withRetry 完整实现；`src/services/api/claude.ts:256` — 导入位置；`src/services/api/claude.ts:544` — 调用处
- 概念层次：① withRetry 的 generator 接口设计 ② 指数退避算法 ③ 哪些错误重试，哪些不重试
- 权衡分析：为什么用 generator 而非 Promise？→ 允许调用者在重试间隙 yield 进度消息
- 包含元素：Mermaid stateDiagram（重试状态机，图13-3）
- **⛔ 内容密度规划**：
  - 引入段（与实例1的区别）：实例1是"减少必须发送的 token"，实例2是"应对 API 不稳定"——两者共同构成 API 通信层的韧性工程
  - 解读段：错误分类是重试逻辑的核心——速率限制（可重试）vs 认证失败（不可重试）vs 内容过滤（不可重试）
- 估计字数：500-600字（散文≥65%）
- 对应 spec 场景：A1、C2、D3

### 节 13.4：模式剖析——提示词缓存优先
- 核心主张：提示词缓存优先是一种贯穿整个 API 通信层的设计约束，所有架构决策都以"保持缓存命中率"为优先
- 包含元素：模式命名框（提示词缓存优先 Cache-First Prompt）+ 适用范围表
- 估计字数：400-500字
- 对应 spec 场景：C2、C3

### 节 13.5：权衡与局限
- 权衡：缓存优先增加了架构约束（子 Agent 不能自定义 system prompt），换取成本节省
- 估计字数：200-300字
- 对应 spec 场景：C4

### 节 13.6：与已知模式的对话
- 对比：HTTP Cache-Control（浏览器缓存策略）vs 提示词缓存断点的类比；Circuit Breaker（断路器）vs withRetry 的差异
- 估计字数：200-300字
- 对应 spec 场景：C2

### 模式提炼
- 模式 1：提示词缓存优先（Cache-First Prompt）
- 模式 2：韧性重试生成器（Resilient Retry Generator）——用 generator 封装重试逻辑
- 模式 3：缓存感知约束（Cache-Aware Constraint）——为了缓存一致性而限制下游自定义

### 你能做什么（5-8 条）
1. 识别系统提示中的"不变前缀"和"动态后缀"，在 Anthropic API 中放置缓存断点
2. 用 splitSysPromptPrefix() 的思路将 system prompt 拆分为缓存友好的结构
3. 设计子 Agent 时，约束其 system prompt 与父 Agent 完全一致以共享缓存
4. 用 async generator 封装 API 重试逻辑，允许在重试间 yield 进度更新
5. 为 API 错误分类（速率限制/鉴权失败/内容过滤），只重试可恢复的错误
6. 用 updateUsage()/accumulateUsage() 模式在多 Agent 场景中聚合 token 用量
7. 在多提供商场景中，用 providers.ts 的模式统一 Anthropic/Bedrock/Vertex 的接口差异

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图13-1 | System Prompt 缓存断点位置 | ASCII box-arrow | 不变前缀(可缓存)→断点标记→动态后缀(不缓存) | 13.1 |
| 图13-2 | 父子 Agent 缓存共享 | Mermaid flowchart | 父Agent→CacheSafeParams→子Agent→相同缓存前缀命中 | 13.2 |
| 图13-3 | withRetry 重试状态机 | Mermaid stateDiagram | 初始→发起请求→成功/失败→判断可重试→退避等待→重试/放弃 | 13.3 |

## 写作顺序

1. ⛔ 读取完整 spec（specs/ch13.spec）和 DESIGN.md STYLE_ID=hunter
2. 先写节 13.1（缓存断点，最核心的新内容）
3. 再写节 13.2（Fork Subagent 缓存共享）
4. 写节 13.3（withRetry，明确说明与13.2的区别）
5. 写图表（先 ASCII 草图，再转 Mermaid）
6. 写节 13.4（模式剖析）+ 13.5（权衡）+ 13.6（对话）
7. 写模式提炼 + 你能做什么
8. 最后补写开篇

## 写作前 checklist

```bash
sed -n '3063,3070p' src/services/api/claude.ts   # addCacheBreakpoints 入口
sed -n '3222,3230p' src/services/api/claude.ts   # splitSysPromptPrefix
sed -n '256,260p' src/services/api/claude.ts     # withRetry 导入
sed -n '544,550p' src/services/api/claude.ts     # queryWithModel 调用 withRetry
wc -l src/services/api/claude.ts                 # 确认 3419 行
```

## ⛔ Spec 一致性声明

**完整 spec 文件**：specs/ch13.spec

| # | 提取项 | 是否已写入 plan | 对应 plan 节 |
|---|--------|---------------|-------------|
| 1 | 意图 | ✓ | 章节意图 |
| 2 | 通用约束·必须 | ✓ | 通用约束·通用必须 |
| 3 | 通用约束·禁止 | ✓ | 通用约束·通用禁止 |
| 4 | 风格专属必须 | ✓ | 风格专属·必须 |
| 5 | 风格专属禁止 | ✓ | 风格专属·禁止 |
| 6 | 章节结构 | ✓ | 章节骨架 |
| 7 | 源码引用格式 | ✓ | 各节·源码锚点 |
| 8 | 图表格式 | ✓ | 图表清单 |
| 9 | 边界路径 | ✓ | 通用约束·排除范围 |
| 10 | 验收优先级 | ✓ | Spec 验收场景清单 |
| 11 | 验收场景·A | ✓ | Spec 验收场景清单 A |
| 12 | 验收场景·B | ✓ | Spec 验收场景清单 B |
| 13 | 验收场景·C | ✓ | Spec 验收场景清单 C |
| 14 | 验收场景·D | ✓ | Spec 验收场景清单 D |
| 15 | 验收场景·E | ✓ | Spec 验收场景清单 E |
| 16 | 排除范围 | ✓ | 通用约束·排除范围 |

**验收场景覆盖统计**：A(4/4) + B(3/3) + C(6/6) + D(5/5) + E(3/3) = **21/21**
