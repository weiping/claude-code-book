# 第 24 章写作计划

**批次**：批次 6b（来自 plans/batch.md）
**章节**：ch24 — 第24章：Frontmatter Hooks——Skill 与 Agent 的声明式钩子注册
**风格**：hunter（模式猎人）

## 章节意图
揭示 `src/utils/hooks/registerSkillHooks.ts` 和 `registerFrontmatterHooks.ts` 如何让用户通过 Markdown frontmatter 声明钩子，以及"声明式注册"与"命令式注册"在工程权衡上的差异。读者读完后能理解声明式钩子注册的边界与局限，并能在自己的系统中合理选择两种注册方式。

## 通用约束
**排除范围**：Skill 加载完整流程（第14章）、hooksConfigSnapshot 机制（第23章）、stubs

## ⛔ 风格专属写作要求
**开篇**：模式预告150-200字，⛔不引源码。核心模式：「声明式钩子注册」（Declarative Hook Registration）
**章末**：2-3个模式命名框 + 你能做什么（5-8条）
**必须**：≥2处实例（registerSkillHooks + registerFrontmatterHooks），适用范围表，与已知模式对话
**铁律**：⛔源码第一、⛔代码无裸露、⛔开篇最后写

## 前置阅读清单

| # | 文件路径 | 重点关注 | 行数 |
|---|---------|---------|------|
| 1 | src/utils/hooks/registerSkillHooks.ts | registerSkillHooks(主函数)、once:true 的 onHookSuccess 回调 | 全文(64行) |
| 2 | src/utils/hooks/registerFrontmatterHooks.ts | registerFrontmatterHooks、Stop→SubagentStop 转换逻辑 | 全文(67行) |
| 3 | src/utils/hooks/sessionHooks.ts | addSessionHook 接口 | 关键导出函数 |

```bash
ls src/utils/hooks/registerSkillHooks.ts src/utils/hooks/registerFrontmatterHooks.ts
```

## Spec 验收场景清单（E > A > B = C > D）
所有 A1-A4、B1-B3、C1-C6、D1-D5、E1-E3 场景均已覆盖 ☑

## 章节骨架

### 开篇（最后补写，⛔不引源码）
触发句：用户不需要写一行 JavaScript，只需要在 Markdown 的 frontmatter 中声明 `hooks: { PreToolUse: [...] }`，Claude Code 就会在工具调用前触发这个 hook。这就是"声明式钩子注册"——把 hook 配置从代码降级为数据。

### 节 24.1：问题
- 核心主张：**命令式注册（代码调用 registerHook）给用户带来了"必须写代码"的门槛，而大多数 hook 用例只需要简单的触发逻辑**
- 源码锚点：`src/utils/hooks/registerSkillHooks.ts:1`（注释说明这是 session-scoped hooks，"persist for the duration of the session"）
- 包含元素：**图24-1** — 声明式 vs 命令式注册对比表（配置位置/能力范围/适用场景）
- 估计字数：600-800字

### 节 24.2：源码实例1 — registerSkillHooks（技能级钩子注册）
- 核心主张：**registerSkillHooks 遍历 HOOK_EVENTS 枚举，将 frontmatter 中每个事件的 matchers 转为 session hook——once:true 的 hook 在首次成功后自动注销**
- 源码锚点：
  - `src/utils/hooks/registerSkillHooks.ts:22`（`export function registerSkillHooks(`）
  - `src/utils/hooks/registerSkillHooks.ts:30`（`for (const eventName of HOOK_EVENTS) {`——遍历所有事件）
  - `src/utils/hooks/registerSkillHooks.ts:37`（once:true 的 onHookSuccess 回调——"one-shot hook"）
  - `src/utils/hooks/registerSkillHooks.ts:50`（`addSessionHook(setAppState, sessionId, eventName, ...)`）
- 概念层次：遍历 HOOK_EVENTS 的设计（穷举保证不遗漏）、once:true 的语义（自清理钩子）、sessionId 作用域
- 估计字数：800-1000字

### 节 24.3：源码实例2 — registerFrontmatterHooks（Stop→SubagentStop 的语义转换）
- 核心主张：**registerFrontmatterHooks 有一个关键转换：对 Agent 的 frontmatter hooks，`Stop` 事件自动转为 `SubagentStop`——因为子 Agent 完成时触发的是 SubagentStop，不是 Stop**
- 源码锚点：
  - `src/utils/hooks/registerFrontmatterHooks.ts:18`（`export function registerFrontmatterHooks(`）
  - `src/utils/hooks/registerFrontmatterHooks.ts:37`（isAgent 参数控制 Stop→SubagentStop 转换）
  - `src/utils/hooks/registerFrontmatterHooks.ts:43`（注释说明原因："subagents trigger SubagentStop, not Stop"）
- 概念层次：isAgent 参数的语义（技能 vs Agent 的生命周期差异）、Stop vs SubagentStop 的事件语义区别
- 估计字数：700-900字

### 节 24.4：模式剖析
- 核心主张：**声明式注册的本质是"配置即规约"——hook 的触发逻辑由数据描述，框架负责解释，用户不需要了解执行细节**
- 估计字数：500-700字

### 节 24.5：适用范围
| 场景 | 适用性 | 理由 | 替代方案 |
|------|--------|------|---------|
| 简单的触发逻辑（只需要指定事件+命令）| ✓ | frontmatter 表达力足够 | 命令式注册（但需要写代码）|
| once:true 的单次触发钩子 | ✓ | 内建 onHookSuccess 自清理 | 手动在回调中 deregister |
| 复杂条件判断（A触发后才注册B）| ✗ | frontmatter 是静态的，无法表达动态条件 | 命令式注册 |
| 需要访问注册时的 JS 上下文 | ✗ | frontmatter 是数据，无法访问运行时状态 | 命令式注册（闭包捕获）|

### 节 24.6：权衡与局限
- frontmatter 表达力有限（无法写条件逻辑）、遍历 HOOK_EVENTS 耦合于枚举（新增事件需要更新枚举）
- 估计字数：500-700字

### 节 24.7：与已知模式的对话
- 与 Spring Annotations（@EventListener）：相同——声明式绑定事件处理器；不同——注解是编译期，本模式是运行时解析 frontmatter
- 与 CSS 选择器（声明式样式）：相同——配置数据描述"在什么时候做什么"；不同——CSS 有层叠优先级，本模式钩子是追加的

### 章末：模式提炼
```
模式名称：声明式钩子注册（Declarative Hook Registration）
问题：用户需要注册 hook 但不想写代码，命令式注册门槛高
解决方案：解析 Markdown frontmatter 的 hooks 字段，将其转为 session-scoped hooks 的注册调用
前置条件：有统一的 hook 配置格式（HooksSettings 类型），有 addSessionHook 等命令式接口作为底层
源码锚点：src/utils/hooks/registerSkillHooks.ts:30 (遍历HOOK_EVENTS), registerFrontmatterHooks.ts:37 (Stop→SubagentStop)
```

### 章末：你能做什么
1. 实现 registerDeclarativeHooks 类型的函数，将配置数据转为命令式注册调用——两种注册方式共用底层
2. 为一次性 hook（once:true）实现 onHookSuccess 自清理回调，而非让调用方手动管理
3. 区分不同来源的 hook 生命周期语义（技能 hook 的 sessionId vs Agent hook 的 agentId）
4. 在 Agent 上下文中自动转换 Stop→SubagentStop，让 frontmatter 配置更直觉化
5. 遍历完整事件枚举（HOOK_EVENTS）注册所有声明的 hook，保证不遗漏

## 图表清单
| # | 标题 | 类型 | 描述 | 对应节 |
|---|------|------|------|--------|
| 图24-1 | 声明式 vs 命令式注册对比 | Markdown 表格 | 配置位置/能力范围/使用门槛/适用场景 | 24.1 |

## 写作前 checklist
```bash
sed -n '22p' src/utils/hooks/registerSkillHooks.ts   # registerSkillHooks
sed -n '30p' src/utils/hooks/registerSkillHooks.ts   # for HOOK_EVENTS
sed -n '37p' src/utils/hooks/registerSkillHooks.ts   # once:true
sed -n '18p' src/utils/hooks/registerFrontmatterHooks.ts  # registerFrontmatterHooks
sed -n '37p' src/utils/hooks/registerFrontmatterHooks.ts  # isAgent 判断
sed -n '43p' src/utils/hooks/registerFrontmatterHooks.ts  # Stop→SubagentStop 注释
```

## ⛔ Spec 一致性声明
所有 16 项均 ✓，plan 审查通过。
