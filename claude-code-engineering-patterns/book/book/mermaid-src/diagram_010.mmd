graph TD
    REPL["REPL.tsx\n5005 行\n主组件"]

    subgraph local["本地 useState（必须在 REPL 内）"]
        Q["toolUseConfirmQueue\n阻塞工具执行的 Promise 队列"]
        SQ["sandboxPermissionRequestQueue\n沙箱权限队列"]
        PQ["promptQueue\n异步提示请求队列"]
    end

    subgraph app["AppState（全局共享）"]
        SP["speculation\nSpeculationState"]
        MSG["messages"]
        TASK["tasks"]
        PERM["toolPermissionContext"]
    end

    subgraph hooks["会话 Hooks（本地绑定）"]
        RS["useRemoteSession"]
        DC["useDirectConnect"]
        SSH["useSSHSession"]
    end

    REPL --> local
    REPL --> app
    REPL --> hooks

    Q -->|"resolve 回调传给 query loop"| REPL
    SP -->|"onSubmit 读取并 accept/abort"| REPL
