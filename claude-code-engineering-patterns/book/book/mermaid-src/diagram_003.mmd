graph LR
    subgraph callers["调用方（query.ts / REPL.tsx / tools/）"]
        C[" "]
    end

    subgraph api["bootstrap/state.ts 公开接口（216 个函数）"]
        G["get 函数（只读）
getOriginalCwd
getSessionId"]
        S["set 函数（受控写入）
setOriginalCwd
setIsRemoteMode"]
        M["内部直接操作
regenerateSessionId
（原子修改多字段）"]
    end

    STATE[("const STATE
模块级单例
不对外 export")]

    callers -->|"只通过函数调用"| G
    callers -->|"只通过函数调用"| S
    G -->|"STATE.field 读取"| STATE
    S -->|"STATE.field 写入"| STATE
    M -. "模块内部直接操作
调用方无法绕过" .-> STATE
