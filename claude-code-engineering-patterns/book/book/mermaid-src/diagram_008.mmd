sequenceDiagram
    participant JS as JavaScript 主线程
    participant MDM as MDM 子进程
    participant KC as Keychain 读取
    participant M as 模块求值

    Note over JS: profileCheckpoint('main_tsx_entry')
    JS->>MDM: startMdmRawRead()（launch subprocess）
    JS->>KC: startKeychainPrefetch()（async I/O）
    JS->>M: import 求值（~135ms 阻塞）
    MDM-->>JS: MDM 配置就绪（并行完成）
    KC-->>JS: Keychain 就绪（并行完成）
    M-->>JS: 所有模块加载完毕
    Note over JS: 三路结果均已就绪，无需额外等待
