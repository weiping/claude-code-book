# 第 23 章写作计划

**批次**：批次 5（第六篇：上下文工程）
**章节**：ch23 — 第23章：会话系统——标识、持久化与恢复
**风格**：autopsy（工程师解剖）

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/utils/sessionStorage.ts | getTranscriptPath、MAX_TRANSCRIPT_READ_BYTES 常量 | 行号 202-235 |
| 2 | src/bootstrap/state.ts | getSessionId、switchSession 会话 ID 管理 | 行号 800-850 |
| 3 | src/utils/concurrentSessions.ts | SessionKind 枚举与会话隔离 | 行号 18, 30-80 |
| 4 | src/utils/sessionStorage.ts | appendEntryToFile、readTranscript 追加写入与读取 | 行号 2600-2700 |
| 5 | src/commands/resume/ | /resume 命令的实现——会话查找与恢复 | 行号 1-100 |
| 6 | src/services/api/ | 对话记录如何序列化为 JSONL 格式 | 行号 1-50 |

验证文件存在：
```bash
ls -l src/utils/sessionStorage.ts \
      src/bootstrap/state.ts \
      src/utils/concurrentSessions.ts \
      src/commands/resume/
```

## 章节骨架

### 开篇（必填）
- **开篇触发点**：你在 REPL 中和 Claude 讨论了一个小时，突然进程崩溃。重启 Claude Code 后，能恢复之前的对话吗？上次的对话数据存在哪里？下次 `/resume` 时系统如何找到它？
- **工程问题**：REPL 会话是有状态的，但进程随时会终止。需要一个持久化系统将对话历史保存到磁盘，并在需要时恢复。同时要支持多并发会话隔离，避免不同窗口的对话相互污染。
- **权衡**：完整性（保留所有对话细节）vs 存储成本（无限增长）vs 恢复速度（读取大文件的延迟）

### 节 23.1：JSONL 格式存储与追加写入设计
- **核心主张**：会话对话记录以 JSONL 格式存储在 `.claude/projects/{hash}/{sessionId}.jsonl`。采用追加写入（append-only）设计：每条对话（用户输入 + Claude 回复）都被序列化为一行 JSON，直接追加到文件末尾。不覆写、不重排。
- **源码锚点**：`src/utils/sessionStorage.ts:202` — getTranscriptPath 函数展示 JSONL 路径格式
- **概念层次**：存储架构 / 追加写入语义 / 格式规范
- **权衡分析**：为什么追加而不是覆写整个文件？追加写入的优点：
  1. 崩溃安全（如果进程在写入中途崩溃，已写入的行不会损坏）
  2. 减少 I/O（只写入新增行，不需要读-修改-写整个文件）
  3. 便于增量读取（可以从上次读取位置继续）
  4. 便于调试（历史记录完整，追踪每条对话的顺序）
- **包含元素**：JSONL 格式示例、追加写入示意图、与覆写方案的对比
- **估计字数**：600-800 字
- **对应 spec 场景**：verify_jsonl_storage_path

### 节 23.2：会话标识与路径构造
- **核心主张**：每个会话都有唯一的 sessionId（UUID）。对话文件路径由三部分组成：基础目录 ~/.claude/projects + 项目哈希（根据初始工作目录生成） + sessionId.jsonl。项目哈希确保不同项目的会话不混淆。
- **源码锚点**：`src/bootstrap/state.ts:getSessionId` — sessionId 的生成和管理, `src/utils/sessionStorage.ts:202-210` — 路径构造逻辑
- **概念层次**：标识设计 / 哈希函数 / 路径规范
- **权衡分析**：为什么用项目哈希而不是项目路径？路径很长且可能包含特殊字符，用哈希（如 sha256）能生成固定长度的安全目录名。这简化了文件系统操作。
- **包含元素**：sessionId 的生成算法、哈希计算方法、完整路径示例
- **估计字数**：500-700 字
- **对应 spec 场景**：verify_jsonl_storage_path

### 节 23.3：MAX_TRANSCRIPT_READ_BYTES 与大会话处理
- **核心主张**：为了防止内存爆炸，系统对读取的对话历史设置上限 MAX_TRANSCRIPT_READ_BYTES（通常 50MB）。当会话文件超过此限制时，只读取最近的 50MB 内容（截断头部）。超出限制时会被标注，Claude 知道对话历史不完整。
- **源码锚点**：`src/utils/sessionStorage.ts:229` — MAX_TRANSCRIPT_READ_BYTES 常量定义
- **概念层次**：限制机制 / 截断策略 / 标注方法
- **权衡分析**：为什么需要限制？长时间运行的 REPL 会话可能累积 GB 级的对话历史。加载整个历史会耗尽内存和延迟启动。50MB 的限制平衡了保留足够上下文（约 10 万行对话）和内存压力。
- **包含元素**：会话大小增长示例、截断前后的内存占用对比、超出限制时 Claude 收到的通知
- **估计字数**：500-700 字
- **对应 spec 场景**：verify_transcript_size_limit_handling

### 节 23.4：/resume 命令——会话查找与恢复
- **核心主张**：/resume 命令扫描 ~/.claude/projects/ 目录，按修改时间排序，找到最近的会话。读取其 JSONL 文件（受 MAX_TRANSCRIPT_READ_BYTES 限制），恢复对话历史到新的 REPL 会话。switchSession 状态管理确保恢复的会话 ID 被设置为当前会话。
- **源码锚点**：`src/bootstrap/state.ts:switchSession` — 会话切换状态管理, `src/commands/resume/` — /resume 命令实现
- **概念层次**：查找算法 / 恢复流程 / 状态转换
- **权衡分析**：为什么用修改时间排序而不是创建时间？修改时间反映了会话的"活跃度"，用户通常想恢复最近活跃的会话（而非最早创建的）。
- **包含元素**：/resume 查找流程的伪代码、会话恢复的状态转换图、实际的恢复示例
- **估计字数**：600-800 字
- **对应 spec 场景**：verify_resume_lookup_logic

### 节 23.5：SessionKind 与并发会话隔离
- **核心主张**：系统支持四种会话类型（SessionKind）：interactive（交互式 REPL）、bg（后台任务）、daemon（守护进程）、daemon-worker（守护进程工作线程）。每种类型有独立的 sessionId 和 JSONL 文件，实现多并发会话隔离。不同类型的会话不会相互干扰。
- **源码锚点**：`src/utils/concurrentSessions.ts:18` — SessionKind 类型定义, `src/utils/concurrentSessions.ts:30-80` — 会话类型检测和隔离逻辑
- **概念层次**：会话分类 / 隔离机制 / 使用场景
- **权衡分析**：为什么需要多种会话类型而不是统一的 sessionId？不同类型的会话有不同的生命周期和用途（交互式持久，后台短暂），独立的 sessionId 避免了混淆和冲突。
- **包含元素**：四种会话类型的定义表、生命周期图、隔离示意图（两个终端窗口同时运行）
- **估计字数**：500-700 字
- **对应 spec 场景**：verify_concurrent_session_isolation

### 节 23.6：状态管理与会话切换
- **核心主张**：会话 ID 存储在全局状态（getSessionId / setSessionId）。当恢复会话或用户显式切换时，switchSession 更新全局状态中的 sessionId，后续所有 API 请求、对话追加都使用新的 sessionId。这确保了多次 /resume 能无缝切换会话。
- **源码锚点**：`src/bootstrap/state.ts:getSessionId/switchSession` — 会话 ID 的状态管理
- **概念层次**：状态存储 / 切换机制 / 生命周期
- **权衡分析**：为什么会话 ID 放在全局状态而不是本地变量？全局状态确保整个进程内的所有模块（API 客户端、存储层、REPL）都能访问当前的 sessionId。本地变量会导致 ID 传递困难、状态不一致。
- **包含元素**：状态图、switchSession 的调用链、多次 /resume 的状态转变序列
- **估计字数**：400-600 字
- **对应 spec 场景**：verify_source_citations_exist

### 章末：模式提炼（必填）

| 模式名 | 解决的问题 | 核心做法（一句话） | 前置条件 | 源码证据锚点 |
|--------|-----------|-----------------|---------|------------|
| Append-only JSONL | 进程崩溃中途可能丢失对话 | 采用追加写入，每条对话序列化为一行 JSON 直接追加，避免覆写导致的数据损坏 | 长时间运行的会话 | `src/utils/sessionStorage.ts:202` |
| sessionId 与项目隔离 | 不同项目的会话容易混淆 | 用项目哈希 + sessionId 两层组合构造路径，确保隔离 | 多项目工作流 | `src/bootstrap/state.ts:getSessionId` |
| MAX_TRANSCRIPT_READ_BYTES 限制 | 超大会话导致内存爆炸 | 设置 50MB 读取上限，超过则截断头部，标注为不完整 | 长期会话累积 | `src/utils/sessionStorage.ts:229` |
| 按修改时间 /resume | 用户不记得哪个会话最重要 | 扫描所有会话，按修改时间排序，恢复最近活跃的会话 | 多个会话并存 | `src/commands/resume/` |
| SessionKind 多类型隔离 | 交互式和后台任务的 sessionId 冲突 | 定义四种会话类型（interactive/bg/daemon/daemon-worker），每种独立 sessionId | 并发会话需求 | `src/utils/concurrentSessions.ts:18` |

### 章末：用户能做什么（必填）

阅读本章后，工程师能做到：
1. **理解会话持久化机制**：知道对话记录以 JSONL 格式存储在哪里（~/.claude/projects/{hash}/{sessionId}.jsonl）
2. **手动查看会话历史**：能找到会话文件，用文本编辑器打开 JSONL 查看对话细节
3. **恢复意外终止的会话**：理解 /resume 的查找和恢复流程，知道修改时间排序的含义
4. **处理超大会话**：理解 MAX_TRANSCRIPT_READ_BYTES 限制，知道超过 50MB 时会截断头部
5. **调试多并发会话冲突**：理解 SessionKind 的四种类型，诊断为什么某些会话被相互覆盖
6. **设计自己的会话系统**：基于 Append-only JSONL 的架构，实现"崩溃安全 + 内存高效 + 恢复快速"的会话持久化
7. **优化会话存储**：理解追加写入的性能特性，知道如何在长期运行的进程中管理会话文件大小

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图23-1 | JSONL 追加写入示意 | ASCII 或示意图 | 展示多条对话如何被逐行追加到 JSONL 文件 | 23.1 |
| 图23-2 | 会话文件路径结构 | 目录树 | ~/.claude/projects/{hash}/{sessionId}.jsonl 的完整路径示例 | 23.2 |
| 图23-3 | /resume 查找流程 | Mermaid flowchart | 扫描 ~/.claude/projects/ → 按修改时间排序 → 读取最近会话 → 恢复 | 23.4 |
| 图23-4 | SessionKind 隔离示意 | 示意图或表格 | 四种会话类型的定义、生命周期、隔离方式 | 23.5 |

## 写作顺序

1. **先写 23.1**（Append-only JSONL）— 是本章的存储基础，易于理解且很重要
2. **写 23.2**（会话标识与路径）— 紧接着 23.1 的存储位置
3. **写 23.3**（MAX_TRANSCRIPT_READ_BYTES 限制）— 与存储直接相关的优化策略
4. **写 23.4**（/resume 恢复）— 运行时功能，依赖前面的存储和标识理解
5. **写 23.5**（SessionKind 隔离）— 并发模型，相对独立但依赖会话标识
6. **写 23.6**（状态管理）— 整体的生命周期管理
7. **补写模式提炼** — 五个模式自然浮现
8. **补写开篇** — 写出"为什么重要"
9. **补写用户能做什么** — 生成 7 条行动建议

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
PROJECT_ROOT="$(pwd)"
BOOK_ROOT="${PROJECT_ROOT}/book"

# 验证 ch23 的所有源码锚点
grep -n "getTranscriptPath\|MAX_TRANSCRIPT_READ_BYTES" "${PROJECT_ROOT}/src/utils/sessionStorage.ts" | head -5 && echo "✓ sessionStorage.ts 中的关键函数存在" || echo "✗ 不存在"

grep -n "getSessionId\|switchSession" "${PROJECT_ROOT}/src/bootstrap/state.ts" | head -3 && echo "✓ state.ts 中的会话管理函数存在" || echo "✗ 不存在"

grep -n "type SessionKind" "${PROJECT_ROOT}/src/utils/concurrentSessions.ts" && echo "✓ SessionKind 类型定义存在" || echo "✗ 不存在"

[ -d "${PROJECT_ROOT}/src/commands/resume" ] && echo "✓ /resume 命令目录存在" || echo "✗ 不存在"

echo "✓ 所有 ch23 锚点检查完毕"
```

如果任何锚点不存在，重新搜索正确行号：
```bash
grep -n "getTranscriptPath\|getSessionId\|SessionKind" "${PROJECT_ROOT}/src/utils/sessionStorage.ts" "${PROJECT_ROOT}/src/bootstrap/state.ts" "${PROJECT_ROOT}/src/utils/concurrentSessions.ts"
```
