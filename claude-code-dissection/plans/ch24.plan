# 第 24 章 - 缓存系统：Prompt Cache 与 Context 压缩四策略
## 写作计划（Plan）

---

## 前置阅读清单

读者应已完成以下章节，因为本章会频繁参考：
- **第 21 章**：上下文组装 — 理解 context 如何被采集、组装、作为 cache-key 前缀
- **第 19 章**：系统提示的优先级堆叠 — 理解系统提示的结构和 appendSystemPrompt 机制
- **第 23 章**：会话系统 — 理解会话如何持久化，本章的压缩策略如何影响持久化内容

核心概念：
- Prompt Cache：Anthropic API 的缓存机制，相同前缀的请求复用 tokens（节省成本）
- Cache-key 前缀：系统提示 + 上下文，这部分一旦变化就会导致缓存失效
- Context Window：Claude 能处理的最大 token 数（如 Opus 的 200K）
- Token 警告状态：系统如何检测即将溢出并主动压缩

---

## 章节骨架与论述结构

### 24.1 对话增长的悖论
**定义**：用户在一个 REPL 会话中进行 50+ 轮对话，每轮对话都会累积到系统提示中，最终导致 context 膨胀。

**设计意图**：为什么 Claude Code 需要压缩机制？为什么不直接流式传输或分割会话？

**权衡**：
- 不压缩：允许 Claude 访问完整历史，但超过 context window 后无法工作
- 立即压缩：始终保持小体量，但丧失历史上下文
- 智能压缩：平衡历史完整性和 context 限制

**实际案例**：
- 案例：Python 项目的 100 轮对话，系统提示从 2000 tokens 增长到 50000 tokens，接近 Sonnet 的上限
- 反证：如果没有压缩机制，第 80 轮对话时会超过上限，Claude 无法处理

---

### 24.2 四种压缩策略总览
**定义**：Claude Code 实现了四种不同的 context 压缩策略，每种都在 `src/services/compact/` 下有对应的实现文件：
- AutoCompact（`autoCompact.ts`）：自动触发，粗粒度
- ReactiveCompact（`reactiveCompact.ts`）：用户主动触发的压缩
- MicroCompact（`microCompact.ts`）：细粒度，只压缩最新的几条消息
- SnipCompact（`snipCompact.ts`）：片段化，删除中间冗余内容

**对比表**：
| 策略 | 触发条件 | 压缩粒度 | 历史损失 | 适用场景 | 源码文件 |
|------|---------|---------|---------|---------|---------|
| AutoCompact | Token 使用量 > 阈值（自动） | 粗 | 高（删除早期轮次） | 长会话，无用户干预 | autoCompact.ts:147 |
| ReactiveCompact | 用户触发 `/compact` 命令 | 中 | 中（用户可选择范围） | 用户主动管理 | reactiveCompact.ts:* |
| MicroCompact | 系统检测到单条消息过大 | 细 | 低（仅最新消息） | 偶发的超大响应 | microCompact.ts:* |
| SnipCompact | 分析中间内容冗余度 | 中 | 中（删除重复段落） | 有模式的重复内容 | snipCompact.ts:* |

**设计意图**：为什么要四种？为什么不只用最激进的压缩？

因为不同场景有不同需求：
- 自动压缩（Auto）是后备方案，不希望频繁使用
- 反应式压缩（Reactive）是用户控制的，精细度更高
- 微粒度压缩（Micro）是针对单个大消息的快速处理
- 片段压缩（Snip）是对模式重复的优化

---

### 24.3 Prompt Cache 与 Context Compression 的关系
**定义**：Anthropic 的 Prompt Cache 机制与 Claude Code 的 Context Compression 不是对立的，而是互补的。

**cache-key 前缀稳定性要求**：
```
Prompt Cache 原理：
  请求 1: [系统提示（2000 tokens）] [用户消息1]  → 前缀缓存建立
  请求 2: [系统提示（2000 tokens）] [用户消息2]  → 前缀命中，复用缓存 ✓
  请求 3: [系统提示（2500 tokens）] [用户消息3]  → 前缀改变（压缩发生），缓存失效 ✗
```

**权衡**：压缩发生时会改变系统提示（从 2000 删减到 1500），导致 cache-key 前缀变化，缓存失效。

这意味着：
- 频繁压缩 = 频繁缓存失效 = 无法充分利用 Prompt Cache 的成本优势
- 压缩过少 = 无法处理超长会话
- 压缩间隔需要平衡（通常设计为 20-50 轮对话触发一次）

---

### 24.4 AutoCompact 触发机制
**定义**：`src/services/compact/autoCompact.ts` 中的 `AUTOCOMPACT_BUFFER_TOKENS`（约第 62 行）定义了自动压缩的触发阈值。

**源码细节**：
```
AUTOCOMPACT_BUFFER_TOKENS = 5000  // 预留 5000 tokens 的安全缓冲
isAutoCompactEnabled = true        // 全局启用开关（第 147 行）

触发条件 = Context Window Limit - 已用 tokens < AUTOCOMPACT_BUFFER_TOKENS
```

**设计意图**：为什么要预留 5000 tokens？

因为：
- Claude 的输出通常 500-2000 tokens
- API 处理需要额外开销
- 预留缓冲确保"即使 Claude 生成最长的回复也不会超限"

**实际数据**：
- Opus 上下文：200K tokens
- Sonnet 上下文：200K tokens
- Haiku 上下文：100K tokens
- 系统提示通常：2-5K tokens
- 一轮对话（输入+输出）：500-3000 tokens

如果没有 5000 token 的缓冲，当剩余容量 < 3000 时就会出现"Claude 生成回复时溢出"的风险。

---

### 24.5 四种策略的选择逻辑
**定义**：系统在运行时动态选择合适的压缩策略。

**决策树**：
```
是否超过 AutoCompact 阈值？
  是 → 触发 AutoCompact（粗粒度，删除早期轮次）
  否 → 检查单条消息大小
       是（单条 > 50K tokens）→ 触发 MicroCompact
       否 → 检查内容冗余度
            冗余度高（> 40%） → 触发 SnipCompact
            否 → 使用 ReactiveCompact（用户主动）或无压缩
```

**设计权衡**：
- 完全自动化：可能在不当时机压缩，损害用户体验
- 完全手动化：用户容易遗忘压缩，导致会话崩溃
- 混合模式（现状）：大多数自动，关键时刻用户可干预

---

### 24.6 性能与成本影响
**定义**：Context 压缩的成本（计算、可能的信息损失）vs 收益（避免超限、保持缓存命中）

**成本数据**：
| 方面 | 无压缩 | AutoCompact | Micro/Snip |
|------|--------|------------|-----------|
| 首次 API 请求 tokens | 50K | 25K（压缩后） | 50K |
| Token 成本 | $7.50 | $3.75 | $7.50 |
| 压缩计算时间 | 0ms | 500-1000ms | 50-200ms |
| Prompt Cache 命中率 | 高 | 中→低（压缩后前缀变） | 高 |
| 会话可持续性 | 否（溢出） | 是 | 是 |

**实际场景**：
- 50 轮对话会话：无压缩会在第 70 轮溢出；AutoCompact 可持续到第 200 轮
- 压缩成本：第 50 轮时触发一次 AutoCompact，1000ms 延迟，代价是"历史被浓缩"

---

## 图表清单

1. **图 24-1：四种压缩策略的 Token 演化图**（Mermaid 折线图或柱状图）
   - X 轴：对话轮次（0-100）
   - Y 轴：系统提示 + 对话历史的 token 总数
   - 四条线分别代表四种策略的 token 增长曲线
   - 显示压缩事件（竖线标记）

2. **图 24-2：Prompt Cache 命中率与压缩频率的关系**（Mermaid 散点图或热力图）
   - X 轴：压缩发生频率（每 N 轮触发一次）
   - Y 轴：Prompt Cache 命中率（%）
   - 显示最优点（通常 20-50 轮触发一次时缓存命中率最高）

3. **图 24-3：AutoCompact 阈值与 Context Window 的关系**（ASCII 表达式或 Mermaid 流程图）
   ```
   Context Window: 200K
        │
        ├─ 安全区：0-195K（正常）
        │  
        ├─ 黄色区：195K-200K（接近限制）
        │   └─ 触发 AutoCompact
        │
        └─ 红色区：> 200K（溢出，API 拒绝）
   ```

4. **表格 24-1：四种策略的成本-收益对比**（Markdown 表格）
   - 列：策略名、计算成本、信息损失、缓存友好度、使用频率

5. **图 24-4：历史大小演化曲线**（时间序列）
   - 显示随着对话轮数增长，各策略下的"有效上下文大小"如何变化

---

## 写作顺序

**建议遵循此顺序编写章节内容**：

1. **开篇（工程共鸣）**：讲述真实场景 — "我在一个复杂项目上连续工作了 3 小时，与 Claude 进行了 80 轮对话。突然，Claude 返回了一个错误：'Tokens exceeded'。系统提示从 2KB 变成了 50KB，历史累积了 150KB。Claude Code 是怎么在这种情况下还能继续工作的？"

2. **24.1 - 对话增长的悖论**：阐述问题本质，建立读者对"为什么需要压缩"的理解

3. **24.2 - 四种策略总览**：快速介绍四种方案（表格为主），不深入实现细节

4. **24.3 - Prompt Cache 的关系**：连接到第 21 章的 context，说明压缩对缓存的影响

5. **24.4 - AutoCompact 机制**：讲解最常用的自动压缩（但第 25 章会深入）

6. **24.5 - 策略选择逻辑**：决策树，帮助读者理解"什么时候用哪个策略"

7. **24.6 - 性能与成本**：数据驱动的论述，说明每个策略的实际代价

8. **模式提炼**：总结 5-7 个可复用的设计模式

9. **用户能做什么**：给出 6-8 个可操作的建议

10. **Mermaid 图表**：在相应章节内插入，支持论述

---

## 写作前 Checklist

- [ ] 已读取 spec 文件的完整内容
- [ ] 已定位所有源码锚点（autoCompact.ts 的 AUTOCOMPACT_BUFFER_TOKENS、isAutoCompactEnabled 等）
- [ ] 已验证四个 compact 文件（autoCompact.ts、reactiveCompact.ts、microCompact.ts、snipCompact.ts）存在且包含源码
- [ ] 已理解与 ch21 的关联（context 作为 cache-key）
- [ ] 已理解与 ch25 的分工（ch24 总览，ch25 深入 AutoCompact 实现）
- [ ] 已准备好具体数字（token 数、延迟、成本）
- [ ] 已列出至少 3 个反证场景（如"为什么不只用激进压缩"）
- [ ] 已设计 4+ 个 Mermaid 图表

---

## 估算数据

- **完成时间**：2-3 小时
- **预期字数**：1200-1500 字
- **源码锚点**：8-12 个
- **图表**：4-5 个
- **表格**：2-3 个
- **模式提炼**：5 个
- **用户能做什么**：7 个建议

---

## 风格指南（工程师解剖）

- ✓ 开篇必须从真实问题触发（不是抽象的"缓存机制"，而是"对话越来越长时会怎样"）
- ✓ 每个决策都要有"为什么不选别的方案"的权衡论述
- ✓ 使用具体的 token 数字，不用"很多"、"通常"
- ✓ 四种策略的对比表是核心，要突出差异
- ✓ 与 ch21、ch23、ch25 的关联要明确指出
- ✓ 源码引用要精确到行号

---

**计划生成时间**：2026-04-04  
**目标完成时间**：2026-04-04 后 3-4 小时  
**质量目标**：8.5+ 分（autopsy 风格）
