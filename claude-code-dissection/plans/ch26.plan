# 第 26 章 - 本地记忆系统：memdir 与 SessionMemory
## 写作计划（Plan）

---

## 前置阅读清单

- **第 23 章**：会话系统 — 理解会话如何持久化
- **第 19 章**：系统提示 — 理解记忆如何被注入到系统提示

---

## 章节骨架

### 26.1 记忆架构概览
**问题**：Claude Code 对话结束后，它"记住了什么"？下次打开项目时能想起来吗？

**答案**：记忆分两层：
- **会话内记忆**（SessionMemory）：一个会话内的临时记忆
- **跨会话记忆**（memdir）：持久化到文件系统的长期记忆

### 26.2 memdir 结构与 MEMORY.md 入口
**核心**：`src/memdir/memdir.ts` 第 272 行的 `buildMemoryPrompt()`

memdir 是一个目录树，包含多个 markdown 文件。MEMORY.md 是入口索引。

**源码锚点**：
- buildMemoryPrompt（第 272 行）
- loadMemoryPrompt（第 419 行）
- MAX_ENTRYPOINT_BYTES = 25000（第 38 行）
- ENTRYPOINT_NAME = "MEMORY.md"

**设计意图**：为什么要一个入口索引？

因为记忆文件可能很多（几十个或更多），Claude 不能在系统提示中放入所有文件（会浪费 token）。MEMORY.md 是一个目录和摘要，Claude 可以根据摘要判断是否需要读取更详细的记忆文件。

### 26.3 MAX_ENTRYPOINT_BYTES 限制
**定义**：MEMORY.md 最多 25000 字节，超过则截断

**权衡**：
- 太小：记忆信息不足，Claude 无法准确想起关键信息
- 太大：浪费系统提示的 token 预算
- 25000 字节 ≈ 6250 tokens，在系统提示中占比约 10%

**数据对比**：
| 限制大小 | 记忆条数 | Token 占用 | 系统提示占比 |
|---------|---------|----------|----------|
| 5000 字 | ~100 条 | 1250 | 2% |
| 25000 字 | ~500 条 | 6250 | 10% |
| 100000 字 | ~2000 条 | 25000 | 40% |

25000 是一个平衡点。

### 26.4 SessionMemory 的提炼机制
**核心**：`src/services/SessionMemory/sessionMemory.ts` 的 `shouldExtractMemory()` 和 `initSessionMemory()`

每个 REPL 会话结束时，系统分析对话内容，提炼出"值得记住"的部分，写入 memdir。

**触发条件**：
```
会话结束 → 分析对话历史 → 提炼关键信息 → 写入 memdir
```

**设计意图**：为什么不自动保存所有对话？

因为：
- 对话量太大（一个 50 轮会话可能有 100KB+ 的对话历史）
- 大部分对话是临时的（调试过程、失败的尝试等）
- 系统提示的 token 预算有限

提炼机制选择出"有长期价值"的信息（如"项目的架构决策"、"已知的 bug"等），而忽略短期信息（如"第 15 轮的一个尝试"）。

### 26.5 跨会话记忆的可用性
**定义**：下次打开同一个项目时，memdir 中的记忆被加载回系统提示

**流程**：
```
新会话启动 → loadMemoryPrompt() → 读取 MEMORY.md → 解析链接
         → （如需要）读取详细记忆文件 → 注入系统提示
```

**设计权衡**：
- 加载时间：读取和解析 memdir 需要 100-200ms
- 记忆准确性：旧的记忆可能过时
- 隐私考量：跨会话记忆存储在本地文件系统

### 26.6 本地记忆与缓存的关系
**问题**：memdir 被加载后，作为系统提示的一部分，会改变 Prompt Cache 的 cache-key，导致缓存失效吗？

**答案**：是的，但这是可以接受的。因为跨会话记忆改变很少（几天一次），而单个会话内的缓存命中率仍然很高。

---

## 图表清单

1. **图 26-1：memdir 目录树结构**（ASCII 树状图）
   ```
   .claude/memory/
   ├── MEMORY.md (入口，25KB限制)
   ├── project-architecture.md
   ├── known-issues.md
   ├── decisions/
   │   ├── decision-2024-01-15.md
   │   └── decision-2024-01-20.md
   └── learnings/
       ├── python-tips.md
       └── performance-tricks.md
   ```

2. **图 26-2：SessionMemory 生命周期**（Mermaid 流程图）
   ```
   会话启动 → loadMemoryPrompt() → 注入系统提示
   ↓
   对话进行（50 轮）
   ↓
   会话结束 → shouldExtractMemory() → 提炼关键信息 → 写入 memdir
   ```

3. **表格 26-1：MAX_ENTRYPOINT_BYTES 的权衡**

4. **图 26-3：跨会话记忆的信息流**（Mermaid 序列图）

---

## 写作顺序

1. 开篇："每次对话结束后，Claude Code ' 记住了什么'？"
2. 26.1 - 架构概览
3. 26.2 - memdir 结构
4. 26.3 - 字节限制的设计
5. 26.4 - 提炼机制
6. 26.5 - 跨会话可用性
7. 26.6 - 与缓存的关系
8. 模式 + 用户能做什么

---

## 估算

- **时间**：2-2.5 小时
- **字数**：1100-1300 字
- **锚点**：6-8 个
- **图表**：4 个
