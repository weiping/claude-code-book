# 第 28 章 - MCP 认证：OAuth、JWT 与 xaa
## 写作计划（Plan）

---

## 前置阅读清单

- **第 27 章**：MCP 客户端 — 理解三种连接协议
- **第 23 章**：会话系统 — 理解令牌的持久化

---

## 章节骨架

### 28.1 MCP 认证的三条路径
**问题**：MCP 服务器需要登录时，Claude Code 怎么帮你完成认证？本地为什么要起一个 HTTP 服务器？

**答案**：三条路径，对应三种认证方式。

### 28.2 OAuth PKCE 流程
**核心**：`src/services/mcp/oauthPort.ts` 的 `buildRedirectUri()` 和 `findAvailablePort()`

**PKCE (Proof Key for Code Exchange) 流程**：
```
1. 用户启动 Claude Code
2. 系统检测 MCP 需要 OAuth
3. 本地启动临时 HTTP 服务器（随机端口，如 5678）
4. 构建 redirect_uri = http://localhost:5678/callback
5. 打开浏览器 → MCP 提供商的登录页面
6. 用户登录并授权
7. 提供商重定向到 redirect_uri
8. 本地服务器捕获授权码
9. 服务器用授权码交换 access_token
10. Token 存储到本地配置
11. 关闭临时 HTTP 服务器
```

**为什么需要本地 HTTP 服务器？**

因为 OAuth 的标准流程要求有一个"回调 URL"来接收授权码。对于 CLI 应用，没有公网地址，所以使用本地 localhost。

**安全考量**：
- PKCE：防止授权码拦截攻击
- 状态参数：防止 CSRF 攻击
- 令牌存储：保存在本地（~/.claude/mcp-tokens 或类似）

### 28.3 JWT 令牌刷新调度器
**核心**：`src/services/mcp/auth.ts` 的令牌刷新逻辑

**问题**：访问令牌（Access Token）通常有过期时间（如 1 小时）。一个长时间运行的 REPL 会话怎么保持认证状态？

**答案**：刷新令牌（Refresh Token）。

**调度器机制**：
```
收到 access_token（过期时间：1 小时）
  → 计算刷新时机：过期前 5 分钟
  → 设置定时任务（在 55 分钟后执行）
  → 时间到达 → 用 refresh_token 交换新的 access_token
  → 更新本地存储
  → 后续 MCP 请求使用新 token
```

**数据**：
- 典型访问令牌过期时间：1-24 小时
- 刷新提前量：5-10 分钟
- 刷新令牌有效期：14-30 天（较长）

### 28.4 xaa (Cross-App Access) 企业认证
**核心**：`src/services/mcp/xaa.ts` 的 `performCrossAppAccess()` 和 `XaaTokenExchangeError`

**定义**：xaa 是针对企业场景的认证机制。在企业环境中，用户在多个应用间切换时，不需要多次登录。

**工作原理**：
```
企业单点登录（如 Okta）已认证
  → Claude Code 向企业身份提供商请求 xaa 令牌
  → 提供商验证用户身份（已登录）
  → 下发 xaa 令牌
  → Claude Code 用 xaa 令牌向 MCP 服务器认证
  → MCP 服务器验证令牌（由同一个企业 IdP 签发）
  → 授予访问权限
```

**设计意图**：为什么企业需要 xaa？

在大型组织中，员工可能使用几十个工具和服务。如果每个工具都要独立登录，会导致：
- 用户疲劳（重复登录）
- 安全风险（密码重复或简化）
- 管理成本（多个账户）

xaa 通过信任企业的中央身份认证，简化流程。

### 28.5 令牌生命周期管理
**定义**：从获取、存储、使用、刷新到过期的完整流程

**存储位置**：
- 本地文件：~/.claude/mcp-tokens （不加密，需要权限保护）
- 系统钥匙链：macOS Keychain、Windows Credential Manager（更安全）

**生命周期阶段**：
```
1. 获取：首次认证时
2. 验证：每次使用前检查过期时间
3. 刷新：过期前自动刷新（调度器）
4. 失效：用户显式登出或令牌撤销
```

### 28.6 认证失败的处理
**定义**：OAuth 失败、令牌过期、xaa 验证失败时的降级策略

**策略**：
- 首次失败：立即重试（可能是临时网络问题）
- 第二次失败：提示用户重新认证（`/mcp auth re-login`）
- 重复失败：禁用该 MCP 服务器，建议用户检查配置

---

## 图表清单

1. **图 28-1：OAuth PKCE 完整序列图**（Mermaid sequenceDiagram）
   - 参与方：用户、本地 CLI、本地 HTTP 回调服务器、OAuth 提供商
   - 步骤：启动 → 打开浏览器 → 用户授权 → 回调捕获 → Token 交换

2. **图 28-2：JWT 令牌刷新的时间轴**（Mermaid 时间轴或 ASCII 表示）
   ```
   0min        55min       60min       期望的刷新点
   ├──────────┤───────────├──────────┤
   拿到        刷新(55min)  原过期时间   使用新token
   access_token  时刻执行    点
   ```

3. **表格 28-1：三种认证方式的对比**
   | 方式 | 适用场景 | 复杂度 | 安全性 | 企业支持 |
   |------|---------|--------|--------|---------|
   | OAuth | 互联网应用 | 高 | 中 | 通常支持 |
   | JWT | 企业内部 API | 低 | 高 | 支持 |
   | xaa | 大型企业 | 高 | 最高 | 专供 |

4. **图 28-3：令牌生命周期**（Mermaid 状态图）

5. **图 28-4：xaa 与 OAuth 的对比**（Mermaid）

---

## 写作顺序

1. 开篇："MCP 服务器需要登录时，Claude Code 怎么帮你完成认证？"
2. 28.1 - 三条路径概览
3. 28.2 - OAuth PKCE 深入
4. 28.3 - JWT 刷新调度
5. 28.4 - xaa 企业认证
6. 28.5 - 令牌生命周期
7. 28.6 - 失败处理
8. 模式 + 用户能做什么

---

## 估算

- **时间**：2-2.5 小时
- **字数**：1100-1300 字
- **锚点**：6-8 个
- **图表**：5 个
