# 第9章写作计划

**批次**：批次 2
**章节**：ch09 — Query Engine——多轮编排与状态管理
**风格**：autopsy 工程师解剖

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/QueryEngine.ts | QueryEngineConfig(130行)、class定义(184行)、submitMessage(209行)、abort(1159行) | 130-215, 360-380, 510-520, 1155-1165行 |
| 2 | src/utils/abortController.ts | createAbortController | 全文 |
| 3 | src/utils/fileStateCache.ts | cloneFileStateCache、FileStateCache 类型 | 1-40行 |

```bash
ls src/QueryEngine.ts src/utils/abortController.ts src/utils/fileStateCache.ts
sed -n '184p' src/QueryEngine.ts
```

## 章节骨架

### 开篇
- 开篇触发点：「`query.ts` 处理单轮，但对话是多轮的。谁来记住"我们已经聊到哪了"？`QueryEngine` 是 `query.ts` 的编排者。」
- 三个问题：1）多轮上下文如何累积？2）abort 信号如何穿透到 query.ts？3）fileStateCache 为什么需要跨轮传递？

### 节 9.1：一个 QueryEngine 管理一次对话
- 核心主张：QueryEngine 是有状态的——`mutableMessages` 跨轮累积，`abortController` 跨轮传递，每次 `submitMessage` 是一轮
- 源码锚点：`src/QueryEngine.ts:184`（class 定义 + 私有字段）、`src/QueryEngine.ts:209`（submitMessage 签名）
- 包含元素：QueryEngine 的私有字段表（哪些跨轮保持，哪些每轮重置）
- 估计字数：600字

### 节 9.2：abort 信号的生命周期
- 核心主张：一个 AbortController 贯穿整个 QueryEngine 的生命周期，abort() 调用会穿透到 query.ts 内部的 LLM 流和工具执行
- 源码锚点：`src/QueryEngine.ts:203`（abortController 初始化）、`src/QueryEngine.ts:368`（传递给 query()）、`src/QueryEngine.ts:1159`（abort() 方法）
- 包含元素：Mermaid 序列图，展示 REPL→QueryEngine.abort()→abortController→query.ts 信号流
- 权衡分析：单一 AbortController vs 每轮新建（单一：abort 能中断所有轮次；每轮新建：无法跨轮中断）
- 估计字数：600字

### 节 9.3：fileStateCache 为什么跨轮传递？
- 核心主张：多轮对话中工具读取的文件状态必须一致，使用缓存避免同一文件被重复读取产生不同结果
- 源码锚点：`src/QueryEngine.ts:193`（readFileState 字段）、`src/utils/fileStateCache.ts:cloneFileStateCache`
- 估计字数：400字

### 节 9.4：compact boundary 的处理
- 简要说明 QueryEngine 如何在上下文溢出时截断历史（指向第24-25章）
- 源码锚点：QueryEngine.ts 中对 compact boundary 消息的处理
- 估计字数：300字

### 章末：模式提炼
| 模式名 | 解决的问题 | 核心做法 | 前置条件 | 源码证据 |
|--------|-----------|---------|---------|---------|
| 有状态对话编排（Stateful Conversation Orchestration）| 多轮对话需要跨轮维护上下文 | 编排层持有消息历史和 abortController，原子循环层无状态 | 对话有多轮交互 | `src/QueryEngine.ts:184` |
| 穿透式取消（Percolating Cancellation）| 取消操作需要传播到深层嵌套的异步调用 | 在最顶层持有 AbortController，通过参数向下传递 | 有嵌套的异步调用链 | `src/QueryEngine.ts:1159,368` |

## 图表清单
| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图9-1 | QueryEngine 状态字段生命周期 | 表格+flowchart | 哪些字段跨轮持久，哪些每轮重置 | 9.1 |
| 图9-2 | abort 信号传播路径 | Mermaid sequenceDiagram | REPL → QueryEngine → query.ts 的信号传播 | 9.2 |

## 写作顺序
1. 写节 9.1（QueryEngine 基本结构）
2. 写节 9.2（abort 信号，最重要的设计决策）
3. 写节 9.3（fileStateCache）
4. 写节 9.4（compact，简短，指向后续章节）
5. 模式提炼，开篇

## 写作前 checklist
```bash
sed -n '184,210p' src/QueryEngine.ts         # class 定义 + 私有字段
sed -n '203p' src/QueryEngine.ts             # abortController 初始化
sed -n '368p' src/QueryEngine.ts             # 传递给 query()
sed -n '1159p' src/QueryEngine.ts            # abort() 方法
```

## 估算
- 目标字数：2200-2600字
- 预计轮次：7轮（含2轮验证）
