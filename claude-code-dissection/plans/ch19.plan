# 第 19 章写作计划

**批次**：批次 5（第五篇：提示词工程）
**章节**：ch19 — 第19章：系统提示的优先级堆叠与组装
**风格**：autopsy（工程师解剖）

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/utils/systemPrompt.ts | buildEffectiveSystemPrompt 函数及五级优先级注释 | 行号 30-65 |
| 2 | src/bootstrap/state.ts | getMainLoopSystemPrompt、setMainLoopSystemPrompt 状态管理 | 行号 200-250 |
| 3 | src/utils/systemPrompt.ts | Override（loop mode）的处理逻辑 | 行号 60-100 |
| 4 | src/services/api/buildSystemPromptInput.ts | systemPrompt 如何被注入 API 请求 | 行号 1-50 |
| 5 | src/utils/agentDefinition.ts | Agent 系统提示在 proactive 模式下的追加逻辑 | 行号 50-100 |
| 6 | src/replLauncher.tsx | REPL 启动时 systemPrompt 的初始化 | 行号 100-150 |

验证文件存在：
```bash
ls -l src/utils/systemPrompt.ts \
      src/bootstrap/state.ts \
      src/utils/agentDefinition.ts \
      src/services/api/buildSystemPromptInput.ts
```

## 章节骨架

### 开篇（必填）
- **开篇触发点**：`--system-prompt` 参数和 CLAUDE.md 文件都能设置系统提示——谁说了算？如果三种提示同时存在，优先级顺序是什么？
- **工程问题**：系统必须支持多层次的提示注入（全局/项目/用户/命令行），但它们必须在某个地方合并。这个合并逻辑是系统提示工程的核心。
- **权衡**：灵活性（支持多种覆盖方式）vs 可预测性（清晰的优先级顺序）

### 节 19.1：五级优先级堆栈
- **核心主张**：系统提示由五个优先级别组成，每一级都有特定的触发条件。最高优先级是 Override（完全替换所有），最低优先级是 Default（标准提示）。
- **源码锚点**：`src/utils/systemPrompt.ts:30-38` — 五级优先级的完整列表与注释
- **概念层次**：定义 / 源码实现 / 实际场景
- **权衡分析**：为什么用五级而不是三级或十级？五级的必要性在于：Override 必须存在（loop mode 绝对覆盖）；Coordinator 可能为 stub 但必须预留槽位；Agent 与 Custom 必须分开以支持 proactive 模式的追加；Default 是后备（总是存在）。
- **包含元素**：ASCII 优先级堆栈图 + 表格（每级的触发条件 + 何时为空）
- **估计字数**：600-800 字
- **对应 spec 场景**：verify_five_level_priority_order

### 节 19.2：Override 模式——绝对替换
- **核心主张**：Override 优先级为 0，一旦设置就完全替换其他所有提示（包括 appendSystemPrompt 也被跳过）。
- **源码锚点**：`src/utils/systemPrompt.ts:47-55` — Override 判断与替换逻辑
- **概念层次**：定义（什么是 Override）/ 设计意图（为什么需要它）/ 实际案例（loop mode 何时触发 Override）
- **权衡分析**：
  - 为什么 Override 会跳过 appendSystemPrompt？完全替换的语义要求不能有"追加"，否则 Override 就不是真正的"绝对"替换。
  - 为什么不用 Override 代替 Custom？Custom 只替换 Default，不替换 Coordinator 和 Agent，两者适用场景不同。
- **包含元素**：代码片段展示 Override 判断、对比表格（Override vs Custom）
- **估计字数**：500-700 字
- **对应 spec 场景**：verify_override_semantics

### 节 19.3：Agent 提示的双模式——替换与追加
- **核心主张**：Agent 系统提示有两种行为：在 proactive 模式下追加到 Default 之后；在普通模式下替换 Default。这个设计支持"自治 Agent + 团队指令"的协作模式。
- **源码锚点**：`src/utils/systemPrompt.ts:56-65` — Agent 提示的 proactive 判断逻辑
- **概念层次**：设计意图 / 源码实现 / 使用场景
- **权衡分析**：
  - 追加而非替换的原因：proactive 模式中，Agent 是"队友"角色，自治指令是基础，用户指令叠加在上。如果替换，会丢失 Agent 的基础行为。
  - 替换而非追加的原因（普通模式）：用户手动指定 Agent 提示时，通常想要该 Agent 的完整行为，而非混合。
- **包含元素**：代码片段展示 isProactiveActive 判断、伪代码展示两种场景的合并结果
- **估计字数**：600-800 字
- **对应 spec 场景**：verify_agent_proactive_append

### 节 19.4：Custom 与 Default 的替换关系
- **核心主张**：Custom 提示（通过 `--system-prompt` 参数）直接替换 Default 提示，是用户最直接的覆盖方式。
- **源码锚点**：`src/utils/systemPrompt.ts:41-45` — buildEffectiveSystemPrompt 的 Custom 判断
- **概念层次**：定义 / 实现逻辑 / 配置示例
- **权衡分析**：Custom 为什么不追加而是替换？默认提示包含系统特定的行为（工具使用、权限模型等），替换而非追加能保证用户指定的提示是完整的、自洽的。
- **包含元素**：示例命令 `claude --system-prompt "..."`、调用堆栈图
- **估计字数**：400-500 字
- **对应 spec 场景**：verify_custom_vs_default_conflict

### 节 19.5：appendSystemPrompt 的追加位置
- **核心主张**：appendSystemPrompt（通过 `--append-system-prompt` 参数）始终追加在所有其他提示的末尾，**除非 Override 模式激活**。这保证了追加内容是最后被看到的。
- **源码锚点**：`src/utils/systemPrompt.ts:60-70` — appendSystemPrompt 的追加时机
- **概念层次**：实现逻辑 / 使用场景 / 限制条件
- **权衡分析**：为什么总是末尾而非其他位置？LLM 对序列中"最后看到"的内容记忆最深，因此追加内容应该最靠后以确保被重视。
- **包含元素**：示例展示 Override 模式下 appendSystemPrompt 被忽略的情况
- **估计字数**：400-600 字
- **对应 spec 场景**：verify_append_system_prompt_position

### 节 19.6：状态管理与 REPL 初始化
- **核心主张**：系统提示在 REPL 启动时通过 `getMainLoopSystemPrompt` 从全局状态读取，后续可通过 `/set system-prompt` 命令动态修改。
- **源码锚点**：`src/bootstrap/state.ts:getMainLoopSystemPrompt/setMainLoopSystemPrompt`、`src/replLauncher.tsx:120-150` — REPL 初始化
- **概念层次**：状态流向 / API 集成 / 修改流程
- **权衡分析**：为什么系统提示存储在全局 state 而非 SessionMemory？全局 state 确保多轮对话保持一致，Session 级修改会被覆盖。
- **包含元素**：状态流向图、修改前后的 LLM 请求对比
- **估计字数**：500-700 字
- **对应 spec 场景**：verify_source_citations_exist

### 章末：模式提炼（必填）

| 模式名 | 解决的问题 | 核心做法（一句话） | 前置条件 | 源码证据锚点 |
|--------|-----------|-----------------|---------|------------|
| 五级优先级堆栈 | 多源提示如何不冲突 | 定义五个优先级（Override>Coordinator>Agent>Custom>Default），后处理时按级覆盖 | 有多个提示源 | `src/utils/systemPrompt.ts:30-65` |
| Override 绝对替换 | 需要彻底改变 Claude 行为的场景 | 检查 overrideSystemPrompt 存在时跳过其他所有提示源 | loop mode 或显式 override | `src/utils/systemPrompt.ts:47-55` |
| Proactive 追加与替换二元 | Agent 既要有自治行为又要听用户指令 | 在 proactive 模式追加，否则替换；通过 isProactiveActive 判断 | Agent 定义存在 | `src/utils/systemPrompt.ts:56-65` |
| 末尾追加策略 | 追加内容容易被忽略 | appendSystemPrompt 始终加在最后（除 Override），确保 LLM 重视 | 需要追加而非替换 | `src/utils/systemPrompt.ts:60-70` |

### 章末：用户能做什么（必填）

阅读本章后，工程师能做到：
1. **诊断优先级冲突**：当系统提示行为不符合预期时，能按五级顺序逐一排查哪一级覆盖了预期值
2. **设计多租户系统**：理解如何在共享环境中用 Override 隔离租户提示，同时保留 Custom 和 Append 的灵活性
3. **实现 Agent 协作模式**：在 proactive 模式下追加 Agent 提示，实现"自治基础+团队指令"的双层架构
4. **调试提示合并**：通过追踪 buildEffectiveSystemPrompt 的执行路径，理解最终发送给 Claude 的是哪个版本的提示
5. **设计扩展提示系统**：基于五级结构，为新场景添加第六级、第七级优先级而不破坏现有逻辑
6. **保证追加内容被重视**：使用 appendSystemPrompt 而非修改 Custom，确保内容被放在序列末尾
7. **处理提示冲突场景**：识别 Custom 与 CLAUDE.md（第 20 章）冲突时的优先级关系

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图19-1 | 系统提示优先级堆栈 | ASCII 或 Mermaid | 五级优先级的依赖关系，每级的触发条件 | 19.1 |
| 图19-2 | Override vs Custom vs Append | 对比表格 | 三种覆盖方式的适用场景、替换范围、API 接口 | 19.2-19.5 |
| 图19-3 | 状态流向图 | Mermaid flowchart | main.tsx 启动 → getMainLoopSystemPrompt → buildEffectiveSystemPrompt → API 请求 | 19.6 |

## 写作顺序

1. **先写 19.1**（五级堆栈定义）— 是本章概念基础，其他所有节依赖这个框架
2. **写 19.2**（Override）— 最高优先级，逻辑最清晰
3. **写 19.3**（Agent proactive）— 最复杂的模式，需要清晰的伪代码和对比
4. **写 19.4**（Custom）— 用户最常用的方式，相对简单
5. **写 19.5**（Append）— 交叉特性，需要 19.2-19.4 的上下文
6. **写 19.6**（状态管理）— 运行时流向，整合前面的静态结构
7. **补写模式提炼** — 此时全章逻辑已清晰，五个模式自然浮现
8. **补写开篇** — 最后写，此时知道全章讲了什么，才能写出真正的"为什么重要"
9. **补写用户能做什么** — 根据模式提炼，生成 7-8 条行动建议

## 写作前 checklist

执行以下命令确认所有源码锚点存在：
```bash
PROJECT_ROOT="$(pwd)"
BOOK_ROOT="${PROJECT_ROOT}/book"

# 验证 ch19 的所有源码锚点
sed -n '30,38p' "${PROJECT_ROOT}/src/utils/systemPrompt.ts" | grep -q "Override\|Coordinator\|Agent\|Custom\|Default" && echo "✓ systemPrompt.ts:30-38 五级注释存在" || echo "✗ 不存在"

sed -n '41,65p' "${PROJECT_ROOT}/src/utils/systemPrompt.ts" | grep -q "buildEffectiveSystemPrompt" && echo "✓ buildEffectiveSystemPrompt 函数存在" || echo "✗ 不存在"

sed -n '47,55p' "${PROJECT_ROOT}/src/utils/systemPrompt.ts" | grep -q "override\|Override" && echo "✓ Override 逻辑存在" || echo "✗ 不存在"

# 验证 bootstrap/state.ts 中的状态函数
grep -q "getMainLoopSystemPrompt\|setMainLoopSystemPrompt" "${PROJECT_ROOT}/src/bootstrap/state.ts" && echo "✓ state.ts 中的系统提示管理函数存在" || echo "✗ 不存在"

echo "✓ 所有 ch19 锚点检查完毕"
```

如果任何锚点不存在，重新搜索正确行号：
```bash
grep -n "buildEffectiveSystemPrompt\|five.level\|五级" "${PROJECT_ROOT}/src/utils/systemPrompt.ts"
```
