# 第4章写作计划

**批次**：批次 1
**章节**：ch04 — Feature Flag 的双层架构
**风格**：autopsy 工程师解剖

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | src/main.tsx | feature() 调用集中段（第76-172行） | 70-175行 |
| 2 | src/services/analytics/growthbook.ts | initializeGrowthBook(第622行)、checkGate_CACHED_OR_BLOCKING(第904行) | 620-640，900-920行 |
| 3 | src/tools.ts | feature() 条件加载工具（第26-55行） | 15-60行 |

```bash
ls src/main.tsx src/services/analytics/growthbook.ts src/tools.ts
```

## 章节骨架

### 开篇
- 开篇触发点：「普通用户和 Anthropic 内部员工安装的 `claude` 命令，二进制文件体积不同。为什么？」
- 可选题词：N/A
- 三个问题：
  1. 编译期 `feature()` 和运行期 A/B flag 有什么本质区别？
  2. 60+ 个 flag 是混乱还是有组织的？
  3. 为什么有些功能对外部用户"物理不存在"？

### 节 4.1：两层 flag 的架构对比
- 核心主张：编译期 flag（`bun:bundle feature()`）删除代码，运行期 flag（GrowthBook）切换行为——两者解决不同问题
- 源码锚点：
  - `src/main.tsx:21` — `import { feature } from 'bun:bundle'`（编译期层）
  - `src/services/analytics/growthbook.ts:622` — `initializeGrowthBook`（运行期层）
- 概念层次：定义（两层是什么）+ 设计意图（各自为什么存在）
- 权衡分析：编译期：修改需要重新构建发布，但产物更小、内部代码不外泄；运行期：可实时推送但存在运行时开销
- 包含元素：**对比表**（编译期 vs 运行期：触发时机/修改成本/对外用户可见性/适用场景）
- 估计字数：600字
- 对应 spec 场景：编译期消除语义正确；双层对比表可读

### 节 4.2：编译期 flag 的工作机制
- 核心主张：`feature('KAIROS')` 在 Bun 编译时被替换为字面量 `false`，整个 `require(...)` 分支物理消失
- 源码锚点：
  - `src/main.tsx:76` — `feature('COORDINATOR_MODE') ? require(...) : null`
  - `src/main.tsx:80-81` — `feature('KAIROS') ? require(...) : null`
  - `src/tools.ts:26-27` — `feature('PROACTIVE') || feature('KAIROS')` 条件工具加载
- 包含元素：代码片段（main.tsx 第76-82行，不超过15行）+ 说明"编译后 null 分支不存在"
- 估计字数：500字
- 对应 spec 场景：编译期消除语义正确；不泄露 stub 模块实现

### 节 4.3：flag 的四类分类
- 核心主张：60+ flag 按用途分为已发布功能开关、实验特性、内部测试、紧急死亡开关四类
- 源码锚点：
  - `TRANSCRIPT_CLASSIFIER`（`src/main.tsx:171`）— 实验特性类
  - `VOICE_MODE`（`src/tools.ts` 或 main.tsx）— 功能开关类
  - `BREAK_CACHE_COMMAND`（commands.ts）— 内部测试类
- 包含元素：分类表（4类 × 示例flag × 用途说明）
- 估计字数：500字
- 对应 spec 场景：flag 分类有代表性示例

### 节 4.4：运行期 GrowthBook 层
- 核心主张：GrowthBook 实现实时 A/B 推送，不需要发布新版本就能改变行为
- 源码锚点：`src/services/analytics/growthbook.ts:904` — `checkGate_CACHED_OR_BLOCKING`
- 包含元素：简要说明 GrowthBook 的初始化时机和缓存机制，不深入配置格式
- 估计字数：400字

### 章末：模式提炼

| 模式名 | 解决的问题 | 核心做法 | 前置条件 | 源码证据 |
|--------|-----------|---------|---------|---------|
| 双层 Flag 架构（Two-Layer Flag Architecture）| 内部/外部用户代码分离 + 实时功能推送 | 编译期 DCE 处理代码存在性，运行期 A/B 处理行为切换 | 有内外部用户区分需求 | `src/main.tsx:21,76` |
| 显式死代码消除（Explicit Dead Code Elimination）| 内部代码随产物泄露 | 用 `feature()` 包裹内部代码，编译时自动移除 | 有不同构建目标 | `src/main.tsx:80` |

### 章末：用户能做什么

1. 用编译期 flag 隔离内部测试代码，避免外部用户可见
2. 评估功能是否应该用"编译期门控"还是"运行期门控"
3. 为项目建立 flag 分类体系（已发布/实验/内部/死亡开关）
4. 查看附录 B 了解 Claude Code 的完整 flag 清单

## 图表清单

| # | 图表标题 | 类型 | 内容描述 | 对应节 |
|---|---------|------|---------|--------|
| 图4-1 | 编译期 vs 运行期 flag 对比 | ASCII 对比表 | 触发时机/修改成本/可见性/适用场景 | 4.1 |

## 写作顺序

1. 先写节 4.2（编译期机制，最具体）
2. 写节 4.1（两层对比，基于 4.2 建立的具体理解）
3. 写节 4.3（flag 分类）
4. 写节 4.4（GrowthBook，简短）
5. 写模式提炼
6. 最后写开篇

## 写作前 checklist

```bash
sed -n '21p' src/main.tsx                            # bun:bundle import
sed -n '76,82p' src/main.tsx                         # feature() 条件加载
sed -n '171p' src/main.tsx                           # TRANSCRIPT_CLASSIFIER
sed -n '622p' src/services/analytics/growthbook.ts   # initializeGrowthBook
sed -n '904p' src/services/analytics/growthbook.ts   # checkGate_CACHED_OR_BLOCKING
sed -n '26,28p' src/tools.ts                         # feature() 条件工具加载
```

## 估算

- 目标字数：2200-2700字
- 预计轮次：6轮（含2轮验证）
