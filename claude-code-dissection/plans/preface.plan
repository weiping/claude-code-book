# 序文写作计划

**批次**：批次 1
**章节**：preface — 序：为什么要解剖 Claude Code
**风格**：autopsy 工程师解剖（叙述性，无代码密度要求）

## 前置阅读清单

| # | 文件路径 | 重点关注 | 预计阅读行数 |
|---|---------|---------|------------|
| 1 | CLAUDE.md | 整体项目说明、stub 模块列表、60-70% 完整度声明 | 全文（约100行）|
| 2 | DESIGN.md | 核心主张、11篇结构、写作风格声明 | 全文（约300行）|

```bash
ls CLAUDE.md DESIGN.md
```

## 章节骨架

### 开篇
- 开篇触发点：工具启动时那 200ms 的冷启动，Anthropic 工程师真的手写了并发优化
- 可选题词：「官方文档告诉你怎么用，但从不解释为什么这样设计」
- 三个问题：
  1. 为什么要反向工程一个已有文档的工具？
  2. 从源码读出的东西，是官方文档给不了的吗？
  3. 读完这本书能看懂哪类工程决策？

### 节 0.1：写作动机——接口契约 vs 实现决策
- 核心主张：官方文档描述接口契约，本书解析实现决策
- 源码锚点：`src/main.tsx:2-8`（启动注释说明并发原因）
- 包含元素：引用 main.tsx 开头注释作为"源码里藏着设计理由"的具体案例
- 估计字数：300字

### 节 0.2：阅读地图——11篇导读
- 核心主张：11篇形成从骨架到血肉的递进结构
- 包含元素：简洁表格，每篇一句话
- 估计字数：300字

### 节 0.3：源码说明——v2.1.88 重建版本的局限
- 核心主张：60-70% 完整度是充分的，stub 模块已明确排除
- 包含元素：简短说明 stub 模块列表，解释如何验证书中引用（路径:行号格式）
- 估计字数：200字

### 节 0.4：致谢
- 估计字数：100字

## 图表清单

无（序文不含图表）

## 写作顺序

1. 先写节 0.3（源码说明，最具体，定基调）
2. 写节 0.1（动机，基于 0.3 建立的"可验证性"概念展开）
3. 写节 0.2（导读地图，机械性工作）
4. 写节 0.4（致谢）
5. 最后写开篇（此时知道整体基调）

## 写作前 checklist

```bash
sed -n '2,8p' src/main.tsx    # 验证并发启动注释存在
```

## 估算

- 目标字数：900-1000字
- 预计轮次：5轮（含2轮验证）
