# 第 27 章 - MCP 客户端的三协议实现
## 写作计划（Plan）

---

## 前置阅读清单

- **第 7 章**：工具系统 — 理解 Tool 接口和工具执行流程
- **第 15 章**：权限检查 — 理解工具的权限模型

---

## 章节骨架

### 27.1 MCP 协议选择的困境
**问题**：用户配置了一个 MCP 服务器（如 GitHub MCP），Claude Code 是怎么连上去、拿到工具列表的？有几种方式？

**答案**：三种传输协议，每种适用不同的部署场景。

### 27.2 三种传输协议对比
**核心**：`src/services/mcp/client.ts` 第 595 行的 `connectToServer()` 和第 626-683 行的协议选择逻辑

**三种协议**：
1. **SSE (Server-Sent Events)**
   - 用途：HTTP 长连接，适合已有 HTTP 服务的 MCP
   - 认证：支持 Bearer Token、API Key
   - 复杂度：低
   - 延迟：高（HTTP 往返）

2. **Stdio**
   - 用途：进程间通信，适合本地命令行工具
   - 认证：通常无认证（进程启动时）
   - 复杂度：低
   - 延迟：极低

3. **StreamableHTTP**
   - 用途：高带宽 HTTP 流，适合大文件传输或高频请求
   - 认证：支持 OAuth、JWT
   - 复杂度：高
   - 延迟：中等

**对比表**：
| 协议 | 连接类型 | 适用场景 | 认证支持 | 延迟 | 源码位置 |
|------|---------|---------|---------|------|---------|
| SSE | HTTP 长连接 | 远程 HTTP 服务 | Bearer/API Key | 高 | 第 626 行 |
| Stdio | 进程 stdin/stdout | 本地命令行工具 | 无 | 极低 | 第 650 行 |
| StreamableHTTP | 流式 HTTP | 大文件/高频 | OAuth/JWT | 中 | 第 683 行 |

### 27.3 MCP 工具的包装转换
**核心**：MCPTool 和 `createMcpAuthTool()`

MCP 服务器的工具定义（JSON）需要被转换为 Claude Code 的内部 Tool 对象，才能在工具系统中使用。

**转换流程**：
```
MCP 工具定义 (JSON) 
  → MCP SDK 解析 
  → MCPTool 对象 
  → createMcpAuthTool() 
  → 内部 Tool 对象
  → 工具系统（第 7 章）
```

**源码锚点**：第 9-15 行的导入语句，以及工具包装逻辑。

### 27.4 资源和 Prompt 的动态注册
**定义**：MCP 不仅提供工具，还可以提供"资源"（文件、数据库连接等）和"Prompt 模板"

**设计意图**：为什么支持资源和 Prompt？

因为某些 MCP 服务器除了提供工具，还想提供数据或指令模板。例如：
- GitHub MCP 提供工具（查询 PR、创建 issue）
- 同时提供资源（仓库文件内容）
- 和 Prompt 模板（"如何审查 PR"的步骤）

Claude Code 的 MCP 客户端需要统一处理这三种资产。

### 27.5 连接复用与 memoize
**核心**：`connectToServer()` 使用 memoize，同一地址只连接一次

**设计意图**：减少重复连接的开销

**数据**：
- 建立 SSE 连接：300-500ms
- 建立 Stdio 连接：100-200ms
- 缓存复用：1-2ms

在一个 REPL 会话中可能调用同一个 MCP 工具 10+ 次，memoize 能显著降低延迟。

### 27.6 协议选择的启发式规则
**定义**：用户配置 MCP 时，系统如何自动选择合适的协议？

**规则**：
```
如果用户提供 URL 且以 http/https 开头
  → 选择 SSE（或 StreamableHTTP，取决于负载）
否则如果用户提供命令名称
  → 选择 Stdio
否则
  → 报错
```

---

## 图表清单

1. **图 27-1：三种协议的连接流程对比**（Mermaid，三列并行）

2. **表格 27-1：三种协议的详细对比**

3. **图 27-2：MCP 工具包装转换流程**（Mermaid 流程图）

4. **图 27-3：连接复用效果**（柱状图对比有无 memoize 的延迟）

---

## 写作顺序

1. 开篇："你配置了一个 MCP 服务器，Claude Code 是怎么连上去的？"
2. 27.1 - 协议选择困境
3. 27.2 - 三种协议
4. 27.3 - 工具转换
5. 27.4 - 资源和 Prompt
6. 27.5 - 连接复用
7. 27.6 - 选择规则
8. 模式 + 用户能做什么

---

## 估算

- **时间**：2-2.5 小时
- **字数**：1100-1300 字
- **锚点**：5-7 个
- **图表**：4 个
