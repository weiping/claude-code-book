spec: task
name: "第42章：可扩展性的四种机制"
tags: [book-chapter, part-11]
---

## 意图

本章系统提炼 Claude Code 的可扩展性设计。核心问题：Feature flag 模块化、Tool 工厂模式、Plugin/Skill 包格式、MCP 协议——这四种扩展机制各自解决什么问题？它们的边界在哪里？选择哪种取决于什么？

## 约束

必须：
- 用对比表展示四种机制：扩展粒度、修改是否需要重新构建、面向的扩展者（内部/外部）、适用场景
- 引用 src/Tool.ts 中的 buildTool 作为工厂模式机制的代表
- 引用 src/services/mcp/client.ts 中的 connectToServer 作为 MCP 协议机制的代表
- 各机制给出"当它不是正确选择时"的反例，避免过度使用

禁止：
- 不得重复各章节的实现细节（用交叉引用代替）
- 不得引入本书未分析过的新扩展机制
- 不得仅罗列功能，每种机制必须有"适用 vs 不适用"的判断标准

## 已定决策

- 本章为全书收尾的工程原则提炼章，代码密度最低，重在决策框架
- 开篇问题："下次你想扩展一个 CLI 工具，你会用哪种方式？四种机制中你怎么选？"
- 四种机制各用一节，格式统一：一句话定义 → 源码证据 → 适用场景 → 不适用场景
- 源码引用格式：`src/Tool.ts:buildTool`（仅用于标注来源，不深入展开）

## 边界

### 允许修改
- book/src/part11/ch42.md

### 禁止做
- 不修改 DESIGN.md 或其他章节文件
- 不引入本书未分析的新机制

## 验收标准

场景: 四种机制对比表完整
  测试: verify_four_mechanism_comparison
  假设 读者想为新功能选择扩展方式
  当 读者查看对比表
  那么 表格包含：扩展粒度（功能开关/工具/能力单元/外部服务）、是否需重新构建（是/否）、面向扩展者（内部/外部开发者）、适用场景四个维度，每行有简洁说明

场景: 每种机制有"不适用"反例
  测试: verify_each_mechanism_has_counterexample
  假设 读者想判断何时不该用某种机制
  当 读者阅读各机制的"不适用场景"
  那么 每种机制都有至少一个具体的反例（如：feature flag 不适合第三方扩展者；MCP 不适合内置能力）

场景: 决策框架可操作
  测试: verify_decision_framework_actionable
  假设 读者面临"要不要给 CLI 加一个新扩展点"的决策
  当 读者按照章节的决策框架思考
  那么 能回答三个问题：谁来扩展（内部/外部）？扩展频率（一次/多次）？是否需要独立运行？并根据答案得出机制选择建议

场景: 源码证据有引用
  测试: verify_source_evidence_cited
  假设 读者想验证某机制的"代表性源码"
  当 读者检查每种机制的源码引用
  那么 每种机制至少有一处源码引用，指向该机制在 Claude Code 中的典型用例（不展开实现）

场景: 全书主题的呼应
  测试: verify_book_theme_echo
  假设 读者是在读完全书后阅读本章
  当 读者检查本章是否呼应全书主题
  那么 本章开头或结尾有对"Harness 设计哲学"的整体回顾，将四种机制定位在全书叙事中

场景: 机制选择失误的代价说明
  测试: verify_wrong_choice_consequences
  假设 开发者选错了扩展机制（如用 feature flag 做第三方扩展）
  当 读者检查代价说明
  那么 章节说明选错机制的典型后果（如维护困难、安全风险），使决策框架有实际权重

## 排除范围

- 不重复各机制的实现细节（已在各章覆盖）
- 不引入本书未分析的新扩展模式（如插件注册表、事件总线等额外机制）
- 不提供具体的代码模板（本章聚焦决策框架，非实现指导）
